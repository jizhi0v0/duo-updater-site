# Что нового в DuoUpdater

Это перевод файла `CHANGELOG.md` на русский язык. Официальной версией остаётся англоязычный оригинал, а версии, отсутствующие в этом переводе, автоматически отображаются на английском языке.

## 0.4.3

**Бета-версии и релиз-кандидаты Xcode теперь обновляются в один клик.** Войдите один раз в свою учётную запись Apple Developer в «Настройки → Xcode», и DuoUpdater будет скачивать и устанавливать новые беты и RC, как любое другое обновление. Раньше он мог только сообщить, что вышла новая.

**Устанавливайте любую версию Xcode рядом с уже установленными.** В «Настройки → Xcode» перечислены все выпуски Xcode, сгруппированные по версиям, и группа для вашей macOS уже открыта. «Установить» помещает выбранную версию в «Программы» как отдельную копию — например, Xcode-26.6.app — и ничего не заменяет; или сохраните только архив (.xip). Если вы вошли в учётную запись, добавляется собственный список Apple, поэтому новые выпуски появляются в течение нескольких минут.

**Вход в Apple Developer поддерживает себя сам.** Apple завершает сеанс разработчика примерно через восемь часов; теперь DuoUpdater получает новый в фоне — без окна и без пароля, — пока Apple ещё узнаёт этот Mac. Это можно отключить в «Настройки → Xcode».

**Новые выпуски Xcode появляются раньше.** По будним дням, в часы, когда Apple обычно публикует выпуски, DuoUpdater проверяет наличие нового Xcode каждые пять минут.

**Несколько приложений снова работают после изменений у их разработчиков.** CapCut beta и Superconductor nightly снова находятся и устанавливаются, а Superconductor сохраняет свою нынешнюю идентификацию приложения. VLC 3.0.24 и JetBrains Air снова показывают свои примечания к выпуску.

## 0.4.2

**Теперь поддерживается Qoder CN IDE.** Версия IDE Qoder для материкового Китая получает проверку обновлений, установку в один клик и примечания к выпуску в окне. Настольное приложение Qoder CN теперь тоже показывает свои примечания к выпуску.

**Qoder IDE больше не меняет мнение о последней версии.** Qoder выкатывает обновления постепенно, и DuoUpdater теперь спрашивает так же, как ваша копия IDE, поэтому каждый раз получает один и тот же ответ. Раньше предлагаемая версия могла переключаться между двумя выпусками, а установка в один клик могла остановиться на полпути, потому что ответ менялся.

**Приложения на Sparkle, скачавшие уже устаревшее обновление, снова можно обновить.** Если приложение, которое обновляет себя через Sparkle, скачало сборку, а потом вышла более новая, DuoUpdater теперь удаляет старую загрузку и устанавливает последнюю. Раньше «Обновить» раз за разом сообщала, что установка сейчас будет отменена, и строка не менялась.

**`duo install` сообщает, когда последняя сборка уже ждёт установки.** Если собственный механизм обновления приложения уже скачал последнюю версию, команда теперь говорит об этом и предлагает закрыть приложение или выполнить `duo restart`. Раньше она ошибочно сообщала, что приложение можно только проверить, но не установить.

## 0.4.1

**搜狗输入法 теперь обновляется в один клик.** DuoUpdater устанавливает обновление так же, как метод ввода обновляет себя сам — установленный бандл остаётся на месте, — и сначала копирует ваш словарь и настройки, поэтому откат вернёт и их.

**Резервные копии на другом диске теперь сохраняют словарь и настройки метода ввода.** Раньше на диск для резервных копий переносилось только само приложение, поэтому откат метода ввода оттуда оставлял на месте данные более новой версии.

**Больше никаких обновлений, которые невозможно установить.** JetBrains Air теперь показывает ту сборку, которую действительно предлагает Toolbox. Приложение из App Store, оставшееся под старым именем после того, как разработчик его переименовал, теперь сообщает, какую копию обновляет App Store, вместо того чтобы предлагать обновление, которое лишь переустанавливает другую копию.

**Примечания к выпуску ещё у ряда приложений теперь отображаются с форматированием.** Приложения, публикующие примечания в Markdown, теперь получают такой же нативный список, как и остальные. Раньше такие примечания показывались простым текстом.

## 0.4.0

**«Перезапустить» больше не соперничает с собственным установщиком приложения.** Когда приложение само скачало обновление, а его резервная копия для отката должна лежать на внешнем диске, перенос теперь ждёт, пока установщик закончит. Раньше он шёл ровно в те несколько секунд, которые macOS даёт установщику на работу.

## 0.3.99

**Резервные копии для отката теперь могут храниться на другом диске.** Резервная копия — это целая копия приложения, и на загрузочном диске они накапливаются. В разделе «Настройки» → «Резервные копии» перечислены подключённые диски со свободным местом на каждом; уже сделанные копии можно перенести туда, а новые записываются туда в фоне — если диск отключён, вы получите задержку, а не пропавшую точку отката. У `duo backups` появились `disks`, `sync`, `verify` и `probe`.

**Теперь поддерживается 千问输入法.** DuoUpdater сообщает о выходе новой версии и обновляет этот метод ввода так же, как он обновляет себя сам.

**Заметки о выпуске больше не застревают на предыдущей версии.** Когда разработчик публикует обновление за несколько минут до того, как оно появляется на странице изменений, старые заметки записывались под новой версией и оставались там навсегда. CleanShot X с самого дня выхода 5.0.1 показывал заметки к 5.0.

**Удаление резервной копии теперь действительно освобождает заявленное место.** Копии, сделанные прежними версиями DuoUpdater, записывались в виде, который macOS отказывается удалять, поэтому «Очистить» оставлял большую часть копии на диске, засчитывая её как освобождённую.

**«Очистить» открывается сразу.** Раньше при каждом нажатии каждая сохранённая копия измерялась заново.

**WorkBuddy снова показывает свою новейшую версию.** Его служба обновлений отвечает следующим шагом цепочки обновлений, а не новейшей сборкой, поэтому все четыре издания WorkBuddy отставали на два выпуска.

**Под капотом.** Приложение, которое так и не указало, какой двоичный файл оно запускает, — например, Muse от Meta — теперь всё равно раскрывает свои фреймворки и SDK.

## 0.3.98

**Обновления, завершённые через «Перезапустить», теперь можно откатить.** Когда приложение уже само скачало обновление и вы нажимаете «Перезапустить», DuoUpdater сначала сохраняет текущую версию, и это обновление появляется в разделе «Откат», как любое другое. Раньше отменить можно было только обновления, которые установил сам DuoUpdater.

**Смотрите, что обновление изменило внутри приложения.** Для любого приложения с сохранённой предыдущей версией на верстаке рядом с «Заметками о выпуске» появился вид «Различия пакета приложения»: подпись и разрешения, минимальная версия macOS, фоновые объекты и объекты входа, встроенные библиотеки, а также добавленные, удалённые и изменившие размер файлы. `duo diff` делает то же самое в Терминале для любых двух копий приложения.

**«Перезапустить» сообщает, когда собственный механизм обновления приложения не применил обновление.** Если приложение закрылось, а обновление так и не установилось, его строка теперь говорит об этом и по-прежнему предлагает «Перезапустить». Раньше индикатор просто молча останавливался.

**Cherry Studio снова показывает свою новейшую версию.** После того как Cherry Studio переименовал загрузки для Mac, DuoUpdater принимал более старый выпуск за последний: свежие копии считались актуальными, а более старым предлагалась эта старая сборка.

**Обновления из App Store больше не застревают из-за неотвечающего помощника.** Когда помощник DuoUpdater переставал отвечать, обновление замирало на 0 %, а все обновления из App Store в очереди за ним ждали, пока вы не закроете DuoUpdater.

**«Обновить всё» больше не перезапускает приложения, которые вы игнорируете.** При включённом «Автоматически перезапускать обновлённые приложения» в конце партии могло закрыться и снова открыться игнорируемое приложение или приложение, скачанную версию которого вы пропустили.

**Теперь поддерживаются Tinycast и SuperCmd.** DuoUpdater сообщает о выходе новой версии Tinycast или его беты, а также теперь распознаёт исходный SuperCmd с открытым кодом наряду с SuperCmd 2. Заметки о выпуске Kimi Code теперь тоже отображаются.

**Заметки о выпуске соответствуют предлагаемой версии.** Страница последних заметок о выпуске VS Code оказывалась пустой, а сразу после выхода новой версии панель заметок могла показывать заметки предыдущего выпуска.

**Журнал релизов больше не добавляет один и тот же выпуск при каждом запуске.** У таких приложений, как Claude, Raycast и Cline, при каждом запуске DuoUpdater появлялась повторная запись.

**Боковая панель верстака разделена на вкладки «Приложения», Brew и «Откат».** Нажмите на вкладку или проведите по вкладкам, чтобы переключиться; поиск фильтрует все три сразу.

**Под капотом.** «Перезапустить» и «Откатить» для одного и того же приложения больше не мешают друг другу; лента обновлений больше не может заставить DuoUpdater удалить файлы за пределами папки загрузок; фоновые проверки больше не рискуют вызвать запрос macOS на доступ к данным других приложений; очень большое число в фильтре «Запросы» больше не закрывает приложение; а строка меню теперь учитывает запрос прав администратора, который вы отклонили в `duo install`.

## 0.3.97

**Обновления iStat Menus теперь определяются правильно.** Когда iStat Menus перевыпускал обновление, DuoUpdater мог продолжать предлагать его уже после установки, а повторное нажатие «Обновить» заканчивалось ошибкой.

**Теперь поддерживается Memoh Desktop.** DuoUpdater сообщает о выходе новой версии и может обновить его в один клик.

**«Обновить всё» больше не просит перезапустить приложение из App Store, которое уже открылось снова.** Когда App Store закрывает приложение и снова открывает его в новой версии, его строка сразу показывает, что оно обновлено, а не «Перезапустить сейчас» до конца всей партии.

## 0.3.96

**Приложение, которое уже само скачало своё обновление, больше не скачивает его второй раз.** Некоторые приложения тихо загружают обновление в фоне и ждут, пока вы их перезапустите. Нажмите «Обновить» в DuoUpdater — теперь он установит копию, которая уже лежит на диске: обновление займёт секунды и не потребует загрузки. Раньше DuoUpdater скачивал тот же выпуск заново.

**Теперь поддерживаются Cua Driver и Petex.** DuoUpdater сообщает о новом выпуске любого из них, а Cua Driver к тому же обновляется в один клик и показывает примечания к выпуску.

**Об одном и том же обновлении больше не приходят уведомления снова и снова.** Когда сервер разработчика переключался между двумя версиями, каждая проверка выглядела как новое обновление и присылала ещё одно уведомление.

**Только что установленный пакет больше не предлагает установиться снова.** Раньше в его строке по-прежнему говорилось, что скачанный пакет будет открыт повторно.

**Для DuoUpdater теперь нужна macOS 15 Sequoia или новее.** Mac, оставшийся на macOS 14, сохраняет текущую версию, но больше не получает обновлений; любой Mac с Apple silicon можно обновить до поддерживаемой macOS.

**Под капотом.** Плановые проверки больше не получают ссылки на загрузку заранее — из-за этого, когда сервер загрузки разработчика не отвечал вовремя, рабочая кнопка «Обновить» могла ненадолго пропадать.

## 0.3.95

**Теперь поддерживается Aside.** DuoUpdater сообщает о выходе новой сборки браузера Aside и показывает её примечания к выпуску.

**Обновления, которые ваша macOS не может запустить, больше не предлагаются, а в строке теперь объясняется почему.** Если разработчик указывает, какие версии macOS поддерживает выпуск, DuoUpdater следует этому: после обновления macOS Little Snitch больше не предлагает сборку с ограничением ниже вашей системы; Xcode больше не предлагает сборку, которой нужна более новая macOS, чем у вас; а установочный пакет, чьему приложению нужна более новая macOS, отклоняется, вместо того чтобы установить приложение, которое не откроется. Строки, где раньше был пустой прочерк, теперь показывают «Пока не для этой macOS» или «Нужна новая версия macOS», а подробности — в один клик.

**Видно, с каким SDK собрано каждое приложение.** Нажмите на значок рядом с названием приложения — в подробностях теперь написано, например, «Собрано с SDK macOS 27.0.». Удобно, чтобы понять, какие приложения уже пересобраны под новейшую macOS.

**Приложения Homebrew, которые выходят двумя пакетами — для старых систем и для новейшей, — теперь читаются из нужного.** OnyX разделён так для macOS 27, а DuoUpdater смотрел только на пакет для старых систем: либо вовсе не показывал OnyX, либо считал его актуальным по версии, которую Homebrew отказывается ставить на 27. Теперь DuoUpdater следит за тем из двух, который установлен у вас.

**Бета-версии TestFlight больше не теряют своё обновление при запуске TestFlight.** Бета с ожидающей новой сборкой могла снова стать «актуальной» и оставаться такой, пока вы заново не откроете TestFlight.

**Обновление в один клик для Dropbox и ToDesk скачивает правильный файл.** На Mac с Apple silicon Dropbox раньше скачивал сборку только для Intel, которую затем отклоняла проверка безопасности; для ToDesk предлагалась сборка раннего доступа, которую получает лишь часть пользователей, вместо общего выпуска.

**«Перезапустить» сразу завершает обновление Spotify.** Когда Spotify уже скачал собственное обновление, «Перезапустить» раньше крутилось несколько минут, прежде чем обновление применялось.

**Бета-сборки переходят к своим выпускам.** Бета Xcode получает предложение своего релиз-кандидата вместо статуса «актуально»; беты CotEditor видят его релиз-кандидаты; а бета Carbon Copy Cloner, чей тестовый цикл завершился, получает выпуск, которым она стала, вместо неудачной проверки.

**Примечания к выпуску соответствуют вашей версии.** Blender 5.2 раньше показывал примечания к 5.1, а Raycast 1.x — к 2.x. «Открыть страницу» у Gemini теперь открывает текущую страницу настольной версии Google вместо ошибки.

**Настройки объясняют каждый параметр прямо рядом с ним.** Длинные пояснения под целыми разделами заменены короткой строкой под каждым элементом управления, а меню выбора описывают только выбранный вариант.

**Приложение теперь пишет своё название как DuoUpdater**, как на сайте.

## 0.3.94

**Пакеты Homebrew из tap, которым вы не доверяете, снова видны.** Начиная с Homebrew 6, brew при выводе списка установленного молча пропускает пакеты из недоверенных tap, поэтому они без объяснений пропадали из DuoUpdater. Теперь они отображаются как «Не проверено · tap не доверенный», а рядом — готовая к копированию команда `brew trust`; стоит выполнить её (или обновление) в Терминале, и окно подхватит изменения, как только вы в него вернётесь.

**Приложения, которым для самообновления нужен пароль администратора, больше не обновляются дважды.** Когда приложение вроде Tailscale уже скачало своё обновление и ждало перезапуска, DuoUpdater этого не видел и всё равно предлагал «Обновить», что могло столкнуться с собственным установщиком приложения. Теперь в строке предлагается «Перезапустить», а «Обновить всё» его не трогает.

**Раздел Homebrew теперь умеет обновлять сам Homebrew.** Когда выходит новая версия Homebrew, вверху появляется строка, которая запустит за вас `brew update`. Если у вас задан `HOMEBREW_NO_AUTO_UPDATE`, строка не показывается.

**Неудачное обновление пакета Homebrew теперь показывает настоящую ошибку brew.** Раньше в строке был совет, который brew печатает после ошибки, или вообще ничего, так что сообщение вроде «Command Line Tools слишком устарели» до вас не доходило. Кроме того, обновление нескольких пакетов сразу больше не сообщает о завершении раньше времени.

**Значок в строке меню считает больше 50.** Если ожидало больше 50 обновлений, он раньше застревал на 50.

**Заметки о выпуске: у Claude они разбиты на New, Improved и Fixed, как в самом Claude; у Mac Mouse Fix показываются текстом, а не встроенной страницей; у собственного приложения Homebrew теперь тоже есть заметки.**

**`duo check` больше не пишет «Everything is up to date.», если на самом деле не смог проверить.** Теперь он сообщает, когда не удалось прочитать TestFlight или когда сканирование приложений было прервано.

## 0.3.93

**Заметки о выпуске теперь отображаются на вашем языке, если приложение публикует их сразу на нескольких.** Некоторые приложения выпускают заметки в переводе вместе с каждым выпуском, а DuoUpdater брал тот перевод, который приложение случайно перечислило первым или последним, — поэтому заметки одного приложения у всех читались по-немецки, а у другого язык менялся от выпуска к выпуску.

**Приложения, у которых появилась версия для Mac, больше не отображаются как «Не поддерживается на этом Mac».** Приложение для iPhone или iPad, запущенное на Apple silicon, ошибочно помечалось так в тот самый момент, когда разработчик выпускал для него настоящую сборку для Mac, — то есть из-за изменения, которое делает обновление доступнее, а не наоборот.

**Приложение из App Store, бету которого вы также тестируете, больше не принимается за сборку из TestFlight.** Когда разработчик переводил бету в выпуск без изменений, у обеих сборок совпадал номер сборки, и ваша купленная копия передавалась TestFlight — так что App Store уже никогда не мог предложить ей обновление.

**Обновление из App Store, которому нужна более новая macOS, чем у вас сейчас, теперь так и говорит.** Раньше строка всё равно предлагала его установить, а App Store отказывал на последнем шаге, и ничто на экране не объясняло почему.

**Окно «Запросы» теперь показывает, насколько далеко в прошлое на самом деле уходит его журнал, и отмечает периоды, которые не может охватить целиком.** Раньше выбор «За 30 дней» для журнала, начинающегося всего несколько часов назад, выглядел ровно как выбор «За 24 часа», и ничто на экране не объясняло почему.

**Вы сами выбираете, насколько активно DuoUpdater занимается бета-версиями TestFlight, — в «Настройки → Основные».** Вариант *При обновлении* читает то, что TestFlight уже знает, и запрашивает свежий ответ, когда вы нажимаете кнопку обновления списка; *Поддерживать актуальность* позволяет DuoUpdater спрашивать и по собственной инициативе, поэтому бета из TestFlight, установленная в фоне, больше не висит вопросительным знаком до обновления списка, а ожидающая вас сборка не остаётся незамеченной за словом «актуально»; *Выключено* не читает ничего и так и сообщает в этих строках, вместо того чтобы гадать. На компьютерах Mac, где полный доступ к диску уже был предоставлен, изначально выбрано *При обновлении*, у всех остальных — *Выключено*.

**Теперь достаточно самому открыть TestFlight, чтобы DuoUpdater заметил, что тот установил.** Бета, установленная вами через TestFlight, раньше висела вопросительным знаком до следующего обновления списка — а на macOS 27, где уведомление TestFlight «Ready to Test» больше не приходит для уже установленных приложений, это могли быть часы.

**Cline теперь проверяется на обновления — и на основной линии выпусков, и на бета-линии — и показывает свои заметки о выпуске.** До сих пор он стоял с вопросительным знаком вместо версии: он не публикует ленту обновлений того вида, который DuoUpdater умеет читать, и пакета Homebrew для него нет. Теперь DuoUpdater спрашивает тот же адрес, что и собственный механизм обновления Cline, поэтому предлагается ровно то обновление, которое Cline установил бы сам, а бета-сборка остаётся на бета-треке.

**Заметки о выпуске, где изменения сгруппированы под заголовками вроде Added и Fixed, теперь сохраняют эти заголовки.** Раньше все группы сливались в один общий список, и было не понять, какие изменения — новые возможности, а какие — исправления ошибок.

**Mac Mouse Fix теперь предлагает свои бета-выпуски, если в его собственных основных настройках включено «Get Beta Versions».** Раньше DuoUpdater видел только обычные выпуски Mac Mouse Fix, поэтому бета-сборка оставалась незамеченной до выхода следующей обычной версии.

**Приложения, у которых номер сборки — просто сквозной счётчик, больше не скрывают собственные корректирующие выпуски.** Для приложения, сообщающего версию вроде 12.10 со сборкой 282987, выпуск 12.10.1 читался как «уже актуально».

**О новой сборке приложения, сохраняющей прежнее название версии, объявляют повторно.** Стоило объявить об одной сборке, как все последующие под тем же названием приходили молча: строка загоралась, значок их считал, но баннер так и не появлялся.

**«Обновить всё» теперь считает только те приложения, которые действительно обновились.** Приложение, открывающее установщик Apple, чтобы вы завершили установку сами, засчитывалось как готовое, пока его окно было ещё открыто, — поэтому «обновлено приложений: 2» могло означать, что не изменилось ничего.

**Обновление, которое установилось, но оставило после себя остаток, теперь отображается как установленное, а не как «предоставьте управление приложениями».** Новая версия уже работала, а строка в это время отправляла вас в Системные настройки.

**Остановка «Обновить всё» теперь останавливает и идущую загрузку.** Раньше многогигабайтная передача доходила до конца, повторяясь до пяти раз, и только потом замечала, что её отменили.

**Заметки о выпуске больше не путаются между двумя приложениями с общей страницей изменений и остаются актуальными после обновления для приложений, чьи заметки лежат на отдельных страницах для каждой версии.** Antigravity и Antigravity IDE могли по четверти часа показывать заметки друг друга; Thunderbird, WeChat, Opera и ещё несколько приложений некоторое время после обновления продолжали показывать заметки предыдущей версии.

**Поиск по списку приложений не обращает внимания на диакритику — как это уже делал поиск по настройкам.** Набрав «cafe», вы теперь найдёте «Café».

**На странице «Диагностика» состояние показывается отдельной строкой для каждого канала выпусков.** Раньше сломанное правило для беты или предварительной версии скрывалось за исправным стабильным соседом.

**Ответ GitHub «доступ запрещён» больше не выдаётся за превышение лимита запросов.** Из-за репозитория, ставшего приватным, или токена без нужных прав вам предлагали добавить токен, который всё равно не помог бы.

**«Перезапустить» больше не предлагается для приложения, обновляющего себя само, если ожидающая сборка старше уже работающей.**

**Под капотом.** Установка, резервное копирование и проверка пакетов больше не занимают потоки, на которых работает остальная часть приложения, поэтому меню остаётся отзывчивым, пока что-то из этого выполняется; меню также остаётся плавным во время большой загрузки; «Журнал релизов» учитывает каждый выпуск, который поставщик выпускает под одним и тем же названием версии; резервная копия для отката отклоняется, а не сохраняется, если в ней не хватает собственного исполняемого файла приложения; первый запуск на новом Mac больше не пишет в журнал ложные ошибки базы данных; `duo verify` и `duo reconcile` теперь сообщают о схлопнувшихся записях в заметках о выпуске и об адресе установщика, который не работает уже несколько дней; зависшая команда `duo` прекращает сканирование через двадцать секунд, а не висит бесконечно.

## 0.3.92

**Некоторые приложения, которые обновляют себя сами, больше не выглядят актуальными, когда уже вышла более новая версия.** Для приложений, чьи сведения об обновлении лежат за медленно обновляющимся сервером загрузок, DuoUpdater мог ещё несколько дней после выпуска видеть старую версию.

**Заметки о выпуске Kimi теперь отображаются в DuoUpdater.**

**Обновления CodeEdit теперь отображаются.** Раньше при более новой версии CodeEdit строка оставалась вопросительным знаком вместо предложения обновиться.

**Под капотом.** Список Homebrew в меню заполняется быстрее, а команды `duo` запускаются быстрее.

## 0.3.91

**«Проверить снова» для беты из TestFlight теперь даёт настоящий ответ.** Раньше строка из-за этого превращалась в вопросительный знак до следующего обновления списка.

**Бета-версии из TestFlight сохраняют свой ответ, пока кнопка обновления списка сверяется с TestFlight.** Раньше на несколько секунд все они могли превратиться в вопросительные знаки.

## 0.3.90

**Добавлена поддержка super.engineering: проверка обновлений, заметки о выпуске и установка в один клик.** Новая ночная сборка появляется в списке вместе с тем, что в ней изменилось, а «Обновить» устанавливает её за вас.

**Microsoft Edge Beta мог предложить вам стабильную сборку Edge.** Пока лента бета-канала Microsoft ненадолго пустовала, строка называла стабильную версию, а обновление установило бы её поверх вашей беты.

**Бета-версии из TestFlight теперь показывают обновления, которые для них предлагает TestFlight, включая бета-версии iPhone- и iPad-приложений.** Кнопка обновления списка в фоне сверяется с TestFlight, обновление больше не исчезает через несколько минут после того, как вы его обнаружили, а кнопка TestFlight открывает страницу этой беты.

**Если DuoUpdater не может определить, актуальна ли бета из TestFlight, он так и говорит.** Строка показывает вопросительный знак, вместо того чтобы называть бету актуальной, — например, если в TestFlight не выполнен вход или вы больше не тестируете эту бету.

**Теперь DuoUpdater объясняет, зачем нужен полный доступ к диску, и без него ничто вас не донимает.** Без этого доступа DuoUpdater больше не вызывает предупреждения macOS о чтении данных других приложений; если он требуется бете из TestFlight или CotEditor, вам один раз объяснят, зачем он нужен и где его предоставить.

**«Перезапустить» исчезает, как только обновлённое приложение завершает работу.** Приложение, оставлявшее после выхода работающий вспомогательный процесс, продолжало просить о перезапуске ещё долго после того, как обновление вступило в силу.

**Запрос конфиденциальности macOS, оставшийся без ответа, больше не стопорит проверку обновлений.** Проверка продолжается, обходясь без настройки этого приложения.

**Заметки о выпуске Rockxy и Ollama снова отображаются целиком.** Заметки Rockxy охватывают его недавние выпуски, а не только самый новый, и самый свежий выпуск Ollama больше не пропускается.

**`duo`, необязательный спутник для командной строки, лучше работает с TestFlight.** `--refresh-testflight` работает независимо от того, открыт ли TestFlight, и не захватывает ваш экран, а `duo check` больше не называет бету актуальной, если TestFlight уже объявил о более новой сборке.

## 0.3.89

**Telegram Desktop снова проверяется на обновления.** Telegram переименовал файл, который публикует, и строка больше не могла прочитать из него версию — поэтому она показывала ошибку проверки вместо обновления, ожидавшего за ней.

**Приложение для iPhone или iPad, установленное через TestFlight, распознаётся именно как таковое.** DuoUpdater принимал его за покупку в App Store, поэтому строка называла не того, кто им занимается, а у магазина запрашивалась карточка, которой не существует, — при каждой проверке, всё время, пока приложение оставалось установленным.

**Бета из TestFlight помечается собственным значком TestFlight.** Строки, которыми занимается App Store, уже несли значок магазина; строки, которыми занимается TestFlight, вместо этого выписывали название — и один и тот же вид строки был помечен двумя разными способами.

**Заголовок окна «Сеть» остаётся на месте при переключении вкладок.** Две его вкладки размещали заголовок на чуть разной высоте, поэтому переход между ними выглядел как подёргивание окна.

## 0.3.88

**Прокрутка всего списка приложений снова плавная.** При быстрой прокрутке полного списка терялись кадры; теперь каждая строка сообщает свою высоту, для чего её больше не нужно сначала собирать.

**Заметки о выпуске для приложения из App Store теперь всегда берутся из App Store.** Когда собственный запрос магазина по приложению ничего не находил или завершался ошибкой, окно могло откатиться к заметкам другого канала распространения этого приложения — другой сборки, со своей нумерацией версий — и описывало выпуск, который вашей копии никогда бы не предложили.

**Windscribe на канале Beta или Guinea Pig получает сборки именно этого канала.** DuoUpdater читает, какой канал обновлений вы выбрали в настройках самого Windscribe, поэтому копии, следующей за предварительной линией, больше не сообщают, что она актуальна, пока на этой линии есть более новые сборки. В окне также показываются заметки к этим предварительным сборкам — раньше оно перечисляло только стабильные.

**Windscribe теперь проверяется на обновления, вместе с заметками о выпуске.** Копия на старой сборке отображается с версией, на которую может перейти, и с тем, что в ней изменилось; раньше у DuoUpdater не было возможности увидеть версию Windscribe вообще. Обновление по-прежнему идёт через собственный установщик Windscribe, который настраивает и части, лежащие вне приложения.

**Обновление больше не устанавливается поверх приложения, которое исчезло, пока вы нажимали.** Если между нажатием и началом установки приложение удалили, заменили или оно перестало читаться, DuoUpdater теперь останавливается и говорит об этом, а не устанавливает по этому пути всё равно.

**`duo`, необязательный спутник для командной строки, больше не называет установку пакета завершённой раньше времени.** При установке приложения, которое поставляется как `.pkg`, открывается установщик macOS, а остальное остаётся вам — но сводка считала это установленным: «1 installed», когда ещё ничего не было заменено. Теперь это считается отдельно. Вывод `--json` также помечает каждую строку тем, что произошло с этим приложением, так что скрипту больше не нужно читать английское пояснение, чтобы отличить сбой от намеренного пропуска.

**Under the hood.** Перепроверка непосредственно перед установкой, которая защищает обновление в один клик, теперь защищает и `duo install`; а проверки, которые загрузка обязана пройти, прежде чем заменить приложение, собраны в одном месте для обоих путей, которые их используют.

## 0.3.87

**Нажатие «Обновить» больше не остаётся без результата, когда источник обновлений противоречит сам себе.** Если проверка, запускаемая в момент нажатия, возвращает версию старее той, что предлагалась в строке, DuoUpdater теперь сообщает об этом и оставляет обновление доступным. Раньше приложение считалось актуальным и исчезало из списка, а то же обновление снова появлялось при следующей проверке.

**Обновления Fork снова предлагаются, если в Fork выбран канал Develop.** DuoUpdater читал настройку канала Fork наоборот и следил за каналом Stable, который заметно отстаёт, — копия на Develop числилась актуальной, хотя сам Fork уже предлагал более новую версию.

**Mac Performance Monitor теперь показывает свои примечания к выпуску.** Приложение публикует их в своём репозитории, а не в той ленте, которую мы читаем, поэтому окну было нечего показать.

**Добавлена поддержка CotEditor — и основной линии выпусков, и бета-линии.** Какой линии следует копия, определяется установленной версией и собственной настройкой CotEditor «Update to prereleases when available», поэтому бета-копии предлагается следующая бета, а не выпуск, который откатил бы её назад.

**Приложению, установленному из App Store, никогда не предлагается загрузка из другого источника.** Если запрос к магазину не удался или вернулся пустым, строка теперь сообщает, что приложением управляет магазин, — без номера версии. Раньше проверка могла перейти к другому каналу распространения приложения — к другой сборке с собственной нумерацией — и предложить установить её поверх копии из магазина.

**Приложению никогда не предлагается обновление, которое откатило бы его на более старую версию.** В некоторых лентах стабильный выпуск стоит выше предварительной версии, которая на деле ушла дальше, — установка такого выпуска отбросила бы приложение назад.

**Длинная ошибка в строке больше не сдвигает вниз остальной список.** Она укладывается в две строки, полный текст показывается при наведении.

## 0.3.86

**Добавлена поддержка ещё четырёх программ: WhatCable, Qoder IDE, Qoder и Yaak.** Для каждой работают проверка обновлений и установка в один клик, а заметки о выпуске загружаются в окно как текст, а не как встроенная страница.

**Две программы Qoder для Mac теперь различаются.** IDE и настольная программа делят название и страницу загрузки, но выходят разными линейками версий, поэтому каждая теперь отслеживается отдельно.

**Бета-сборки WhatCable и Yaak отслеживаются на собственном канале.** У копии с бета-версией раньше не было вообще никакого источника, и она оставалась на «Failed»; теперь ей предлагается следующая бета, а заметки о выпуске отделены от заметок финальных версий. Для WhatCable сюда входит и финальная версия, в которую в итоге переходит бета, — приняв её, копия переходит на финальный канал.

## 0.3.85

**Проверка ваших программ из App Store расходует долю прежнего трафика.** Раньше при каждой проверке заново загружалась страница каждой программы из App Store; теперь страницы хранятся час, а магазин опрашивается обо всех ваших программах за несколько запросов вместо одного на программу. При интервале проверки в пять минут это примерно на треть меньше трафика в целом; при интервале по умолчанию в шесть часов страницы всё равно устаревают между проверками, поэтому там экономия меньше.

**Повторная проверка одной программы больше не загружает заново все программы из App Store.** Одно нажатие «Check Again» отбрасывало все сохранённые страницы, и следующая плановая проверка платила за все; теперь обновляется только та программа, о которой вы спросили.

**Проверка программ, которые распространяются через GitHub, тоже расходует долю прежнего трафика.** Раньше при каждой проверке заново загружалось полное описание каждого выпуска, даже если ничего не публиковалось; теперь у GitHub спрашивается, изменился ли выпуск с прошлого раза, и ничего не загружается, если не изменился. Раз в сутки каждый выпуск перечитывается целиком, поэтому отозванный выпуск обнаруживается в течение дня.

**Программы на бета- или ночном канале GitHub теперь запрашивают один выпуск вместо страницы выпусков.** Почти всегда ответом оказывается самый новый выпуск, и полная страница загружается только в те заходы, когда это не так.

**Проверка обновлений Vorssaint больше не идёт через переадресацию.** Репозиторий переименовали, и переход по старому имени молча переводил запрос на анонимный лимит GitHub; теперь проверка обращается сразу к новому имени.

**Под капотом.** Готовый выпуск теперь собирается, подписывается и нотаризуется на Mac, размещённом у GitHub, с происхождением сборки, которое может проверить любой, а в журнале запросов ответ из кэша отличается от ответа из сети.

## 0.3.84
**Выгруженные журналы запросов больше не содержат имя вашей учётной записи.** Для программ, установленных в домашней папке, в каждой строке был полный путь; теперь там `~` — каким бы способом вы ни выгрузили журнал.

**Вкладка «Requests» теперь сообщает, что в неё попадает, а что нет.** Она записывает запросы, которые DuoUpdater делает сам. Страница с заметками о выпуске сама загружает изображения и шрифты, а обновления из App Store и Homebrew выполняют отдельные инструменты — ничего этого там нет, и теперь окно об этом говорит, а не оставляет вас догадываться.

**«Copy URL» теперь экранирует адрес, который отдаёт.** Пути с пробелом — среди них загрузки Firefox, Thunderbird и Bartender — копировались как есть: браузер это исправляет, командная строка — нет.

**Строка бета-версии CapCut больше не сообщает о неудачной проверке.** Между бетами — после того как одна становится финальной и до открытия следующей — разработчик ничего не публикует на этом канале. Это выглядело как красная строка и кнопка «Retry», которая не могла сработать. Теперь строка просто не имеет ответа из этого источника, пока не появится следующая бета.

**Audacity теперь показывает значок того, на чём оно построено.** Приложение запускается через небольшой загрузчик, который передаёт управление настоящей программе рядом с ним, а значок считывался с загрузчика — а он не связан ни с чем.

**Заметки о выпуске из App Store теперь приходят на вашем языке.** Раньше они запрашивались на языке магазина по умолчанию, поэтому на Mac с китайским или японским языком они всё равно были английскими.

**Собственные обновления DuoUpdater теперь отображаются под его собственным именем в окне «Network».** Проверка версии, заметки о выпуске и загрузка значились там с пустым столбцом приложения.

**Под капотом.** Учётные данные внутри пути веб-адреса теперь удаляются до записи запроса — так же, как это давно происходит с данными в строке запроса.

## 0.3.83

**Окно «Download Traffic» теперь называется «Network» и получило вторую вкладку.** «Downloads» — это прежний журнал: во что обошлось каждое обновление как файл. «Requests» — новое: каждый запрос, который DuoUpdater делает за вас, ради чего он был, к какому приложению относился и сколько стоил, с полем фильтра, чтобы спрашивать журнал, а не пролистывать его. Цифры сверху отвечают на то, что вы отфильтровали, а не показывают итог за всё время.

**Проверка обновлений Spotify почти не расходует сеть.** Spotify нигде не публикует файл с версией, поэтому проверка читает её из двухмегабайтного установщика-заглушки — и каждый раз скачивала его целиком, круглые сутки. Теперь она сначала спрашивает, изменился ли файл, и не скачивает его, если нет.

**PDF Expert теперь находит свои обновления и показывает, что изменилось.** Он читал список выпусков, который разработчик перестал пополнять в 2022 году, и поэтому сообщал, что версия актуальна, какая бы ни была установлена. Теперь он следует тому списку, которым пользуется собственный механизм обновления приложения, и теперь показывает заметки и к прежним версиям, а не только к последней.

**Предварительные сборки UTM теперь получают собственные обновления, устанавливаются одним нажатием и показывают соответствующие заметки о выпуске.** Раньше предварительная копия сравнивалась с финальной веткой и молча получала ответ «установлена последняя версия»; теперь она следует своей линии, а получив финальный выпуск этой линии, возвращается на финальную ветку.

**Под капотом.** Записанные запросы переехали в то же хранилище, что и журнал загрузок, так что два учёта одной загрузки больше не могут разойтись. Существующий журнал переносится без изменений.

## 0.3.82

**Ещё пятнадцать приложений под наблюдением, и все, кроме одного, обновляются в один клик.** AgentsView, AnythingLLM, Chatbox, ChatGPT Classic, DSH Desktop, FluidVoice, GitHub Copilot, Kun, Meetily, Microsoft 365 Copilot, OpenLogi, OpenSuperWhisper, Paseo, T3 Code — на обеих ветках, alpha и nightly — и Vorssaint. Исключение — ChatGPT Classic: проверку мы берём на себя, но у него есть собственный установщик, который обновляет приложение сам, поэтому это обновление остаётся за вами.

**Восемь приложений теперь показывают свои примечания к выпуску прямо в DuoUpdater, а не отправляют вас на веб-страницу.** Xcode, Antigravity, Antigravity IDE, AnyDesk, AnythingLLM, Chatbox, Headlamp и Helium. У трёх из них отправлять было попросту некуда: страница AnyDesk отвечает проверкой на робота, авторы Helium не публикуют страницу с примечаниями вовсе, а у Antigravity IDE не было никакой ссылки.

**Бета-версии Xcode рассказывают, что изменилось в каждой бете.** Раньше в строке были только номер сборки и ссылка; теперь она показывает примечания Apple к вашей бете и ко всем предыдущим бетам того же выпуска.

## 0.3.81

**Word, Excel, PowerPoint, Outlook и OneNote теперь замечают, что обновление установлено.** Эти пять приложений продолжали предлагать заново открыть установщик, который вы уже завершили, и никогда не доходили до предложения перезапустить — ожидание и повторная проверка не помогали.

**Строка, ожидающая перезапуска, снова объясняет себя — в обоих окнах.** Когда приложение одновременно опережало то, что публикует поставщик, и ждало перезапуска, строка над кнопкой описывала совсем другое; для строк, ожидающих перезапуска после «Обновить все», окно и меню к тому же противоречили друг другу.

**Метки состояния больше не вытесняют имя приложения в русском языке.** Заодно исправлены метки, заимствовавшие формулировки из несвязанных строк во всех переведённых языках.

**Заметки о выпуске теперь появляются и для приложений, чей поставщик указывает только дату без времени.** Раньше такие выпуски не оставляли следа нигде.

**Страница заметок, которую мы отказываемся открывать, теперь называет причину.** Раньше она оставалась пустой, что выглядело в точности как приложение, вовсе не публикующее заметок.

**Заметки о ночных сборках WeChat DevTools больше не пустые.**

**Под капотом.** Обработка версий и дат выпуска сведена в одно место, поэтому номер сборки уже не может быть прочитан как маркетинговая версия, а хронология выпусков больше не выдумывает время суток, которого поставщик не называл.

## 0.3.80

**Неудачные проверки теперь видны и в окне.** Для строки, проверка которой не удалась, окно не рисовало ничего — как и для тех, что вы скрыли, для пропущенных версий и для приложений, которыми управляют App Store, Toolbox или TestFlight. Выглядело это ровно так же, как «обновлений нет». Теперь оба окна говорят об одном приложении одно и то же, и кнопка повтора есть в обоих.

**«Check Again» в любой строке спрашивает только про это приложение.** Заодно заново считывается список запущенных приложений — это самый быстрый способ поправить зелёную точку, которая выглядит неверно.

**Пропуск версии и скрытие приложения теперь отменяются из контекстного меню окна.** Обе строки предлагали отменить это правым щелчком — в окне, меню которого ничего подобного не содержало.

**Фоновая проверка больше не забирает заметки о выпуске, которые вы читаете.** Ежечасная проверка выбрасывала все уже загруженные заметки, и открытая панель возвращалась к индикатору загрузки. Теперь всё начинается заново только по вашему запросу на обновление.

**Прерванные загрузки проверяются, прежде чем считаться завершёнными.** И серверу, приславшему файл заново целиком, и серверу, оборвавшему передачу раньше времени, верили на слово, а беда всплывала шагом позже — установкой из повреждённого архива. Загрузки через некоторые прокси, которые раньше не удавались никогда, теперь проходят.

**Обновление одного приложения больше не перечитывает все приложения на диске.** Каждое нажатие означало два полных обхода всех ваших программ ради одной, о которой вы спросили.

**Заметки о выпуске Chrome больше не могут зависнуть.** Обычного изменения оформления блога Google хватало, чтобы застопорить их на минуты; теперь они загружаются быстро, как бы страница ни выглядела.

**Уже загруженный установщик больше не выбрасывается ради повторной загрузки.** Когда поставщик записывал один и тот же выпуск двумя способами (`v1.2.3` и `1.2.3`), ожидающий пакет не опознавался, а «Перезапустить» могло ждать замену, которая уже произошла.

**Неудачная установка с правами администратора больше не принимается за вашу отмену.** Никакой ошибки не показывалось, и это приложение молча переставало предлагать обновление в одно нажатие, пока вы снова не запрашивали права администратора из меню его строки.

**Зелёная точка «запущено» теперь замечает каждое приложение.** О запуске или завершении некоторых программ macOS не сообщает вовсе, и их точка оставалась неверной, пока её не обновляло что-то постороннее.

**Кнопка «Обновить все» больше не мигает во время проверки**, а приложение, обновлённое посреди проверки, не возвращается к предложению уже установленного обновления.

**Если проверка не удалась в тот момент, когда вы нажали «Обновить», вам об этом скажут** — вместо того чтобы записать это как «делать нечего».

**Под капотом.** Даты выпуска, записанные в необычных форматах, читаются правильно, проверка адресов страниц с заметками о выпуске охватывает все равнозначные написания, а рутинный учёт больше не обращается к диску каждый раз, когда на вашем Mac открывается или закрывается любое приложение.

## 0.3.79

**Метка Docker теперь описывает интерфейс Docker, а не его фоновую службу.** В строке значилось «нативное»; но Docker Desktop — приложение на Electron. Метка читается из бандла приложения, а бандл Docker — это оболочка: программа, которую он называет, — фоновая служба, написанная на Go, собственного фреймворка она не несёт, а приложение, которое на самом деле рисует окна, лежит уровнем внутри. Читалось всё правильно — просто не из того файла. Теперь DuoUpdater заглядывает во вложенное приложение, но только если внешнее само по себе ничего не даёт и содержит ровно одно такое приложение, которое доказывает, на чём оно построено; так что вспомогательный процесс, поставляемый рядом с настоящим интерфейсом, по-прежнему не может одолжить свою личность хозяину. Docker читается как Electron 42.5.0, и из ста сорока шести приложений в списке на машине, где это писалось, меняется только эта строка.

**Заметки о выпусках CleanShot X снова читаются — в том числе для 5.0.** CleanShot выпустил самое крупное обновление за годы и заодно перестроил страницу с описанием изменений: дата переехала выше номера версии, вокруг него появилось два новых слоя, а в функциональном выпуске между версией и списком изменений теперь вставлены вступление и две ссылки на видео. Правило чтения, которым пользовался DuoUpdater, перестало совпадать хоть с чем-нибудь из этого. Хуже пустой панели было то, что пустой она не выглядела: уже сохранённые заметки предыдущего выпуска остались на экране под заголовком нового — панель говорила «5.0» над изменениями 4.8.10, и нигде об этом не сообщалось.

**Заметки, сохранённые до публикации выпуска, теперь перечитываются, а не принимаются на веру навсегда.** Это вторая половина той же истории. DuoUpdater узнал о существовании 5.0 за шесть минут до того, как CleanShot опубликовал её содержание, сохранил страницу в том виде, в каком она была, и подшил её под 5.0 — и с тех пор каждая проверка находила уже сохранённое и больше не смотрела. Теперь сохранённые заметки один раз за сеанс подтверждаются у поставщика, прежде чем считаться окончательными, так что версия, чьи заметки пришли позже, дополняет себя при следующем открытии, а не остаётся неверной.

**То, что вы открываете из строки меню, теперь открывается с первого щелчка.** Выбор «Что нового» в строке — или Настроек, или журнала выпусков — в первый раз не делал ровно ничего и срабатывал только со второго щелчка. Повторное открытие меню снова превращало каждую попытку в «первую», так что для тех, кто открывает меню, щёлкает один раз и ждёт окно, это не работало никогда. Первый щелчок внутри меню уходил на то, чтобы вывести DuoUpdater вперёд, вместо того, на что он был нацелен. Открытое окно теперь ещё и удерживается впереди: его выводили вперёд и в тот же миг задвигали обратно — второй способ, которым всё это выглядело как щелчок без последствий.

## 0.3.78

**Приложение, оставившее собственное имя пустым, теперь всё равно его получает.** Eudic (欧路词典) стоял в списке со значком, с версией — и с пустотой там, где должно быть имя. Его бандл действительно объявляет отображаемое имя — и оставляет его пустым, потому что настоящие имена лежат в переводах самого приложения, — а DuoUpdater принимал этот пустой ответ за ответ, вместо того чтобы задать следующий вопрос. Теперь он переходит к другому имени приложения, а затем к имени самого файла приложения, так что ни одна строка не остаётся безымянной. Здесь это касалось одного приложения из ста пятидесяти; суть в том, что сведения всё это время были на месте, просто их пропускали.

**Перезапуск теперь сообщает, что приложение отказалось завершиться, а не выглядит как нажатие, от которого ничего не произошло.** Если у приложения открыто окно, которое вас ждёт — запрос на сохранение, форма входа, какой-нибудь диалог, — macOS не даст ему завершиться, и с этой стороны сделать нельзя ничего: это окно самого приложения. Раньше DuoUpdater крутился тридцать секунд, а затем без объяснений возвращал ту же кнопку — а это выглядит ровно как сломанная кнопка, и её нажимают снова. Теперь строка говорит, что произошло, и отправляет вас к приложению разобраться с этим окном. Ничего не изменилось, новая версия уже установлена, так что остался действительно только перезапуск — и если вы просто закроете приложение сами в ближайшие десять минут, DuoUpdater это заметит и вернёт его уже на новой версии, без единого нажатия с вашей стороны.

**Примечания к выпуску 欧路词典 снова относятся к одному выпуску, а не к шестнадцати годам выпусков.** Приложение публикует всю свою историю — каждую версию вплоть до 2.5.0 — внутри примечаний к тому выпуску, который оказался последним, поэтому панель подробностей показывала всё это под заголовком «26.9.0», и до того, что изменилось только что, приходилось прокручивать десятилетие. Теперь каждая версия получает в списке собственную запись — так же, как у всех остальных приложений.

**Значок, показывающий, на чём сделано приложение, теперь требует для Tauri доказательства, а не сходства.** Tauri не оставляет в бандле ничего, что можно найти, — ни фреймворка, ни собственной папки, — поэтому именно этот значок выводился из того, как приложение упаковано, плюс из того, что оно связывается с веб-представлением Apple. Longbridge подходит под всё это и при этом не Tauri: свои окна оно рисует тем же движком, что и Zed, а веб-представление встраивает лишь в один угол интерфейса. Теперь DuoUpdater, прежде чем это утверждать, читает из двоичного файла собственный отпечаток Tauri — так что приложение называется Tauri тогда, когда оно им является, а Longbridge читается тем, чем оно и есть: нативным приложением для Mac.

**CapCut больше не краснеет из-за того, что у серверов ByteDance выдалась неудачная полусекунда.** Точка доступа, у которой DuoUpdater спрашивает версию CapCut, отвечает кодом успеха, а затем примерно в одном случае из пятидесяти — объектом ошибки вместо ответа: это тайм-аут внутри собственной инфраструктуры ByteDance, около 390 байт там, где ожидалось 436 килобайт. С этой стороны отличить такое от того, что CapCut изменил форму своего ответа, было невозможно, и строка сообщала о неудачной проверке — а это читается как «сломалось, кто-то должен починить» применительно к тому, что само проходит со следующей попытки. Теперь эта конкретная форма распознаётся как то, чем она является: запрос немедленно повторяется, и о нём сообщают как о настоящей проблеме, только если он держится пять дней.

## 0.3.77

**Каждое приложение в списке теперь говорит, на чём оно сделано.** Строка приложения из App Store всегда несла значок магазина, а все остальные выглядели одинаково — приложение на Sparkle, приложение на Electron и нативное было не отличить. Теперь рядом с каждым именем стоит знак самой технологии: нажмите — увидите название и одну фразу, наведите — подсказку: Electron, Tauri, Flutter, Qt, Java, Chromium, Mac Catalyst, приложение для iPhone на Apple silicon или нативное приложение для Mac — вместе с версией среды там, где её можно прочитать как факт (Electron 42.4.1, Qt 6.2, встроенный в приложение Chromium, тот Tauri, на котором оно собрано). Всё это читается из самого пакета — фреймворка, который пришлось положить сборщику, среды, нужной запускающему файлу, библиотек, с которыми слинкован бинарник, — то есть это факт о том, что установлено, а не догадка по имени приложения. Когда длинное имя не оставляет места, знак уступает, а не выталкивает имя на вторую строку: строка принадлежит имени. Выключается всё это в Настройках → Основные.

**LibreOffice больше не заявляет о переходе на версию, которая у вас уже стоит.** Его индекс загрузок публикует версии из трёх частей (`26.8.0`), тогда как установленная копия сообщает о четырёх (`26.8.0.3`), и заполнение недостающей части нулём делало установленную копию новее — из-за чего в строке появлялась приглушённая заметка «поставщик отстал от вас» о приложении, которое было в точности актуальным. Источник, публикующий меньше частей, чем сообщает приложение, теперь читается как описание того же выпуска, а не более старого; настоящий откат по-прежнему называется своим именем.

**Настройка, добавленная обновлением, теперь один раз указывает на себя.** Новый параметр посреди окна настроек — это параметр, которого никто не найдёт. После обновления, которое его добавило, на шестерёнке в строке меню появляется синяя точка, на нужной странице — точка в боковом списке, и на самом элементе — пока вы на него не посмотрите. Только для тех, кто действительно обновился до него: новую установку не встречают точками на возможностях, которые для неё просто часть приложения.

## 0.3.76

**Приложения на Electron, добавленные в прошлом выпуске, теперь действительно обновляются одним нажатием.** В 0.3.75 DuoUpdater научился читать файл, который такие приложения носят внутри себя, — и он его читал: определял новую версию, нужную именно вашему Mac загрузку и контрольную сумму для её проверки. Только отправить всё это было некуда. Кнопка установки не появлялась никогда, а `duo install` объяснял отказ причиной, которая не была настоящей. В этом выпуске путь установки подключён. На практике вы можете не увидеть никакой разницы: это чтение стоит позади каждого написанного вручную правила, поэтому оно говорит только за приложение, которое больше ничем не покрыто, — а сегодня, на машине, где это собиралось, таких нет ни одного. Это важно для приложения, которое вы установите завтра и для которого правила ещё никто не написал.

**Загрузка, которая увела бы вас с нативной сборки, теперь отклоняется.** Apple Silicon по-прежнему может выполнять приложения Intel через трансляцию — значит, загрузка только для Intel проходила проверку «запустится ли это на вашем Mac» и устанавливалась без единой жалобы, оставляя вас с транслируемой копией приложения, которое до этого работало нативно; тихо, и повторяя то же самое при каждом следующем обновлении. Теперь любая установка на месте сравнивает то, что у вас есть, с тем, что пришло, и отказывается от такой замены. В обратную сторону, а также с универсальной сборки на сборку для Apple Silicon, — обычное дело и по-прежнему разрешено.

**Когда доказать архитектуру загрузки нельзя, теперь следует отказ, а не догадка.** Некоторые поставщики публикуют сборку для Apple Silicon рядом со своей загрузкой по умолчанию, и единственный способ понять, что по умолчанию идёт сборка Intel, — заметить, что существует вторая. Если эта вторая проверка не возвращается чисто — сервер поставщика её отклоняет, соединение обрывается, или обе расходятся в том, какая это версия, — DuoUpdater больше не принимает это молчание за ответ. Он сообщает версию и не предлагает установку; это честный исход.

**Сломанное правило больше не может исчезнуть из «Диагностики».** Для приложений, покрытых одновременно написанным вручную правилом и новым чтением — а таких большинство, и это сделано намеренно, — сбой правила гасился успехом чтения сразу следом, и приложение с действительно сломанным правилом продолжало выглядеть исправным. Теперь они записываются раздельно. Кроме того, новое чтение впервые сообщает о собственных сбоях: на машине, где это собиралось, три приложения указывали на адреса, которые уже некоторое время отвечают «не найдено», и до сих пор об этом ничто не говорило вслух.

**Строки, которые видят обновление, но не могут его установить, теперь предлагают одно и то же в обоих местах.** В строке меню показывалась кнопка «Открыть», открывавшая Finder, а в окне для той же строки не показывалось ничего.

## 0.3.75

**Приложения на Electron теперь распознаются без того, чтобы кто-то заранее вносил их в список.** Очень многие приложения для Mac носят внутри себя небольшой файл, где указано, где искать их обновления. До сих пор DuoUpdater знал только те, для которых кто-то вручную написал правило, — все остальные стояли в списке без номера версии, и сообщить вам о новой сборке было нечем. Теперь он читает этот файл напрямую, ровно так же, как всегда читал файл Sparkle. Благодаря этому подобное приложение оказывается охвачено в тот день, когда вы его установили, а не в тот, когда до него дошли руки.

**Приложения, для которых правило уже было написано вручную, остаются как были.** Новый способ чтения стоит позади них, поэтому он способен лишь закрыть пробел и никогда не перехватит то, что и так работало.

**Правильность предлагаемой загрузки решают две детали, и обе выяснены на настоящих приложениях, а не приняты на веру.** Одни поставщики помечают сборку для Intel как «основную» загрузку, хотя рядом публикуют и сборку для Apple Silicon; другие дают сборке для Apple Silicon имя файла, ничем не отличимое от интеловского, так что по имени ничего не понять. DuoUpdater выбирает по архитектуре, а там, где он не может быть уверен, что загрузка запустится на вашем Mac, он называет версию и не предлагает установку — вместо того чтобы вручить вам нечто, что установится без единой заминки, а потом не откроется.

## 0.3.74

**Если вы пользуетесь бета-версией, релиз-кандидатом или ночной сборкой приложения, DuoUpdater всё это время незаметно следил не за той веткой.** Он определяет вашу ветку, находя вашу сборку в списке выпусков самого разработчика, — но предварительная версия обычно сохраняет тот же публичный номер, что и стабильный выпуск, из которого она выросла, и для предварительной версии это совершенно обычное поведение. В итоге совпадение приходилось на стабильную запись. Ошибка была беззвучной: на экране ничего не выглядело неправильным, вы просто переставали узнавать о следующей сборке своей ветки, а показанные примечания к выпуску относились к стабильной линии. Это выяснилось, когда реальные предварительные сборки Supacode и TypeWhisper были установлены и проверены в деле; теперь обе следуют той ветке, на которой действительно находятся.

**Бета CapCut показывала версию, которой у вас нет.** В строке значилось «9.3.4545 → 9.4.0-beta6», тогда как сам CapCut, Finder и любой другой инструмент обновления называли вашу копию 9.4.0-beta5. Некоторые приложения записывают настоящую версию не в то поле, что большинство, и DuoUpdater со своей стороны стрелки читал более опрятное на вид значение, а со стороны разработчика — настоящее. Предлагаемое обновление всегда было верным: неверной была только подпись. Теперь обе половины строки берутся из одного места — и для CapCut, и для семи других приложений, устроенных так же.

**MacWhisper, GitHub Copilot for Xcode, TypeWhisper и OpenUsage теперь показывают примечания к выпускам.** У всех четырёх панель примечаний оставалась пустой: в их лентах обновлений примечаний нет вовсе, и это не замечали, пока каждую не сверили с тем, что разработчик публикует на самом деле. Теперь DuoUpdater читает собственную страницу примечаний MacWhisper, файл изменений Copilot и сайт изменений TypeWhisper, а OpenUsage направляет к его списку выпусков.

**Helium теперь обновляется через собственную службу обновлений, что даёт бета-канал и заметно меньшие загрузки.** Раньше он отслеживался по публичному списку выпусков, где появляются только стабильные сборки, — то есть тем, кто пользовался бетой Helium, выдавалась стабильная версия. Собственная служба публикует ещё и патчи, поэтому обычное обновление весит около 40 МБ вместо повторной загрузки на 124 МБ. Проверено на стабильной и на бета-сборке, а загрузка сверяется с ключом подписи внутри вашей установленной копии, прежде чем что-либо будет заменено.

## 0.3.73

**Бета-, developer- и nightly-выпуски Firefox и Thunderbird теперь отслеживаются как надо — до сегодняшнего дня ни одна из этих пяти линий выпуска ни разу не сообщила об обновлении.** Их читали из открытого файла версий Mozilla, где публикуется только та версия, которую вам показывают, а при установке беты «b5» с неё срезается: бета Firefox весь цикл называет себя 155.0, так что единственный задаваемый вопрос звучал как «155.0b5 новее, чем 155.0?», и ответ — нет. С nightly было ещё хуже: Mozilla выпускает по одной каждый день, и все они называются 157.0a1, поэтому четырёхнедельный цикл не давал ровным счётом ничего. Теперь DuoUpdater обращается к собственной службе обновлений Mozilla — по тому же адресу, которым пользуется встроенный апдейтер Firefox и который приложение хранит внутри себя, — и сравнивает идентификатор сборки, который есть у обеих сторон. Перед выпуском это сверили с настоящими загрузками по всем пяти линиям: идентификатор, который сообщает эта служба, побайтно совпадает с тем, что лежит внутри вашей копии; бета, отставшая на одну сборку, теперь показывает обновление, и nightly, собранный раньше в тот же день, тоже. Стабильный выпуск и ESR никогда не были затронуты и остались без изменений. Одно ограничение стоит назвать прямо: эта служба не публикует дату выпуска, поэтому для этих пяти линий журнал релизов по-прежнему фиксирует, когда DuoUpdater впервые увидел сборку, а не когда её выпустила Mozilla.

## 0.3.72

**Wispr Flow, AionUi и Devin теперь обновляются в один клик, а не просто сообщают, что вышла новая версия.** Все три и раньше видели свои обновления, но ни одно не могло его применить — с формулировкой, что поставщик выпускает отдельные сборки для Intel и Apple Silicon, а у DuoUpdater нет способа выбрать нужную. Проверка вместо предположения показала, что причина не выдерживает: конечные точки, из которых читаются эти три приложения, и так предназначены для Apple Silicon, а сам DuoUpdater работает только на Apple Silicon — выбирать было не из чего. Каждую загрузку получили и проверили до того, как подключить: внутри нужное приложение, подписанное тем же разработчиком, что и ваша копия, заверенное Apple, а у AionUi — ещё и сверенное с контрольной суммой, которую публикует его собственный манифест. Затем каждое действительно установили поверх более старой копии и убедились, что оно вернулось на новой версии.

**Grok Bot теперь отслеживается — новые версии и обновление в один клик.** Настольное приложение xAI собирает и подписывает Anysphere, компания, стоящая за Cursor, и обновляется оно через собственный сервис выпусков Cursor. Именно поэтому ни один из обычных путей не отвечал за него: нет ленты Sparkle, нет карточки в App Store, нет публичного репозитория релизов, а cask в Homebrew возвращает обновление самому приложению. Вместо этого DuoUpdater читает собственную конечную точку версии у поставщика. Были ещё две, и обе оставлены в стороне по названным причинам: та, что за кнопкой загрузки на x.ai, вообще не публикует номер версии, а та, которой пользуется собственный апдейтер приложения, отвечает пустым телом, когда у вас уже актуальная версия, — молчание, неотличимое от сломанной конечной точки. Загрузку проверили до того, как её подключить: в образе диска лежит нужное приложение, подписанное тем же разработчиком, что и ваша копия, и заверенное Apple. Одного он намеренно не предлагает — примечаний к выпуску: xAI не публикует их для этого приложения, а единственный changelog, который есть, относится к другому продукту.

**Comet и Msty Studio теперь тоже обновляются в один клик, а новая проверка следит за приложениями, которые могли бы, но не обновляются.** Это были последние двое из той группы, кто видел обновления, но не мог их применить, — и ни одному не понадобилось ничего нового: достаточно было внимательнее посмотреть, что именно присылает поставщик. Ссылка на загрузку Comet подписана и живёт час, поэтому кнопка обновления указывает на собственный шлюз Perplexity, а ссылка создаётся в момент нажатия, а не часами раньше, при проверке. Msty публикует четыре загрузки в одном файле, версия для Intel идёт первой, и отпечаток для проверки загрузки брался именно из этой первой записи; теперь он привязан к той сборке для Apple Silicon, которая действительно скачивается, — а заодно выпуск, вышедший между проверкой и нажатием, в большинстве случаев будет замечен, а не установлен молча. Обе загрузки перед выпуском были скачаны и проведены через настоящие установочные заслоны. Более общий вывод не касается ни того, ни другого приложения: ничто в DuoUpdater не могло заметить, что приложение осталось только для обнаружения по причине, которая давно перестала быть верной. Теперь ночной обход рецептов сообщает, когда приложение, за которым мы лишь наблюдаем, уже протягивает нам установщик.

**QQ音乐 теперь отслеживается — новые версии, обновления одним кликом и его заметки о выпуске прямо в окне приложения.** Ни один из обычных путей не отвечал за него. Он не публикует Sparkle-поток и вообще не несёт фреймворка Sparkle; его cask в Homebrew возвращает обновление самому приложению; а конечная точка обновлений, которую называет по имени его собственный бинарник, отвечает 200 и пустым телом на всё, что бы мы её ни спросили, — молчание, неотличимое от сломанной конечной точки, поэтому её оставили в стороне. Вместо неё DuoUpdater читает файл данных, из которого строится собственная страница загрузки поставщика: эта страница отдаётся пустой оболочкой и заполняется из этого файла — он же единственное место, где вообще существуют заметки о выпуске: нет ни блога, ни appcast, ни отдельной страницы на каждую версию. Поэтому заметки читаются прямо из него и отображаются в собственном окне DuoUpdater, а обновление устанавливает нотариально заверенный образ диска, названный в том же ответе. Одна ловушка стоит того, чтобы её записать: этот файл до сих пор несёт рядом с текущей записью вторую, шестилетней давности запись для Mac, поэтому оба правила опираются на версионированное имя файла загрузки для Mac, а не на само слово «Mac». И одно ограничение стоит назвать прямо: файл публикует ту версию, которую вам показывают, но никогда — номер сборки под ней, поэтому переупаковка версии, сохранившей своё имя, здесь невидима. Приложение не выдумывает обновление, которого нет, — оно просто не способно увидеть этот один тип.

**TimeMachineEditor теперь отслеживается — новые версии и обновления одним кликом.** Ни один из обычных путей не отвечал за него: нет ленты Sparkle (в приложении нет ни адреса ленты, ни фреймворка, который мог бы её использовать), нет карточки в App Store, нет публичного репозитория релизов, а cask в Homebrew возвращает обновление самому приложению. Зато у него есть небольшой сайт поставщика, единственная ссылка на загрузку которого называет версию прямо в своём тексте, — та же страница, прочитанная тем же способом, на который опирается собственная проверка версии в Homebrew, так что это та поверхность, которую и подразумевал поставщик, а не догадка. Обновление устанавливается как собственный пакет установщика поставщика, а не как простая замена приложения, и это сделано намеренно: загрузка также кладёт снаружи самого приложения фоновый планировщик, инструмент командной строки и launch daemon, поэтому замена одного лишь приложения оставила бы новую копию рядом с устаревшим планировщиком, и ничто не заметило бы несовпадение.

**Little Snitch теперь отслеживается — и на стабильных, и на ночных релизах.** Object Development публикует файл версии, на который опирается собственный апдейтер приложения, — тот же файл читает Homebrew, — и DuoUpdater читает его напрямую, потому что Little Snitch не публикует ленту Sparkle, а его cask возвращает обновление самому приложению. Оба релиза несут одну и ту же идентичность, из-за чего их обычно невозможно отличить друг от друга; но этот выдаёт себя в самой строке версии: ночная сборка пишет слово полностью («6.5 nightly (7301)»), тогда как стабильная сборка читается как простое «6.4.1». Стоит сказать прямо, чего это намеренно не делает: оно не устанавливает обновление за вас. Little Snitch запускает вместе с приложением системное расширение и привилегированную фоновую службу, а сохранится ли это в целости при простой замене приложения — не проверялось на настоящей машине, поэтому DuoUpdater сообщает вам, что вышла новая версия, и отправляет вас к собственной загрузке поставщика, вместо того чтобы гадать с сетевым фильтром.

**Carbon Copy Cloner теперь отслеживается, и он не станет пытаться перевести вас между старшими версиями.** Bombich одновременно поддерживает живыми три поколения — CCC 5, 6 и 7 — и все они сообщают системе одну и ту же идентичность, из-за чего вопрос «есть ли что-то новее?» оказывается хитрее, чем кажется: 7.1.6 действительно сортируется выше 6.1.13, но переход между ними — это отдельная покупка, а для CCC 7 нужна более новая macOS, чем та, что вообще может стоять на машине с CCC 5. Ответ по одному лишь номеру версии говорил бы каждому владельцу CCC 5 и 6, что его ждёт бесплатное обновление, — вечно, и каждый раз неверно. Поэтому DuoUpdater читает каждое поколение с его собственного адреса и предлагает только следующий релиз внутри того самого поколения, которое у вас действительно установлено. Бета-релизы тоже подхватываются — для тех, кто включил их прямо внутри CCC. Пока это только обнаружение — CCC устанавливает рядом с приложением привилегированный помощник, а это более серьёзная заявка, чем уже имеющиеся здесь обновления в один клик, — и стоит записать, почему раньше эти обновления вообще не были видны: собственная лента обновлений приложения успешно отвечает на каждый запрос совершенно пустой страницей.

**Обновление, которое ваш Mac на самом деле не может запустить, больше не загружается и не устанавливается.** Каждый бандл приложения указывает самую старую macOS, на которой оно запустится, а некоторые поставщики называют и самую новую — «эта сборка не для настолько новой ОС», это именно то, как приложение, ещё не угнавшееся за macOS 27, об этом сообщает. DuoUpdater не читал ни то, ни другое. Для приложений, отслеживаемых через ленту Sparkle, нижняя граница уже соблюдалась, но таких меньшинство: среди приложений на типичном Mac те, что читаются из собственной конечной точки поставщика или из релизов GitHub, составляют большую половину, а релиз GitHub вообще нигде не публикует требование к macOS. Так что приложение, ушедшее на более новую macOS, чем ваша, могло быть предложено, полностью загружено, подставлено на место — а затем просто не открыться. Теперь DuoUpdater читает это требование прямо из загруженного приложения, рядом с уже существующей проверкой того, что оно собрано под ваш процессор, и отказывается от замены, вместо того чтобы заменить рабочую копию той, что не запустится. Верхняя граница, названная поставщиком, тоже соблюдается там, где её публикует лента, поэтому сборка, которую разработчик пометил как не предназначенную для вашей macOS, вообще не предлагается. Три ограничения стоит назвать прямо. Чтение требования из загрузки означает, что загрузка уже состоялась, — это предотвращает сломанную установку, а не сам трафик, потому что для большинства приложений раньше спросить попросту негде. Приложение, чью новейшую версию ваш Mac не может запустить, будет и дальше показывать это обновление и дальше отказываться его устанавливать; отказ теперь называет нужную версию macOS, но DuoUpdater пока не запоминает этот ответ и не перестаёт предлагать обновление. И проверка охватывает только те пути, где DuoUpdater сам подставляет бандл приложения на место: пакет установщика передаёт файл macOS, которая сама следит за требованиями пакета, а Homebrew и App Store сами выбирают нужные им сборки; приложения, пришедшие из раздела App Store для iPhone и iPad, пропускаются намеренно, поскольку указанная в них версия — это версия iOS, и сравнивать её с macOS было бы хуже, чем вовсе не проверять. Одну вещь это пока не проговаривает вслух: там, где разработчик пометил сборку как не предназначенную для вашей macOS, это обновление попросту не предлагается, а приложение читается как актуальное, вместо того чтобы объяснить почему. Для более старого Mac это само собой разрешается в день, когда вы обновите macOS. Для Mac, который слишком новый, — нет, и здесь причитается ответ получше, чем молчание.

## 0.3.71

**百度网盘 теперь отслеживается — новые версии, обновления одним кликом и его заметки о выпуске прямо в окне приложения.** Ни один из обычных путей не был открыт. Он не публикует Sparkle-поток; Homebrew cask ничего не знает о копии, установленной вручную; а манифест обновлений, который его собственный бандл называет по имени, перестал отвечать — этот файл, его arm64-версия и каталог, в котором лежит всё это, возвращают 404. Поэтому DuoUpdater читает конечную точку, на которой построена собственная страница загрузок поставщика, и устанавливает нотариально заверенный образ диска arm64, указанный в том же ответе. Заметки о выпуске приходят тем же путём: на странице «版本更新» есть вкладка Mac版, но сама страница отдаётся пустой и заполняется из API, поэтому DuoUpdater читает этот API напрямую и показывает в своём окне последние сорок релизов. Одно ограничение стоит назвать прямо: этот поток публикует ту версию, которую вам показывают, но никогда — номер сборки под ней, поэтому переупаковка версии, сохранившей своё имя, здесь невидима. Приложение не выдумывает обновление, которого нет, — оно просто не способно увидеть этот один тип.

**Проверка, которая не прошла, теперь так и говорит, вместо того чтобы показывать пустоту.** У приложения, версию которого может назвать только собственная конечная точка поставщика, был один способ сказать «нет ответа»: пустое тире — точно так же выглядит и приложение, которое вообще ничем не охвачено. Поэтому истечение времени ожидания у конечной точки поставщика или её переезд читались ровно как «DuoUpdater не отслеживает это приложение» — вердикт, выглядящий постоянным, без предложенной кнопки Повторить и без места в счётчике «приложения не удалось проверить». Теперь это неудачная проверка, чем она и является: кнопка Повторить, место в счётчике и названная причина. Намеренно не всё: положение, с которым ваш Mac попросту ничего не может поделать, — нет рецепта для трека выпуска, на котором вы сидите, конечная точка требует идентификацию, которой у этой машины нет, — по-прежнему остаётся тем же тихим тире, каким было всегда, а проверка, три раза подряд закончившаяся неудачей, всё равно выходит из баннера, а не закрепляется в нём навсегда. Приложение, которое устанавливает JetBrains Toolbox, тоже сохраняет свою кнопку «открыть Toolbox», когда чтение версии за ней проваливается, — ведь открыть Toolbox было ответом в любом случае.

**Напоминание «перезапустите, чтобы применить» теперь исчезает, как только вы перезапустились.** Когда собственный апдейтер приложения загружал сборку в фоне, DuoUpdater сообщал вам об этом и предлагал в уведомлении кнопку Перезапустить. Нажатие её срабатывало — приложение возвращалось на новой сборке и говорило «Теперь запущена версия 1.0.» — но напоминание, которое оно заменило, оставалось лежать в Центре уведомлений под ним, по-прежнему прося перезапустить то, что вы только что перезапустили. DuoUpdater следил за тем, когда напоминание *перестанет* быть актуальным, а момент, в который он это проверял, был как раз тем моментом, который уже был учтён. Теперь он убирает напоминание, как только не остаётся ожидающего перезапуска, — это заодно убирает и то, что оставило приложение, применившее собственное обновление, пока DuoUpdater не был запущен.

## 0.3.70

**Приложения, которые выпускают много сборок под одним номером версии, теперь везде обрабатываются правильно.** Приложение для Mac несёт две версионные строки: ту, что оно вам показывает («1.0»), и номер сборки, который действительно растёт. Большинство приложений меняют обе. Некоторые меняют только сборку: Amp выпустил за один день десять сборок, и все они назывались 1.0; Surge выпустил четыре отдельных релиза под номером 6.9.0; предварительные сборки JetBrains делают то же самое. DuoUpdater решал вопрос «изменилось ли это?», сравнивая *показываемую* версию примерно в дюжине мест, а для таких приложений это сравнение способно ответить только «нет» — или, там, где спрашивалось «это одно и то же?», только «да», — что бы на самом деле ни произошло. Дальше идёт то, что это ломало. Это одна-единственная ошибка, обнаруженная потому, что Amp сделал её видимой.

**Перезапуск больше не выглядит зависшим на три минуты, а затем не сообщает о сбое, которого не было.** Нажатие Перезапустить у приложения, чей собственный апдейтер уже держал наготове сборку, крутило спиннер три минуты, а затем говорило, что попытка провалилась, — хотя на самом деле обновление применилось за секунду-две, и приложение уже успело открыться заново на новой сборке. DuoUpdater ждал у показываемой версии изменения, которое просто не должно было наступить. На Amp это измерили: 189 секунд спиннера ради замены, занявшей меньше секунды. Теперь это укладывается в куда меньше секунды, а настоящий сбой по-прежнему сообщается как сбой.

**Перезапуск больше не предлагает вам сборку, которая уже устарела.** Когда апдейтер приложения уже загрузил одну сборку, а разработчик с тех пор опубликовал другую, DuoUpdater всё равно предлагал Перезапустить — так что вы перезапускались и сразу же оказывались на сборку позади, а ведь именно это должна была предотвращать эта проверка. Теперь такие строки вместо этого предлагают **Обновить**, что подтягивает актуальную сборку.

**Пропуск версии больше не заставляет приложение молчать навсегда.** Вот это стоит знать особо. «Пропустить эту версию» должно отклонять один релиз и пропускать следующий. Оно записывало только показываемую версию, поэтому для приложения, которое сохраняет одно имя версии между сборками, однократный пропуск отклонял *каждый* будущий релиз — постоянно, переживая перезапуски, и на экране не было ничего, что говорило бы, что приложение замолчало. Теперь пропуск записывает ту сборку, которую он отклонил. Одно следствие этого исправления: версия, пропущенная более старой версией DuoUpdater, будет предложена вам ещё раз, потому что старая запись не может сказать, какую сборку она имела в виду. Пропустив её заново, вы запишете это уже правильно.

**Откат снова доступен для обновлений, которые выглядели так, будто ничего не изменили.** Верстак скрывает откат, который ничего бы не сделал, — а для этих приложений каждый откат выглядел именно так, будто он ничего не сделает, поэтому строка исчезала после настоящего обновления, хотя на диске лежала полная резервная копия, до которой не было способа добраться.

**Напоминание о перезапуске снова объявляется один раз на сборку — и переживает публикацию разработчиком ещё одной.** В 0.3.69 напоминание, повторявшееся каждые пять минут, заменили на такое, которое говорит один раз за подготовленную сборку, — но сборку в нём определяли по имени, поэтому для таких приложений оно объявляло первую сборку, а затем молчало по поводу каждой следующей. Это исправлено. Отдельно: если приложение отказывается закрываться из-за несохранённой работы, DuoUpdater хранит заметку, чтобы открыть его снова, как только вы ответите. Раньше эта заметка выбрасывалась, если разработчик публиковал ещё одну сборку, пока вы раздумывали, — и приложение оставалось закрытым после замены, и ничто не открывало его снова. Теперь она следует за новой сборкой.

**Неудачное обновление из App Store больше не тихо записывается как успешное**, а загруженный пакет установщика больше не считается тем, что сейчас предлагается, если это более старая сборка. Оба случая происходили из одного и того же сравнения. История релизов тоже засчитывала десять сборок приложения как один релиз; отныне они считаются отдельно, хотя уже записанную историю восстановить нельзя.

**Сама строка теперь говорит, какую сборку применит перезапуск.** Она читалась как «1.0 → 1.0» — строка, не называющая никакой разницы. Теперь она читается как «1.0 (129) → 1.0 (130)», и только тогда, когда изменилась именно сборка; там, где имена версий уже отличаются друг от друга, строка не меняется. То же исправление коснулось подсказок у Перезапуска, уведомления, заметки, объясняющей, почему установка была отложена, и отказа `duo install`.

## 0.3.69

**微信输入法 и 豆包输入法 снова можно обновлять одним кликом — и обновляются они именно так, как обновляют себя сами.** Обновление методов ввода одним кликом было отозвано в 0.3.25 в тот же день, когда вышло, после того как у кого-то пропали настройки метода ввода. Проблема была в форме установки: она заменяла всё приложение целиком, как это делает установщик при первой установке. Метод ввода регистрируется в macOS по *расположению* своего приложения, а оба этих приложения обновляют себя, не трогая это расположение, — они сохраняют само приложение и меняют лишь то, что внутри него. Теперь DuoUpdater делает то же самое, поэтому после обновления зарегистрированное приложение остаётся тем же самым приложением, а сбой на любом этапе оставляет ту копию, которую вы запускали, ровно там, где она была. И ни то, ни другое больше не просит ваш пароль.

**Перед тем как обновить любое из них, сначала копируется всё, чему они успели научиться.** Ваш словарь, ваши настройки и состояние учётной записи живут не внутри приложения, поэтому копия для отката, которую DuoUpdater уже хранил, не могла отвечать за них. Теперь перед каждым обновлением метода ввода с них снимается снимок, который возвращается вместе с откатом, а сама копия обходится почти бесплатно — 578 МБ одного из них занимает десятую долю секунды и почти не расходует место на диске. Это скорее страховочная сеть, чем исправление: то, что приложение решит сделать с вашими данными при следующем запуске, — это код самого приложения, и этому ничто не может помешать. Но теперь есть, к чему вернуться.

**Обновление больше не отнимает у приложения тайком возможность обновлять само себя.** Когда DuoUpdater требовались права администратора, чтобы заменить приложение, он восстанавливал, кому принадлежит приложение, но не разрешения, с которыми оно было установлено, — а загруженная копия обычно распаковывается с более узкими разрешениями, чем те, что задаёт установщик. Для обоих методов ввода это разница между тем, способен ли их собственный апдейтер завершить следующее обновление, и тем, что он не может убрать за собой. Теперь разрешения, с которыми приложение было установлено, переносятся на копию, которая приходит ему на замену.

**DuoUpdater перестаёт просить вас перезапускать одно и то же приложение каждые пять минут.** Приложение, которое обновляет само себя, кладёт новую версию на диск и ждёт, пока вы его закроете. DuoUpdater замечал это при каждой проверке и каждый раз выпускал напоминание заново, поэтому сборка, которую вы решили пока не перезапускать, продолжала докучать вам столько, сколько вы её оставляли. Теперь каждая сборка объявляется один раз: перезапустите её, пропустите или проигнорируйте приложение — и оно останется тихим, а следующая сборка объявит о себе как обычно.

**Приложение, ожидающее перезапуска, теперь считается обновлением, а игнорируемое — вообще не считается.** Новая версия, уже лежащая на диске, *и есть* обновление — она просто загрузилась заранее, — поэтому теперь она учитывается в значке и в строке «доступно N обновлений», а не в отдельном счётчике рядом с ними. С игнорируемыми приложениями было наоборот: приложение, которое вы проигнорировали, могло зажечь значок, хотя его строка показывала приглушённый ярлык «Игнорируемые» и не давала никакой кнопки нажать, — так что число указывало на то, на что вы не могли повлиять. Теперь «игнорировать» и «пропустить» означают одно и то же на всех трёх поверхностях — в напоминании, в значке и в списке.

**搜狗输入法 теперь отслеживается — DuoUpdater спрашивает его собственный апдейтер, а не читает сайт.** Страница со списком изменений Sogou публикует версии из трёх сегментов, тогда как установленная копия несёт четыре, поэтому сравнение с ней потребовало бы сперва обрезать настоящую версию. Собственная проверка обновлений отвечает в родной нумерации бандла. DuoUpdater обращается к этой конечной точке так же, как это делает само приложение, и читает из неё релиз целиком, все четыре сегмента, поэтому переиздание, меняющее только последний из них, становится видимым. Это только обнаружение: апдейтер Sogou делает намного больше, чем просто меняет приложение, — он заново регистрирует генератор QuickLook, переносит ваши данные на новое место и принудительно завершает метод ввода, — поэтому само обновление по-прежнему остаётся за ним. Ещё одна вещь, которую стоит знать, и это особенность самого Sogou, а не наша: их конечная точка отвечает в зависимости от версии macOS, которая спрашивает, и Mac на macOS 28, спрашивающий сам за себя, получает в ответ сборку из 2023 года. DuoUpdater не спрашивает от своего имени, поэтому на каждом Mac вам показывается актуальная версия.

**Заметки о выпуске, которые более позднее исправление прочитало бы правильно, теперь перечитываются, а не остаются неверными навсегда.** Заметки кэшируются по версии, исходя из того, что заметки уже вышедшей версии не меняются. Это верно для самих заметок, но не для того, что DuoUpdater умеет из них извлечь, поэтому приложение, чьи заметки вышли искажёнными, оставалось таким для этой версии, сколько бы исправлений парсинга ни выходило потом. Теперь каждая запись в кэше хранит, каким поколением логики извлечения она была написана, а запись, написанную более старым поколением, загружают заново. Так поступают оба кэша — и для журналов изменений приложений, и для заметок формул Homebrew.

**Проверка, провалившаяся на чьём-то CDN, теперь переспрашивается ещё раз, прежде чем считаться сломанной.** 502, 503 и 504 означают, что промежуточный узел не смог достучаться до сервера за собой: запрос попросту не дошёл, поэтому тот же самый запрос мгновением позже обычно срабатывает. Проверка Headlamp умерла ровно на этом — GitHub ответил 504 вообще без заголовка ограничения частоты запросов. Теперь для этих трёх кодов делается один повтор — спустя восемь десятых секунды. Намеренно только для этих трёх: 500 означает, что отказал сам сервер, и повтор в большинстве случаев просто воспроизведёт тот же отказ, а повтор при ограничении частоты тратит как раз тот бюджет, на нехватку которого оно и жалуется.

**Заметки о выпуске формулы Homebrew теперь следуют за той версией, которой принадлежат.** Заметки запоминались по формуле, а не по версии, поэтому стоило заметкам однажды загрузиться, каждый следующий взгляд на формулу до конца сессии показывал заметки той самой первой версии — даже после появления более новой. Их чтение к тому же занимало ту же очередь, которую ждал весь остальной список формул, так что медленный `brew info` мог застопорить список вокруг себя.

**Щёлкните правой кнопкой по приложению в боковой панели Workbench, чтобы его открыть.** То же самое «Открыть», которое всегда предлагали строки в строке меню, — теперь и в окне, где вам это, вероятнее всего, и нужно.

**Отказ от загруженного установщика забирает с собой и оставленную им ошибку.** Выбрасывание подготовленного пакета уже возвращало строку в состояние «Обновить», но красная ошибка от только что отменённой попытки оставалась висеть на ней — и ничто никогда не убрало бы её, ведь ошибка снимается только тогда, когда строка становится актуальной, а строка, всё ещё предлагающая обновление, этого никогда не сделает. Теперь она уходит вместе с загрузкой.

**`duo`, необязательный компаньон для командной строки, перестаёт неточно описывать сам себя.** `--timeout` исчез — он принимался, документировался и никем не читался. `--budget` впервые задокументирован, и единственное сообщение, которое его упоминает, теперь называет настоящее число минут, а не всегда «15». `--max-calls` заявлял значение по умолчанию 20, хотя на деле это 6. Числовому флагу, которому передали не число, теперь сообщается об ошибке: `duo verify --max-concurrency 1x` раньше просто игнорировался и заменялся значением по умолчанию — четыре, что прямо противоположно тому, чего хочет человек, намеренно замедляющий проверку. И `duo verify` теперь считает запросы, которые реально были сделаны: канал, ответивший 502 и успешно сработавший при повторе, больше не отчитывается чистым `ok`, втихомолку спросив на самом деле дважды. `-h` работает везде, где работает `--help`.

## 0.3.68

**Ещё четыре приложения теперь отслеживаются, ещё два канала выпуска, и каждое из них обновляется на месте.** CapCut, Canva и WorkBuddy — на обоих сайтах, с которых они распространяются: международный и китайский ведут независимые линии релизов, поэтому установке предлагаются только версия и заметки, принадлежащие именно её собственному сайту. Termius и VSCodium получают свои каналы Beta и Insiders в дополнение к уже охваченным стабильным сборкам. Второй трек CapCut — самый неудобный: он выбирается переключателем внутри самого CapCut и никак не отражается в номере версии, поэтому то, на каком треке вы находитесь, читается из копии на диске, а не угадывается по той сборке, которую вы случайно запустили. Стоит знать, если вы включаете этот переключатель: DuoUpdater предложит вам самую новую бету, как только она появится, а это может опережать ту точку, до которой вас довёл бы собственный поэтапный выкат обновлений CapCut.

**Каналы предварительных версий Android Studio больше не предлагают вам сборку старше той, что у вас уже есть.** Google перечисляет свои релизы в порядке публикации, а не по версии, и DuoUpdater перебирал этот список три раза по отдельности — один раз ради версии, один раз ради даты, один раз ради загрузки, — поэтому кандидат в релизы, опубликованный позже более новой canary-сборки, мог отдать версию из одной записи, а файл — из другой. 26 августа канал Canary ответил версией 2026.1.4 RC 2, хотя 2026.2.1 Canary 2 уже вышла. Теперь оба этих ответа обязаны приходить из одной и той же записи.

**Переизданный релиз больше не устанавливает копию, которую он должен был заменить.** Когда проект пересобирает релиз, не меняя его версию, оба файла остаются под одним и тем же тегом — KeePassXC публикует `KeePassXC-2.7.11-1-arm64.dmg` рядом с исходным `KeePassXC-2.7.11-arm64.dmg`, — и DuoUpdater брал тот, что был перечислен первым. Этот порядок алфавитный, и в этот раз он случайно поставил пересборку первой, а в следующий раз поставил бы её последней. Внешне это никак не было заметно: версия в строке была правильной, ошибался только файл за ней. Теперь верный файл выбирается по тому, что говорит само имя файла.

**Ночные и снапшот-сборки теперь распознаются как те предварительные сборки, которыми они и являются.** Ночная сборка VLC, снапшот KeePassXC и ночная сборка Freelens — все они устанавливаются под именем и идентификатором бандла стабильного приложения, поэтому единственное, что их выдаёт, — это строка версии, а её как раз и не читали. Они засчитывались как стабильные установки, а именно так ночная сборка в итоге получает предложение перезаписать себя обычным релизом. Ночная сборка DB Browser for SQLite по той же причине читается из собственного имени файла приложения, и там проблема была реальной, но скрытой: её замороженная версия сортируется выше текущей стабильной только до тех пор, пока стабильная не догонит её, а после этого каждая установка с ночной сборкой тихо получила бы стабильную сборку.

**Перезапуск приложения теперь сообщает о том, что произошло на самом деле.** Несколько приложений несут внутри собственного бандла второе приложение — например, Surge хранит там свою панель Dashboard, — и когда перезапустить требовалось только этому внутреннему приложению, DuoUpdater сообщал, что перезапущено внешнее приложение, хотя ничего не происходило. Теперь он сообщает о результате именно внутреннего приложения и больше не выдаёт уведомление о перезапуске для перезапуска, которого на самом деле не было.

**Обновление, которое уже загрузилось, теперь сообщает об этом, и его можно вернуть обратно.** Строка с уже полностью загруженным пакетом установщика показывала синюю кнопку «Установить» — и ничего больше: ничто не говорило, что загрузка уже завершена или что кнопка заново откроет установщик, а не начнёт обновление с нуля, и не было способа передумать. Теперь это видно прямо в строке, а не спрятано в подсказке, а пункт **«Удалить загруженный установщик»** в контекстном меню строки выбрасывает загрузку и возвращает строку в состояние «Обновить». Отмена загрузки во время уже идущей установки того же приложения раньше могла заодно выбросить пакет, который только что закончил загружаться, — теперь так уже не получится.

**Кнопки заметок о выпуске, которые вели на упразднённые страницы, снова открывают действующие.** Microsoft переименовала корпоративные страницы заметок о выпуске Edge, а Termius перенёс свой список изменений, поэтому три канала Edge и Termius отправляли вас на страницу, которой больше не существовало. Раньше никто и никогда не проверял, остаются ли эти страницы на месте, поэтому они могли сколь угодно долго протухать, пока всё остальное выглядело здоровым, — теперь их проверяют по расписанию. У канала Edge Dev кнопки вообще не будет: Microsoft перестала публиковать для него заметки, а все страницы, которые всё ещё существуют, принадлежат другой линии релизов.

**Raycast теперь отслеживается вплоть до версии 2 — но только на тех Mac, которые способны её запустить.** Raycast 2 требует macOS Tahoe и Apple silicon. Mac, который этому не соответствует, остаётся на линии версии 1 и больше не узнаёт о релизе, который он всё равно никогда не смог бы установить. Теперь у обеих линий свои собственные заметки о выпуске, поэтому установка версии 1 читает историю именно версии 1, а не версии 2.

**Заметки о выпуске теперь отображаются в собственном окне DuoUpdater для большего числа приложений.** BetterDisplay — на всех трёх его треках, Shotbase и WorkBuddy — на обоих сайтах. Заметки BetterDisplay больше не заканчиваются сырой разметкой его кнопки загрузки и больше не повторяют список участников проекта в каждом отдельном релизе.

**`duo`, необязательный компаньон для командной строки, теперь отказывается принимать командную строку, которую не понимает.** Опечатанный флаг раньше просто игнорировался, а проигнорированный флаг выглядит так, будто вы его и не передавали, — поэтому `duo verify --githubb` тихо проверял вообще все рецепты вместо того одного, который вы просили. Неизвестные флаги, флаг, оставшийся без значения, и лишние аргументы теперь являются ошибками, которые называют, что команда на самом деле принимает, а `--help` работает после любой команды.

## 0.3.67

**Предварительные и внутренние сборки BetterDisplay теперь предлагаются тем, кто их и просил.** BetterDisplay ведёт все три свои линии релизов в одном-единственном фиде обновлений и выбирает между ними двумя переключателями в собственных настройках. DuoUpdater мог только угадывать вашу линию по той сборке, которую вы случайно запускали, а это ничего не говорит о переключателе, который вы включили, но по которому ещё не успели ничего получить, — поэтому тот, у кого включены оба переключателя, но кто сидит на стабильной сборке, получал сообщение, что он в курсе всех новостей, хотя собственный апдейтер BetterDisplay в это время предлагал версию, опережающую его на четыре релиза. Теперь ваш настоящий выбор считывается прямо из BetterDisplay, включая случай, когда включение внутренних сборок не отменяет и обычные предварительные версии. Его предварительные сборки только для Apple silicon намеренно оставлены в стороне, поэтому Mac на Intel никогда не предложат ту, которую он не сможет запустить.

**Переключение канала выпуска приложения дважды подряд больше не оставляет строку висеть на старом ответе.** Переключение канала отправляет DuoUpdater перепроверять это приложение, и второе переключение, случившееся, пока ещё выполнялась первая проверка, раньше просто отбрасывалось — строка продолжала предлагать бета-версию тому, кто только что отключил беты, пока что-нибудь постороннее не запускало ещё одну проверку. Теперь более новое переключение перехватывает управление у более старого, а проверка, прерванная на середине, помечает не охваченные ею приложения для следующего прохода вместо того, чтобы записать их как уже обработанные. Строки также теперь переходят в состояние проверки быстрее после переключения, так что остаётся меньше времени, в течение которого ответ на экране уже опровергнут вашим собственным переключателем.

## 0.3.66

**Приложение, живущее внутри другого приложения, теперь закрывается и открывается заново вместе с ним.** Несколько приложений несут внутри собственного бандла второе, полноценное приложение — например, Surge хранит там свою панель Dashboard. macOS считает его отдельным приложением, поэтому ничто не закрывало его, когда обновление применялось к приложению вокруг него: оно продолжало работать со сборкой, которую только что заменили, из копии, которая уже была не там, где оно думало, и из-за этого не могло связаться с приложением, которому принадлежит. Теперь такие приложения закрываются вместе с приложением, в котором живут, и открываются заново после него, а то окно, с которым вы на самом деле работали, оказывается тем, что возвращается на передний план. Так обрабатываются только те приложения, которые вы могли бы открыть сами; невидимые вспомогательные процессы, которые приложение запускает для себя, никто не трогает.

**Точки отката, которые незаметно перестали создаваться, снова создаются.** Приложение может заблокировать один из собственных файлов так, что его ничем не удалить, — а копия, хранившаяся для отката, унаследовала эту блокировку, поэтому её никогда не мог заменить следующий откат, и единственная прерванная попытка могла оставить после себя копию, которую ничто не могло убрать. После этого каждое обновление такого приложения проходило без пути назад — и сообщало об этом одной строкой, вообще не называющей причину. Одно приложение на собственном Mac разработчика находилось в этом состоянии два дня. Копии, хранимые для отката, больше не наследуют блокировку, установленное приложение остаётся ровно таким, каким его настроил разработчик, а застрявшая копия больше не может блокировать ту, что должна была её заменить.

**Обновление, которое прошло успешно, больше не сообщает о сбое.** macOS способна поставить новую версию на место, а затем дать сбой при удалении той, которую она вытеснила. Это принималось за чистую монету: DuoUpdater мог сказать, что приложение не обновилось, хотя оно обновилось, или что точки отката нет, хотя в хранилище резервных копий лежала полноценная, — и в первом случае вас отправляли выдавать разрешение, которое вы уже выдали и которое всё равно ничем не могло помочь. Теперь перед тем, как что-либо сообщить, проверяется, что на самом деле произошло на диске.

**Строки версий больше не повторяют цифры, которые версия уже содержит.** Когда приложение ждёт перезапуска, строка показывает версию, на которой вы сейчас работаете, и ту, на которую вас перенесёт перезапуск. Обе стороны несли номер сборки — именно он делает разницу читаемой, когда разработчик выпускает несколько сборок под одним именем версии, — и превращается в чистый шум, когда версии и так уже различаются. Строка Chrome тратила всю свою ширину на печать «151.0.7922.174 (7922.17…» и обрывалась ещё до цифр, которые на самом деле изменились. Теперь номера сборок появляются только тогда, когда изменилось именно это.

## 0.3.65

**Теперь для повторного открытия приложения, чтобы завершить его обновление, используется одно и то же слово.** Приложение, чей собственный апдейтер уже поставил новую версию на место, просило вас его «Restart»; то, что откладывает новую версию в сторону, пока вы его не закроете, просило «Relaunch». Разница была настоящей за кулисами и не значила для вас ровным счётом ничего: один и тот же клик в обоих случаях, и один и тот же результат, если вы вообще не нажимаете, — ведь обновление применяется при следующем закрытии приложения в любом случае. Теперь всюду используется слово «Перезапустить» — включая уведомления, подсказки и настройку, которая делает это за вас, — и это то же самое слово, которое перед вами ставят Chrome, Claude и большинство приложений, обновляющих себя сами. Только в английском для этого всегда было два разных слова; в немецком, японском, русском и китайском всегда использовалось одно, а испанский и французский переходят на ту формулировку, которую они уже использовали в других местах.

## 0.3.64

**Обновление, которому требуется администратор, теперь так и просит, вместо того чтобы провалиться и обвинить разрешение.** Нужен ли для замены пароль, решалось по папке, в которой лежит приложение, а не по самому приложению. Всё, что macOS установила от имени root — каждое приложение из App Store и любое приложение, положенное туда пакетом установщика, — проходило эту проверку, шло по пути, не требующему пароля, и просто не могло довести дело до конца: удаление старой версии требует права записи в её собственные каталоги. macOS сообщает об этом отказе тем же кодом, что и об отказе в разрешении на управление приложениями, поэтому сбой приходил в виде запроса выдать разрешение «Управление приложениями», которое всё равно ничем не могло помочь, ведь препятствием было владение файлами. На обычном Mac это касалось каждого приложения из App Store и ещё нескольких вдобавок, и страдали от этого именно администраторские учётные записи — со стандартными учётными записями всё и так уже шло правильным путём. Теперь такие обновления идут по пути, который действительно работает, а приложение сохраняет того владельца, который у него был, а не тихо становится вашим.

**Приложения из App Store теперь тоже получают точку отката, как и всё остальное.** Резервное копирование их пропускало, исходя из того, что магазин всегда может заново выдать предыдущую сборку. Не может — App Store предлагает только текущую версию приложения, — из-за чего магазин оставался единственным путём применения обновления без возможности его отменить. Теперь такие приложения перед обновлением тоже резервируются, как и любые другие, а поскольку копия делается клонированием, это почти не расходует место на диске, пока обновление на самом деле не заменит оригинал. Восстановление такой копии говорит именно то, что специфично для магазина: обновление тут же снова появляется в списке обновлений и само переустановится, если включены автоматические обновления приложений. А когда обновление из магазина всё равно не собиралось применяться — приложение для iPhone, запущенное на Mac, или наименование, не продающееся в вашем регионе, — точка отката больше не создаётся вовсе, поэтому строка больше не может предлагать откатиться к той версии, на которой приложение и так уже работает.

**Longbridge Desktop теперь отслеживается, на обеих линиях выпуска.** Stable и Preview получают каждая своё определение версии, установку официальной сборки для Apple silicon одним кликом и заметки о выпуске, отображаемые в собственном окне DuoUpdater, вместе с иллюстрациями.

**Заметки о выпуске WhatsApp теперь появляются, пока его проверка через App Store ещё выполняется.** Страница App Store, показываемая в этом окне, была заведена под именем, которое поиск никогда не мог сопоставить, поэтому панель заметок оставалась пустой вместо того, чтобы её показать.

**Заголовок меню теперь вмещает больше в меньшем пространстве.** Собственная версия DuoUpdater теперь стоит рядом с его именем, и щелчок по ней открывает его заметки о выпуске. «Обновить всё» переехало на отдельную строку, где переведённой подписи хватает места, чтобы полностью прочитаться, а действия внизу теперь представлены значками. Баннер, раньше объявлявший о самообновлении DuoUpdater, исчез — вместо него загорается искорка рядом с версией и остаётся гореть, пока вы не прочли, что изменилось. Строка статуса, слишком длинная для меню, теперь заканчивается многоточием, а не растягивает его.

**Настройки больше не обрезают предлагаемые варианты обновления.** Два меню, решающие, как применяется обновление, обрезали собственные подписи на нескольких языках. Теперь формулировки короче, а там, где словам всё же нужно больше места, они переходят на вторую строку.

**«Что нового» теперь говорит, когда вышла каждая версия.** Теперь у каждой версии в списке есть дата публикации.

## 0.3.63

**Обновления под конкретную архитектуру теперь выбирают сборку, которую этот Mac реально способен запустить.** Некоторые приложения публикуют одну и ту же версию дважды в одном Sparkle-фиде — отдельно для Apple silicon и отдельно для Intel, — и раньше DuoUpdater разрешал этот выбор просто по тому, какой адрес загрузки оказывался первым по алфавиту. Теперь он читает требование к оборудованию, указанное в фиде, а там, где поставщик оставил его пустым, — архитектуру из имени файла. Родная сборка теперь побеждает стабильно, а сборка, которую этот Mac не способен запустить, больше не предлагается. GitHub-релизы получают то же самое обращение — без того, чтобы объявлять совершенно здоровый рецепт сломанным только потому, что его самый новый артефакт нацелен на другую архитектуру.

**Проверки обновлений ChatGPT теперь следуют треку выката, привязанному к учётной записи.** OpenAI иногда держит бизнес- и корпоративные учётные записи на более старой десктопной сборке, пока новая уже дошла до потребительских учётных записей. DuoUpdater раньше не передавал в такой проверке тариф учётной записи, из-за чего для всех тихо выбирался осторожный трек: он мог заявлять, что установленная копия чем-то опережает всех, хотя сам ChatGPT уже загружал более новую сборку, или предлагать сборку, которую собственный апдейтер приложения тут же заменил бы снова. Теперь он отправляет метку тарифного плана из состояния входа ChatGPT/Codex вместе с тем же запросом обновления, который делает само приложение. Если эта метка недоступна, сохраняется прежнее осторожное поведение; сами учётные данные никогда не попадают ни в запрос, ни в диагностику.

**Перезапуск, на который macOS так и не ответила, больше не способен заклинить все последующие обновления.** Launch Services иногда принимает запрос на повторное открытие обновлённого приложения — и затем так никогда и не перезванивает. Строка навсегда оставалась в состоянии «Перезапуск…», её кнопка «Перезапустить» оставалась недоступной, а собственное обновление DuoUpdater ждало за ней в очереди. Теперь запуск, который не ответил в течение минуты, признаётся неудавшимся, поэтому строка восстанавливается, а остальная часть апдейтера продолжает работать.

**Журнал релизов остаётся заполненным, даже если быстро дёрнуть его полосу прокрутки.** Старый «ленивый» стек можно было обогнать длинным прыжком, ненадолго оставив пустое окно, пока строки создавались вокруг новой позиции. Теперь журнал использует переиспользуемый список, способный прыгать сразу к месту назначения. Элемент управления обновлением тоже сохраняет один и тот же размер при переключении между стрелкой и спиннером, поэтому нижняя строка больше не дёргается, когда начинается проверка.

**Трафик загрузок теперь помечает обновления, использовавшие бинарный патч.** Новые загрузки теперь записывают маршрут, которым обновление реально завершилось, — а не просто был ли патч предложен, — и несут в истории значок Delta. Заметно более лёгкие загрузки патчей из 0.3.62 тоже распознаются, хотя они были записаны ещё до того, как в журнале трафика появилось поле маршрута.

## 0.3.62

**Обновления теперь загружают только то, что изменилось, — там, где разработчик публикует их именно так.** Некоторые приложения выпускают вместе с каждым релизом небольшой патч — этого достаточно, чтобы превратить имеющуюся у вас версию в новую, не выкачивая всё заново. DuoUpdater раньше их игнорировал и каждый раз загружал полный пакет. Теперь он берёт патч, если он подходит именно к той сборке, на которой вы находитесь. Последнее обновление ChatGPT обошлось в 1,9 МБ вместо 605 МБ; Docker — в 87 МБ вместо 582 МБ. В обоих случаях результат — идентичное приложение: та же подпись, те же байты, сверенные с полной загрузкой ещё до того, как это было выпущено. Если ни один патч не подходит к тому, что у вас есть, или патч не применяется, происходит полная загрузка, как и раньше, поэтому из-за этого ничто не может не установиться.

**DuoUpdater больше не загружает обновление, которое приложение уже загружает само.** Многие приложения тоже обновляют себя сами, и когда мы оба одновременно тянулись за одним и тем же файлом в 600 МБ, вы платили за этот файл дважды. Теперь DuoUpdater замечает уже идущую загрузку и оставляет её в покое, сообщая об этом прямо в строке, а не молча бездействуя. Если такая загрузка окажется заброшенной, он перестаёт её учитывать через десять минут, чтобы ничто не оставалось заблокированным навсегда.

**Обновление, которое приложение уже подготовило само, больше не перезаписывается.** Приложения, обновляющие себя сами, часто загружаются в фоне, а затем ждут, пока вы их закроете, чтобы подставить новую версию. Установка поверх такого обновления выглядела успешной, а затем откатывалась в тот самый момент, когда вы закрывали приложение, — а там, где собственная ожидающая версия приложения оказывалась старше той, что только что установил DuoUpdater, вы оказывались даже дальше позади, чем в начале. Теперь такие обновления оставляют доводить дело до конца, какую бы версию они ни несли.

**Собственные обновления DuoUpdater тоже стали меньше.** Его релизы теперь поставляются в виде такого же патча, поэтому обновление с недавней версии подтягивает несколько сотен килобайт вместо одиннадцати мегабайт.

## 0.3.61

**Объяснения, оставленные обновлением, теперь исчезают сами.** Когда DuoUpdater передаёт приложение его собственному апдейтеру, строка сообщает об этом — «вывел его на передний план, чтобы его собственный апдейтер применил обновление». Раньше эта фраза оставалась навсегда: единственное, что вообще её убирало, — это запуск ещё одного обновления того же приложения, поэтому она всё ещё лежала под строкой спустя часы после того, как обновление уже состоялось, описывая нечто, давно завершившееся. Теперь она уходит, как только приложение становится актуальным. Предупреждение о том, что обновление было применено без точки отката, намеренно оставлено как есть, потому что оно описывает уже случившееся обновление и начинает иметь значение лишь после того, как всё закончилось.

**Отклонённое обновление теперь объясняет причину на вашем языке.** Когда уже устанавливается что-то ещё — DuoUpdater обрабатывает пакет или `duo` работает в терминале, — строка сообщает вам об этом. Раньше это сообщение было только на английском в остальном полностью переведённом окне, и заканчивалось номером процесса: полезным в терминале и совершенно бесполезным для действия в меню. Теперь оно переведено и говорит, что делать, а не кто держит блокировку. Инструмент командной строки по-прежнему выводит номер процесса — там, где с ним можно что-то сделать.

**Строка с закономерностью выхода релизов, и вместе с ней часы, теперь следуют вашей локали.** Раздел «Журнал релизов → Закономерности» подводил итог фразой вроде «Most often ships Friday, around 6 PM» («Чаще всего выходит по пятницам, около 18:00»). Языки, склоняющие названия дней, не могут сказать это, вставив день посередине предложения, и получали неверную форму слова — по-русски получалось «Чаще всего выходит пятница» там, где нужно «по пятницам». Теперь это надпись-ярлык: «Пик: пятница, около 18:00», которая верна везде. Время идёт вместе с этим — если ваш Mac пишет время на 24-часовом циферблате, так же поступает и это, в том числе на оси графика.

**Поиск по настройкам теперь понимает слова, которые вы видите на экране.** Поле поиска над боковой панелью настроек сверялось со списком дополнительных терминов, написанным по-английски и никогда не переводившимся, поэтому «rollback» находил раздел «Общие», а «Zurücksetzen» и «回滚» не находили ничего. Теперь эти термины существуют на каждом языке, на котором говорит DuoUpdater. Английские варианты по-прежнему работают на любом языке, ведь документация, из которой они взяты, написана по-английски.

## 0.3.60

**Включение «Показать все» больше не заставляет меню задумываться.** Полный список всех ваших приложений целиком измерялся заново при каждом появлении — на Mac со 127 приложениями это выливалось примерно в секунду работы, и почти всё это время уходило на раскладку строк, находящихся далеко за пределами видимой области. Плата взималась при каждом переключении, а не только при первом. Теперь строятся только те строки, что действительно есть на экране.

**Объяснение, оставшееся от неудавшегося обновления, больше не переживает саму неудачу.** Когда обновление не удавалось применить — например, потому что уже устанавливалось что-то другое, — строка красным объясняла почему. Ничто никогда это не убирало: даже когда обновление всё же проходило и строка возвращалась к галочке, старое объяснение по-прежнему висело под ней и оставалось при каждой следующей проверке вплоть до перезапуска DuoUpdater. Теперь оно уходит, как только приложение становится актуальным. Причины, относящиеся к обновлениям, которые всё ещё ожидают, остаются нетронутыми, поэтому ту, что вы ещё не прочли, не может стереть проверка, выполняющаяся в фоне.

**Кнопка «Обновить всё» больше не меняет размер в зависимости от длины списка.** Когда обновлений ожидало совсем немного, кнопку рисовали на размер меньше, чем следовало, с восемьюдесятью пунктами пустого места рядом с ней, и она подскакивала до правильного размера, стоило списку вырасти. Теперь она всегда остаётся тем размером, каким и должна быть.

## 0.3.59

**Трафик загрузок теперь говорит, на какую сборку перешло обновление, а не только на какую версию.** Немало приложений выпускают несколько сборок под одним именем версии — Surge выпустил под номером «6.9.0» четыре отдельных релиза, — поэтому такие строки читались как «6.9.0 → 6.9.0» и ничего вам не сообщали. Теперь они читаются как «6.9.0 (12028) → 6.9.0 (12030)», и только тогда, когда одного имени версии недостаточно; там, где версия и так уже изменилась, номер сборки был бы лишь шумом и опускается.

**Записанной считается та сборка, что реально прижилась, — её считывают с самого приложения уже после установки обновления.** А не то число, которое рекламировал собственный фид обновлений разработчика, — фиды и правда иногда врут, и заодно это работает для тех мест, что вообще не публикуют номер сборки: GitHub, Homebrew и App Store. Обновление, всё ещё ожидающее окно установщика macOS, никогда не угадывается заранее — для него ничего не записывается, пока оно не станет реальностью.

**Загрузка, которая ничего не изменила, теперь так и помечается.** Иногда обновление загружает и устанавливает ту же сборку, что уже стояла на вашем Mac, — несовпадение номера версии на стороне разработчика, зеркало, отдающее то, что у вас уже есть. Это реальный расход трафика без всякого результата, и именно окно трафика — то место, где вы хотели бы это увидеть. Теперь такие строки несут ярлык «без изменений». У загрузок, записанных до этого релиза, нет номеров сборок для сравнения, поэтому их оставляют как есть, а не гадают: «неизвестно» — не то же самое, что «без изменений».

## 0.3.58

**DuoUpdater теперь показывает, во сколько трафика реально обошлось поддержание ваших приложений в актуальном состоянии.** Он и раньше считал, до байта, каждое обновление, загруженное для вас, — но этому числу было негде появиться, и оно просто лежало в файле, который никто не мог прочитать. Теперь есть окно «Трафик загрузок», открываемое кнопкой-графиком внизу меню, а цифра за этот месяц напечатана прямо рядом с этой кнопкой, поэтому на самый частый вопрос отвечает уже сам факт, что вы ничего не открывали. Внутри: итог, последние три месяца бок о бок с изменением между ними, разбивка по источникам этих байтов и все приложения, отсортированные по тому, во сколько они обошлись, — щёлкните по любому, чтобы увидеть каждое его обновление: с какой версии на какую оно перешло и насколько большой была эта загрузка.

**Итог честно говорит о том, чего он не видит.** Homebrew, App Store и приложения, обновляющиеся через собственный встроенный апдейтер, тянут свои байты сами, и DuoUpdater их никогда не обрабатывает, — поэтому эта цифра является нижней границей, а не полным учётом. Так было всегда; изменилось то, что теперь окно говорит об этом постоянно, а не только на пустом экране, который вы видите до того, как хоть что-то было записано.

**Приложения, которые вы с тех пор переименовали или удалили, сохраняют свою историю.** Трафик записывается за тем местом на диске, где живёт приложение, а именно это и позволяет двум каналам одного и того же приложения — скажем, Android Studio Canary и Beta — оставаться раздельными, а не складываться в одно. Плата за это — переименование приложения оставляет его прошлое под именем, которого больше не существует. Когда OpenAI переименовала Codex в ChatGPT, это разделило 30 ГБ загрузок между двумя записями, которые выглядели как дубликаты. Теперь такие записи группируются и приглушаются внизу списка, под заголовком, который поясняет, что это такое. Ничего не выбрасывается, и итог по-прежнему включает их.

## 0.3.57

**Приложению, установленному из App Store, больше не предлагается собственная загрузка с сайта разработчика.** Немало приложений публикуются сразу в двух местах — в магазине и в виде прямой загрузки с сайта самого разработчика — под одной и той же идентичностью, но как по-настоящему разные сборки. Прямая загрузка обычно опережает магазин, потому что не ждёт проверки магазином. DuoUpdater сперва проверяет магазин для приложения, установленного из магазина, но когда эта проверка по любой причине не удавалась — оборвалось соединение, витрина не ответила, — он тихо переходил на сайт разработчика и предлагал всё, что там находил. WhatsApp показал это как «26.32.75 → 26.33.19»: настоящая версия, но не из того источника. Принять её означало бы заменить вашу магазинную копию такой, которую App Store уже никогда не смог бы обновить снова. Теперь приложения, установленные из магазина, проверяются только по магазину и нигде больше.

**Переключение канала обновлений приложения прямо внутри него теперь регистрируется сразу.** Некоторые приложения позволяют выбирать между обычными релизами и бета-версиями — Surge, Tailscale, Fork, OrbStack, IINA, Alfred и другие, — и DuoUpdater следует за тем, что выбрали именно вы, чтобы никогда не предложить сборку, о которой вы не просили. Раньше он замечал перемену только тогда, когда вы закрывали приложение или открывали одно из окон DuoUpdater. Ни то, ни другое не покрывает то, что люди на самом деле делают: выключить настройку, оставить приложение запущенным и просто взглянуть на строку меню. Хуже того, приложения записывают эту настройку на диск тогда, когда им удобно, а не в момент вашего клика — у Surge на это уходило пять минут, — так что даже закрытие приложения могло быть прочитано слишком рано. Теперь DuoUpdater сам следит за изменением этой настройки и перепроверяет именно это приложение в течение секунды-двух. Переключение Surge обратно на обычные релизы теперь сразу же убирает бета-версию из его строки, вместо того чтобы она провисела там ещё до часа.

**Проверка, которая ни до чего не смогла достучаться, больше не выглядит как чистый лист здоровья.** Когда отказывали все источники сразу — не было сети, или прокси тихо отклонял соединения, пока значок Wi-Fi по-прежнему уверял, что всё в порядке, — приложения, проверка которых провалилась, скрывались, а панель показывала «127 приложений · актуально». Это тот же самый экран, который вы видите, когда всё и вправду обновлено, из-за чего провалившуюся проверку было не отличить от успешной. Теперь панель говорит, до скольких приложений ей не удалось достучаться, показывает, что пошло не так, и предлагает попробовать снова именно их — только их, оставив в покое всё, что она всё-таки успела проверить.
