# Neuigkeiten

Dies ist eine deutsche Übersetzung von `CHANGELOG.md`. Die englische Fassung ist maßgeblich; frühere Versionen, die hier nicht aufgeführt sind, fallen automatisch auf Englisch zurück.

## 0.4.3

**Xcode-Betas und Release Candidates lassen sich jetzt mit einem Klick aktualisieren.** Melde dich einmal unter Einstellungen → Xcode bei deinem Apple-Developer-Konto an, und DuoUpdater lädt neue Betas und RCs herunter und installiert sie wie jedes andere Update. Bisher konnte es dir nur sagen, dass eine neue erschienen ist.

**Installiere jede Xcode-Version neben denen, die du schon hast.** Einstellungen → Xcode listet jedes Xcode-Release auf, nach Version gruppiert, und die Gruppe für dein macOS ist schon geöffnet. „Installieren“ legt die gewählte Version als eigene Kopie in „Programme“ ab – zum Beispiel Xcode-26.6.app – und ersetzt nichts; oder du sicherst nur das Archiv (.xip). Wenn du angemeldet bist, ist Apples eigene Liste dabei, sodass neue Releases innerhalb von Minuten erscheinen.

**Deine Apple-Developer-Anmeldung hält sich selbst am Leben.** Apple beendet eine Developer-Sitzung nach etwa acht Stunden; DuoUpdater holt jetzt im Hintergrund eine neue – ohne Fenster, ohne Passwort –, solange Apple diesen Mac noch erkennt. Unter Einstellungen → Xcode kannst du das ausschalten.

**Neue Xcode-Releases erscheinen früher.** An Werktagen während Apples üblicher Veröffentlichungszeiten sieht DuoUpdater alle fünf Minuten nach einem neuen Xcode.

**Einige Apps funktionieren wieder, nachdem ihre Entwickler etwas geändert haben.** CapCut Beta und Superconductor Nightly werden wieder gefunden und installiert, und Superconductor behält seine bisherige App-Identität. VLC 3.0.24 und JetBrains Air zeigen wieder ihre Versionshinweise.

## 0.4.2

**Qoder CN IDE wird jetzt unterstützt.** Die Festlandchina-Ausgabe von Qoders IDE bekommt Update-Prüfungen, die Installation mit einem Klick und ihre Versionshinweise im Fenster. Auch Qoder CN, die Desktop-App, zeigt jetzt ihre Versionshinweise.

**Qoder IDE ändert seine Meinung über die neueste Version nicht mehr.** Qoder verteilt Updates schrittweise, und DuoUpdater fragt jetzt genauso wie deine IDE selbst, sodass es jedes Mal dieselbe Antwort bekommt. Bisher konnte die angebotene Version zwischen zwei Releases hin- und herspringen, und eine Installation mit einem Klick konnte auf halbem Weg abbrechen, weil sich die Antwort geändert hatte.

**Sparkle-Apps, deren heruntergeladenes Update inzwischen veraltet ist, lassen sich wieder aktualisieren.** Hat eine App, die sich über Sparkle selbst aktualisiert, einen Build geladen und ist danach ein neuerer erschienen, entfernt DuoUpdater jetzt den alten Download und installiert den neuesten. Bisher meldete „Aktualisieren“ immer wieder, dass eine Installation jetzt rückgängig gemacht würde, und die Zeile änderte sich nie.

**`duo install` sagt dir, wenn der neueste Build schon bereitliegt.** Hat der eigene Updater einer App die neueste Version bereits geladen, sagt es das jetzt und schlägt vor, die App zu beenden oder `duo restart` auszuführen. Bisher behauptete es fälschlich, die App lasse sich nur prüfen, nicht installieren.

## 0.4.1

**搜狗输入法 lässt sich jetzt mit einem Klick aktualisieren.** DuoUpdater spielt das Update so ein, wie die Eingabemethode sich selbst aktualisiert – das installierte Bundle bleibt dabei an seinem Platz – und sichert vorher dein Wörterbuch und deine Einstellungen, damit eine Wiederherstellung auch die zurückbringt.

**Backups auf einem anderen Volume behalten jetzt Wörterbuch und Einstellungen einer Eingabemethode.** Bisher wurde nur die App selbst auf das Backup-Volume verschoben, sodass eine Wiederherstellung von dort die Daten der neueren Version zurückließ.

**Keine Updates mehr, die sich nie installieren lassen.** JetBrains Air zeigt jetzt den Build, den die Toolbox tatsächlich anbietet. Eine App-Store-App, die nach einer Umbenennung durch den Entwickler unter ihrem alten Namen zurückgeblieben ist, sagt jetzt, welche Kopie der App Store aktualisiert – statt ein Update anzubieten, das nur die andere Kopie neu installiert.

**Die Versionshinweise weiterer Apps erscheinen jetzt formatiert.** Apps, die ihre Hinweise als Markdown veröffentlichen, bekommen jetzt dieselbe native Liste wie alle anderen. Bisher erschienen die Hinweise als reiner Text.

## 0.4.0

**„Neu starten“ kommt dem eigenen Installationsprogramm einer App nicht mehr in die Quere.** Hat eine App ihr Update selbst geladen und gehört ihr Backup für die Wiederherstellung auf ein externes Volume, wartet diese Kopie jetzt, bis das Installationsprogramm fertig ist. Bisher lief sie in genau den wenigen Sekunden, die macOS ihm dafür gibt.

## 0.3.99

**Backups zum Wiederherstellen können jetzt auf einem anderen Volume liegen.** Ein Backup ist eine vollständige Kopie einer App, und auf dem Startvolume summiert sich das. Unter Einstellungen → Backups stehen die angeschlossenen Volumes mit ihrem jeweils freien Speicher; bereits vorhandene Backups lassen sich dorthin verschieben, neue werden im Hintergrund dorthin geschrieben – ist das Volume abgezogen, bedeutet das eine Verzögerung und keinen fehlenden Wiederherstellungspunkt. `duo backups` hat `disks`, `sync`, `verify` und `probe` dazubekommen.

**千问输入法 wird jetzt unterstützt.** DuoUpdater meldet, wenn eine neue Version erschienen ist, und aktualisiert die Eingabemethode so, wie sie sich selbst aktualisiert.

**Versionshinweise bleiben nicht mehr bei der vorherigen Version hängen.** Veröffentlicht ein Entwickler ein Update ein paar Minuten, bevor seine Changelog-Seite es erwähnt, wurden die älteren Hinweise unter der neuen Version abgelegt und dort für immer behalten. CleanShot X zeigte seit dem Erscheinen von 5.0.1 die Hinweise zu 5.0.

**Ein gelöschtes Backup gibt jetzt wirklich den Speicher frei, den es meldet.** Backups aus älteren DuoUpdater-Versionen wurden in einer Form geschrieben, die macOS nicht löscht – „Aufräumen“ ließ den größten Teil der Kopie auf dem Volume und zählte sie trotzdem als freigegeben.

**„Aufräumen“ öffnet sich sofort.** Bisher wurde bei jedem Klick jedes gespeicherte Backup neu vermessen.

**WorkBuddy zeigt wieder seine neueste Version.** Der Update-Dienst antwortet mit dem nächsten Schritt einer Upgrade-Kette statt mit dem neuesten Build, weshalb alle vier WorkBuddy-Ausgaben zwei Releases zurücklagen.

**Unter der Haube.** Eine App, die nie angegeben hat, welche Binärdatei sie ausführt – darunter Metas Muse –, gibt ihre Frameworks und ihr SDK jetzt trotzdem preis.

## 0.3.98

**Updates, die über „Neu starten“ abgeschlossen werden, lassen sich jetzt rückgängig machen.** Hat eine App ihr Update schon selbst geladen und du klickst auf „Neu starten“, sichert DuoUpdater jetzt zuerst die aktuelle Version – das Update erscheint dann wie jedes andere unter „Wiederherstellen“. Bisher ließen sich nur Updates rückgängig machen, die DuoUpdater selbst installiert hatte.

**Sieh dir an, was ein Update in einer App verändert hat.** Für jede App mit einer gesicherten Vorversion hat die Werkbank jetzt neben „Versionshinweise“ die Ansicht „Bundle-Vergleich“: Signatur und Berechtigungen, minimale macOS-Version, Hintergrund- und Anmeldeobjekte, mitgelieferte Bibliotheken sowie hinzugefügte, entfernte oder in der Größe geänderte Dateien. `duo diff` macht dasselbe im Terminal für zwei beliebige Kopien einer App.

**„Neu starten“ sagt dir, wenn der eigene Updater einer App das Update nicht angewendet hat.** Schließt sich die App und das Update kommt nie an, steht das jetzt in ihrer Zeile, und „Neu starten“ wird weiter angeboten. Bisher hörte der Fortschrittskreis einfach wortlos auf.

**Cherry Studio zeigt wieder seine neueste Version.** Nachdem Cherry Studio seine Mac-Downloads umbenannt hatte, hielt DuoUpdater ein älteres Release für das neueste: Neuere Kopien galten als „aktuell“, ältere bekamen genau diesen älteren Build angeboten.

**App-Store-Updates hängen nicht mehr an einem Hilfsprogramm, das nicht antwortet.** Antwortete das Hilfsprogramm von DuoUpdater nicht mehr, blieb das Update bei 0 % stehen, und jedes dahinter eingereihte App-Store-Update wartete, bis du DuoUpdater beendet hast.

**„Alle aktualisieren“ startet ignorierte Apps nicht mehr neu.** War „Aktualisierte Apps automatisch neu starten“ eingeschaltet, konnte das Ende eines Durchlaufs eine ignorierte App beenden und wieder öffnen – oder eine, deren geladene Version du übersprungen hattest.

**Tinycast und SuperCmd werden jetzt unterstützt.** DuoUpdater meldet, wenn eine neue Version von Tinycast oder seiner Beta erschienen ist, und erkennt jetzt neben SuperCmd 2 auch das ursprüngliche Open-Source-SuperCmd. Die Versionshinweise von Kimi Code werden jetzt ebenfalls angezeigt.

**Versionshinweise passen zur angebotenen Version.** Die neuesten Versionshinweise von VS Code blieben leer, und kurz nachdem eine neue Version erschienen war, konnte der Bereich stattdessen die Hinweise des vorherigen Release zeigen.

**Das Release-Protokoll trägt dasselbe Release nicht mehr bei jedem Start erneut ein.** Apps wie Claude, Raycast und Cline bekamen bei jedem Start von DuoUpdater einen doppelten Eintrag.

**Die Seitenleiste der Werkbank ist in die Tabs Apps, Brew und Wiederherstellen aufgeteilt.** Klicke auf einen Tab oder ziehe über die Tabs, um zu wechseln; die Suche filtert alle drei gleichzeitig.

**Unter der Haube.** „Neu starten“ und „Wiederherstellen“ für dieselbe App kommen sich nicht mehr in die Quere; ein Update-Feed kann DuoUpdater nicht mehr dazu bringen, Dateien außerhalb seines Download-Ordners zu löschen; Prüfungen im Hintergrund riskieren nicht mehr, dass macOS nach Zugriff auf die Daten anderer Apps fragt; eine sehr große Zahl im Filter „Anfragen“ beendet die App nicht mehr; und die Menüleiste respektiert jetzt eine Administratoranfrage, die du in `duo install` abgelehnt hast.

## 0.3.97

**Updates für iStat Menus werden jetzt richtig erkannt.** Wenn iStat Menus ein Update neu veröffentlichte, konnte DuoUpdater es nach der Installation weiter anbieten, und ein erneuter Klick auf „Aktualisieren“ endete mit einem Fehler.

**Memoh Desktop wird jetzt unterstützt.** DuoUpdater meldet, wenn eine neue Version erschienen ist, und kann sie mit einem Klick aktualisieren.

**„Alle aktualisieren“ fordert dich nicht mehr auf, eine App-Store-App neu zu starten, die schon wieder geöffnet ist.** Wenn der App Store eine App schließt und mit der neuen Version wieder öffnet, zeigt ihre Zeile sie jetzt sofort als aktualisiert an, statt bis zum Ende des ganzen Durchlaufs „Jetzt neu starten“ anzuzeigen.

## 0.3.96

**Eine App, die ihr Update schon selbst geladen hat, lädt es nicht mehr doppelt.** Manche Apps laden ein Update still im Hintergrund und warten dann darauf, dass du sie neu startest. Klickst du in DuoUpdater auf „Aktualisieren“, installiert es jetzt die Kopie, die schon auf deinem Mac liegt: Das Update ist in Sekunden erledigt und kostet keinen Download. Bisher hat DuoUpdater dasselbe Release noch einmal heruntergeladen.

**Cua Driver und Petex werden jetzt unterstützt.** DuoUpdater meldet, wenn eine der beiden Apps ein neues Release hat; Cua Driver lässt sich außerdem mit einem Klick aktualisieren und zeigt seine Versionshinweise.

**Dasselbe Update meldet sich nicht mehr immer wieder.** Wenn der Server eines Entwicklers ständig zwischen zwei Versionen wechselte, sah jede Prüfung wie ein neues Update aus und löste eine weitere Mitteilung aus.

**Ein gerade installiertes Paket bietet nicht mehr an, sich erneut zu installieren.** Seine Zeile sagte bisher weiterhin, das geladene Paket werde erneut geöffnet.

**DuoUpdater benötigt jetzt macOS 15 Sequoia oder neuer.** Ein Mac, der noch mit macOS 14 läuft, behält seine Version und bekommt keine Updates mehr angeboten; jeder Mac mit Apple Silicon kann auf ein unterstütztes macOS aktualisieren.

**Unter der Haube.** Routinemäßige Prüfungen lösen Download-Links nicht mehr vorab auf – das konnte eine funktionierende „Aktualisieren“-Taste kurz verschwinden lassen, wenn der Download-Server eines Entwicklers nicht antwortete.

## 0.3.95

**Aside wird jetzt unterstützt.** DuoUpdater meldet, wenn ein neuer Build des Aside-Browsers erschienen ist, und zeigt dessen Versionshinweise.

**Updates, die dein macOS nicht ausführen kann, werden nicht mehr angeboten – und die Zeile sagt jetzt, warum.** Wenn ein Entwickler angibt, welche macOS-Versionen ein Release unterstützt, hält sich DuoUpdater daran: Nach einem macOS-Upgrade bietet Little Snitch keinen Build mehr an, dessen Obergrenze unter deinem System liegt; Xcode bietet keinen Build mehr an, der ein neueres macOS als deines braucht; und ein Installationspaket, dessen App ein neueres macOS braucht, wird abgelehnt, statt eine App zu installieren, die sich nicht öffnen lässt. Zeilen, die früher nur einen leeren Strich zeigten, sagen jetzt „Noch nicht für dieses macOS“ oder „Neueres macOS nötig“, die Details sind einen Klick entfernt.

**Sieh, mit welchem SDK jede App gebaut wurde.** Klick auf die Markierung neben dem Namen einer App, und die Details sagen jetzt zum Beispiel „Mit dem macOS 27.0 SDK gebaut.“ – praktisch, um zu erkennen, welche Apps schon für das neueste macOS neu gebaut wurden.

**Homebrew-Apps, die als zwei Pakete erscheinen – eines für ältere Systeme, eines für das neueste –, werden jetzt aus dem richtigen gelesen.** OnyX ist für macOS 27 so aufgeteilt, und DuoUpdater sah nur das Paket für ältere Systeme: Entweder zeigte es OnyX gar nicht an, oder es hielt OnyX für aktuell gemessen an einer Version, die Homebrew unter 27 nicht installiert. Jetzt folgt DuoUpdater dem der beiden, das du installiert hast.

**TestFlight-Betas verlieren ihr Update nicht mehr, wenn TestFlight startet.** Eine Beta mit einem wartenden neuen Build konnte auf „aktuell“ zurückfallen und so bleiben, bis du TestFlight erneut geöffnet hast.

**Ein-Klick-Updates für Dropbox und ToDesk laden die richtige Datei.** Auf Apple Silicon lud Dropbox bisher den reinen Intel-Build, den die Sicherheitsprüfung dann ablehnte; bei ToDesk wurde der Early-Access-Build angeboten, den ToDesk nur an einen Teil der Nutzer verteilt, statt der allgemeinen Version.

**„Neu starten“ schließt das Update von Spotify sofort ab.** Hatte Spotify sein eigenes Update bereits geladen, drehte sich „Neu starten“ minutenlang, bevor das Update durchging.

**Beta-Builds gehen jetzt in ihre Releases über.** Eine Xcode-Beta bekommt ihren Release Candidate angeboten, statt als aktuell zu gelten; CotEditor-Betas sehen dessen Release Candidates; und eine Carbon-Copy-Cloner-Beta, deren Testzyklus beendet ist, bekommt das Release angeboten, zu dem sie geworden ist, statt einer fehlgeschlagenen Prüfung.

**Versionshinweise passen zu deiner Version.** Blender 5.2 zeigte bisher die Hinweise zu 5.1, und Raycast 1.x die zu 2.x. „Seite öffnen“ bei Gemini öffnet jetzt Googles aktuelle Desktop-Seite statt einer Fehlerseite.

**Die Einstellungen erklären jede Option direkt daneben.** Lange Texte unter ganzen Abschnitten sind durch eine kurze Zeile unter jedem Bedienelement ersetzt, und Auswahlmenüs beschreiben nur die gewählte Option.

**Die App schreibt ihren Namen jetzt DuoUpdater**, passend zur Website.

## 0.3.94

**Homebrew-Pakete aus Taps, denen du nicht vertraust, tauchen wieder auf.** Seit Homebrew 6 überspringt brew beim Auflisten der Installationen stillschweigend Pakete aus nicht vertrauten Taps, sodass sie ohne Erklärung aus DuoUpdater verschwanden. Jetzt erscheinen sie als „Nicht geprüft · Tap nicht vertraut“, mit dem `brew trust`-Befehl zum Kopieren – und sobald du ihn (oder ein Upgrade) im Terminal ausgeführt hast, zieht das Fenster nach, wenn du zurückwechselst.

**Apps, die für ihr eigenes Update ein Administratorpasswort brauchen, werden nicht mehr doppelt aktualisiert.** Hatte eine App wie Tailscale ihr Update bereits geladen und wartete auf einen Neustart, sah DuoUpdater das nicht und bot trotzdem „Aktualisieren“ an – was mit dem eigenen Installer der App kollidieren konnte. Die Zeile bietet jetzt „Neu starten“ an, und „Alle aktualisieren“ lässt die App aus.

**Der Homebrew-Bereich kann jetzt Homebrew selbst aktualisieren.** Wenn eine neue Homebrew-Version erschienen ist, bietet eine Zeile ganz oben an, `brew update` für dich auszuführen. Hast du `HOMEBREW_NO_AUTO_UPDATE` gesetzt, bleibt sie ausgeblendet.

**Ein fehlgeschlagenes Homebrew-Upgrade zeigt jetzt den eigentlichen Fehler von brew.** Früher stand in der Zeile der Rat, den brew nach dem Fehler ausgibt, oder gar nichts – eine Meldung wie „deine Command Line Tools sind zu alt“ kam also nie bei dir an. Außerdem meldet ein Upgrade mehrerer Pakete nicht mehr, fertig zu sein, bevor es das ist.

**Das Menüleistensymbol zählt über 50 hinaus.** Bei mehr als 50 wartenden Updates blieb es bisher bei 50 stehen.

**Versionshinweise: Die von Claude sind wie in Claude selbst nach New, Improved und Fixed gegliedert; die von Mac Mouse Fix erscheinen als Text statt als eingebettete Seite; und Homebrews eigene App hat jetzt Versionshinweise.**

**`duo check` meldet nicht mehr „Everything is up to date.“, wenn es gar nicht prüfen konnte.** Es sagt jetzt, wenn es TestFlight nicht lesen konnte oder der App-Scan abgebrochen wurde.

## 0.3.93

**Versionshinweise erscheinen jetzt in deiner Sprache, wenn eine App sie in mehreren veröffentlicht.** Manche Apps liefern ihre Hinweise zu jeder Veröffentlichung übersetzt mit; DuoUpdater nahm bisher einfach die Übersetzung, die die App zufällig zuerst oder zuletzt aufführte, sodass die Hinweise einer App für alle auf Deutsch erschienen und die einer anderen von Veröffentlichung zu Veröffentlichung die Sprache wechselten.

**Apps, die eine Mac-Version nachgereicht haben, gelten nicht mehr als „Auf diesem Mac nicht unterstützt“.** Eine iPhone- oder iPad-App, die du auf Apple Silicon ausführst, wurde fälschlich genau in dem Moment so gekennzeichnet, in dem ihr Entwickler einen echten Mac-Build veröffentlichte — also durch die eine Änderung, die das Update verfügbarer macht statt weniger.

**Eine App-Store-App, die du zugleich als Beta testest, wird nicht mehr für einen TestFlight-Build gehalten.** Gab ein Entwickler eine Beta unverändert frei, trugen beide dieselbe Build-Nummer, und deine gekaufte Kopie wurde TestFlight zugeordnet — der App Store konnte ihr also nie ein Update anbieten.

**Ein App-Store-Update, das ein neueres macOS erfordert als das laufende, sagt das jetzt.** Bisher bot die Zeile die Installation trotzdem an, der App Store verweigerte sie im letzten Schritt, und nichts auf dem Bildschirm erklärte, warum.

**Das Fenster „Anfragen“ zeigt jetzt, wie weit sein Protokoll tatsächlich zurückreicht, und kennzeichnet Zeiträume, die es nicht vollständig abdecken kann.** Bisher sah „Letzte 30 Tage“ bei einem Protokoll, das nur ein paar Stunden zurückreichte, genauso aus wie „Letzte 24 Stunden“, und nichts auf dem Bildschirm erklärte, warum.

**Du entscheidest unter „Einstellungen“ → „Allgemein“, wie viel DuoUpdater für TestFlight-Betas tut.** *Beim Aktualisieren* liest, was TestFlight ohnehin schon weiß, und fragt dort nach einer frischen Antwort, wenn du aktualisierst; *Aktuell halten* lässt DuoUpdater auch von sich aus nachfragen, sodass eine im Hintergrund installierte TestFlight-Beta nicht mehr als Fragezeichen dasteht, bis du aktualisierst, und ein für dich bereitliegender Build nicht mehr hinter einem „aktuell“ übersehen wird; *Aus* liest nichts und sagt das in diesen Zeilen, statt zu raten. Macs mit bereits erteiltem Festplattenvollzugriff starten mit *Beim Aktualisieren*, alle anderen mit *Aus*.

**Es genügt jetzt, TestFlight selbst zu öffnen, damit DuoUpdater bemerkt, was dort installiert wurde.** Eine über TestFlight installierte Beta stand bisher als Fragezeichen da, bis du das nächste Mal aktualisiert hast — und unter macOS 27, wo TestFlights Hinweis „Ready to Test“ für bereits vorhandene Apps nicht mehr eintrifft, konnten das Stunden sein.

**Cline bekommt jetzt Updates, sowohl für seine reguläre als auch für seine Beta-Version, und zeigt seine Versionshinweise.** Bisher stand in seiner Zeile ein Fragezeichen statt einer Version: Es liefert keinen Update-Feed aus, den DuoUpdater lesen könnte, und ein Homebrew-Paket gibt es dafür nicht. DuoUpdater fragt jetzt dieselbe Adresse ab wie Clines eigener Updater, sodass das angebotene Update genau das ist, das Cline selbst installiert hätte, und die Beta-Version bleibt auf der Beta-Linie.

**Versionshinweise, die ihre Änderungen unter Überschriften wie „Added“ und „Fixed“ gruppieren, behalten diese Überschriften jetzt.** Bisher wurden alle Gruppen zu einer einzigen flachen Liste zusammengeführt, sodass nicht zu erkennen war, welche Änderungen neue Funktionen und welche Fehlerbehebungen waren.

**Mac Mouse Fix bietet jetzt seine Beta-Veröffentlichungen an, wenn du in seinen eigenen Einstellungen unter „General“ die Option „Get Beta Versions“ eingeschaltet hast.** Bisher sah DuoUpdater nur die regulären Veröffentlichungen von Mac Mouse Fix, sodass ein Beta-Build unbemerkt blieb, bis die nächste reguläre Version erschien.

**Apps, deren Build-Nummer nur ein fortlaufender Zähler ist, verbergen ihre eigenen Korrekturversionen nicht mehr.** Bei einer App, die sich als Version 12.10 mit Build 282987 meldet, las sich eine Veröffentlichung 12.10.1 bisher als „bereits aktuell“.

**Ein neuer Build einer App, die denselben Versionsnamen behält, wird wieder angekündigt.** War ein Build erst einmal angekündigt, kam jeder weitere unter diesem Namen still an — die Zeile leuchtete auf, die Zahl am Symbol zählte ihn mit, aber es erschien nie ein Banner.

**„Alle aktualisieren“ zählt jetzt nur noch Apps, die tatsächlich aktualisiert wurden.** Eine App, die zum Abschluss Apples Installationsprogramm öffnet, galt bisher schon als erledigt, während dessen Fenster noch offen war — „2 Apps wurden aktualisiert“ konnte also bedeuten, dass sich noch gar nichts geändert hatte.

**Ein Update, das angekommen ist, aber einen Rest hinterlassen hat, wird jetzt als installiert gemeldet statt mit der Aufforderung, „App-Verwaltung“ zu erteilen.** Die neue Version lief bereits, während die Zeile dich in die Systemeinstellungen schickte.

**„Alle aktualisieren“ abzubrechen, bricht jetzt auch den laufenden Download ab.** Bisher lief eine mehrere Gigabyte große Übertragung bis zum Ende weiter, versuchte es bis zu fünfmal erneut und bemerkte erst dann, dass sie abgebrochen worden war.

**Versionshinweise werden nicht mehr zwischen zwei Apps verwechselt, die sich eine Changelog-Seite teilen, und bleiben nach einem Update aktuell, wenn die Hinweise einer App auf einer eigenen Seite pro Version liegen.** Antigravity und Antigravity IDE konnten eine Viertelstunde lang die Hinweise der jeweils anderen zeigen; Thunderbird, WeChat, Opera und einige andere zeigten nach einem Update noch eine Weile die Hinweise der vorherigen Version.

**Die Suche in der App-Liste ignoriert jetzt Akzente, so wie die Suche in den Einstellungen es schon tat.** Die Eingabe „cafe“ findet jetzt „Café“.

**Die Seite „Diagnose“ führt jetzt eine Zustandszeile pro Release-Kanal auf.** Eine kaputte Beta- oder Preview-Regel blieb bisher hinter ihrem gesunden stabilen Gegenstück verborgen.

**Eine „Forbidden“-Antwort von GitHub wird nicht mehr als Ratenbegrenzung gemeldet.** Bei einem Repository, das auf privat gestellt wurde, oder einem Token ohne die nötige Berechtigung wurdest du bisher dazu gedrängt, ein Token zu hinterlegen, das gar nicht geholfen hätte.

**„Neu starten“ wird nicht mehr für eine sich selbst aktualisierende App angeboten, deren bereitliegender Build älter ist als der laufende.**

**Unter der Haube.** Installationen, Sicherungen und Paketprüfungen belegen nicht mehr die Threads, auf denen der Rest der App läuft, sodass das Menü währenddessen bedienbar bleibt; auch während eines großen Downloads bleibt das Menü flüssig; das Release-Protokoll zählt jede Veröffentlichung mit, die ein Anbieter unter einem Versionsnamen herausgibt; eine Backup-Kopie wird abgelehnt statt gespeichert, wenn darin das Programm der App selbst fehlen würde; der erste Start auf einem frischen Mac schreibt keine irreführenden Datenbankfehler mehr ins Protokoll; `duo verify` und `duo reconcile` melden jetzt auch ein Changelog, dessen Einträge in sich zusammengefallen sind, und eine Installationsadresse, die seit Tagen fehlschlägt; ein hängender `duo`-Befehl bricht seine Suche nach zwanzig Sekunden ab, statt stehen zu bleiben.

## 0.3.92

**Manche Apps, die sich selbst aktualisieren, sehen nicht mehr aktuell aus, während schon eine neuere Version vorliegt.** Bei Apps, deren Update-Informationen hinter einem träge aktualisierten Download-Server liegen, konnte DuoUpdater nach einer Veröffentlichung noch tagelang eine ältere Version sehen.

**Die Versionshinweise von Kimi erscheinen jetzt in DuoUpdater.**

**Updates von CodeEdit erscheinen jetzt.** Bei einer neueren CodeEdit-Version blieb in der Zeile bisher ein Fragezeichen stehen, statt dass das Update angeboten wurde.

**Unter der Haube.** Die Homebrew-Liste im Menü füllt sich schneller, und `duo`-Befehle starten schneller.

## 0.3.91

**„Erneut prüfen“ liefert bei einer TestFlight-Beta jetzt das echte Ergebnis.** Bisher wurde die Zeile dadurch bis zum nächsten Aktualisieren zu einem Fragezeichen.

**TestFlight-Betas behalten ihr Ergebnis, während die Schaltfläche zum Aktualisieren bei TestFlight nachfragt.** Bisher konnten sie für ein paar Sekunden alle zu Fragezeichen werden.

## 0.3.90

**super.engineering wird jetzt unterstützt: Update-Prüfungen, Versionshinweise und Ein-Klick-Installation.** Ein neuer Nightly-Build erscheint samt dem, was sich darin geändert hat, und „Aktualisieren“ installiert ihn für dich.

**Microsoft Edge Beta konnte dir einen stabilen Edge-Build anbieten.** Als Microsofts Beta-Feed kurzzeitig leer war, nannte die Zeile eine stabile Version, und ein Update hätte diese über deine Beta installiert.

**TestFlight-Betas zeigen jetzt die Updates, die TestFlight für sie bereithält — auch Betas von iPhone- und iPad-Apps.** Die Schaltfläche zum Aktualisieren fragt im Hintergrund bei TestFlight nach, ein Update verschwindet nicht mehr wenige Minuten, nachdem du es gefunden hast, und die TestFlight-Schaltfläche öffnet die Seite dieser Beta.

**Wenn DuoUpdater nicht feststellen kann, ob eine TestFlight-Beta aktuell ist, sagt es das.** Die Zeile zeigt ein Fragezeichen, statt die Beta aktuell zu nennen — zum Beispiel, wenn du bei TestFlight abgemeldet bist oder diese Beta nicht mehr testest.

**Festplattenvollzugriff wird jetzt erklärt, und ohne ihn nervt dich nichts.** Ohne ihn löst DuoUpdater die Warnungen von macOS zum Lesen der Daten anderer Apps nicht mehr aus; braucht ihn eine TestFlight-Beta oder CotEditor, erfährst du einmalig, warum er nötig ist und wo du ihn erteilen kannst.

**„Neu starten“ verschwindet, sobald eine aktualisierte App beendet wurde.** Eine App, die einen Hilfsprozess weiterlaufen ließ, bat noch lange um einen Neustart, nachdem ihr Update wirksam geworden war.

**Eine unbeantwortete Datenschutzabfrage von macOS hält Update-Prüfungen nicht mehr auf.** Die Prüfung läuft ohne die Einstellung der betreffenden App weiter.

**Die Versionshinweise von Rockxy und Ollama sind wieder vollständig.** Bei Rockxy reichen sie über die jüngsten Veröffentlichungen zurück, statt nur die neueste zu zeigen, und bei Ollama wird die neueste Veröffentlichung nicht mehr ausgelassen.

**`duo`, der optionale Begleiter für die Kommandozeile, kommt besser mit TestFlight zurecht.** `--refresh-testflight` funktioniert, ob TestFlight geöffnet ist oder nicht, ohne deinen Bildschirm in Beschlag zu nehmen, und `duo check` nennt eine Beta nicht mehr aktuell, wenn TestFlight einen neueren Build angekündigt hat.

## 0.3.89

**Telegram Desktop wird wieder auf Updates geprüft.** Telegram hat die Datei, die es veröffentlicht, umbenannt, und die Zeile konnte daraus keine Version mehr lesen — sie zeigte deshalb einen Prüffehler statt des Updates, das dahinter wartete.

**Eine über TestFlight installierte iPhone- oder iPad-App wird als solche erkannt.** DuoUpdater hielt sie stattdessen für einen Kauf aus dem App Store, also nannte die Zeile den falschen Verwalter, während der Store nach einem Eintrag gefragt wurde, den es gar nicht gibt — bei jeder Prüfung, solange die App installiert blieb.

**Eine TestFlight-Beta ist mit TestFlights eigenem Symbol gekennzeichnet.** Zeilen, um die sich der App Store kümmert, trugen bereits das Symbol des Stores; die, um die sich TestFlight kümmert, schrieben den Namen stattdessen aus — dieselbe Art von Zeile war also auf zwei verschiedene Weisen gekennzeichnet.

**Die Kopfzeile des Netzwerk-Fensters bleibt beim Wechsel der Tabs an ihrem Platz.** Die beiden Tabs setzten ihre Überschrift auf leicht unterschiedliche Höhen, sodass der Wechsel zwischen ihnen aussah, als würde das Fenster zucken.

## 0.3.88

**Das Scrollen durch die ganze App-Liste ist wieder flüssig.** Ein schnelles Scrollen durch die vollständige Liste ließ Frames fallen; jede Zeile gibt ihre Höhe jetzt an, ohne dafür erst aufgebaut werden zu müssen.

**Die Versionshinweise einer App aus dem App Store kommen jetzt immer aus dem App Store.** Wenn die Abfrage des Stores für eine App ins Leere lief oder fehlschlug, konnte das Fenster auf die Hinweise der anderen Vertriebsform dieser App zurückfallen — ein anderer Build mit eigenen Versionsnummern — und beschrieb damit eine Veröffentlichung, die deiner Kopie nie angeboten worden wäre.

**Windscribe im Beta- oder Guinea-Pig-Kanal bekommt die Builds dieses Kanals angeboten.** DuoUpdater liest, welchen Update-Kanal du in Windscribes eigenen Einstellungen gewählt hast, sodass einer Kopie auf einer Vorabversionslinie nicht länger gesagt wird, sie sei aktuell, während es auf dieser Linie neuere Builds gibt. Das Fenster zeigt auch die Hinweise zu diesen Vorabversionen, die es zuvor nur für stabile Builds aufgeführt hat.

**Windscribe wird jetzt auf Updates geprüft, mit seinen Versionshinweisen.** Eine Kopie auf einem älteren Build wird mit der Version aufgeführt, auf die sie wechseln kann, und was sich darin geändert hat; zuvor hatte DuoUpdater keine Möglichkeit, Windscribes Version überhaupt zu sehen. Aktualisiert wird weiterhin über Windscribes eigenen Installer, der auch Teile einrichtet, die außerhalb der App liegen.

**Ein Update wird nicht mehr auf eine App angewendet, die während deines Klicks verschwunden ist.** Wird die App zwischen dem Klick und dem Beginn der Installation deinstalliert, ersetzt oder unlesbar, hält DuoUpdater jetzt an und sagt es — statt trotzdem über diesen Ort zu installieren.

**`duo`, der optionale Begleiter für die Kommandozeile, nennt eine Paketinstallation nicht mehr abgeschlossen, bevor sie es ist.** Beim Installieren einer App, die als `.pkg` ausgeliefert wird, öffnet sich der Installer von macOS und der Rest bleibt dir überlassen — die Zusammenfassung zählte das aber als installiert: „1 installed“, während noch nichts ersetzt war. Beides wird jetzt getrennt gezählt. Die `--json`-Ausgabe kennzeichnet außerdem jede Zeile damit, was mit dieser App geschehen ist, sodass ein Skript nicht mehr den englischen Erklärungstext lesen muss, um einen Fehlschlag von einem bewussten Überspringen zu unterscheiden.

**Under the hood.** Die Prüfung unmittelbar vor der Installation, die ein Ein-Klick-Update absichert, schützt jetzt auch `duo install`; und die Kontrollen, die ein Download bestehen muss, bevor er eine App ersetzt, liegen für beide Wege, die sie nutzen, an einer Stelle.

## 0.3.87

**Ein Klick auf „Aktualisieren“ bleibt nicht mehr wirkungslos, wenn eine Update-Quelle sich selbst widerspricht.** Meldet die Prüfung, die im Moment des Klicks läuft, eine ältere Version als die, die in der Zeile angeboten wurde, sagt DuoUpdater das jetzt und lässt das Update im Angebot. Zuvor galt die App als bereits aktuell und verschwand aus der Liste – und dasselbe Update tauchte bei der nächsten Prüfung wieder auf.

**Fork-Updates werden wieder angeboten, wenn Fork auf den Develop-Kanal eingestellt ist.** DuoUpdater las Forks Kanaleinstellung verkehrt herum und folgte dem Stable-Feed, der deutlich hinterherhinkt – eine Develop-Installation galt damit als aktuell, während Fork selbst schon eine neuere Version anbot.

**Mac Performance Monitor zeigt jetzt seine Versionshinweise.** Die App veröffentlicht sie in ihrem Repository statt in dem Feed, den wir lesen, sodass das Fenster nichts dazu anzuzeigen hatte.

**CotEditor wird jetzt unterstützt – auf der Release- wie auf der Beta-Linie.** Welcher Linie eine Installation folgt, ergibt sich aus der laufenden Version und aus CotEditors eigener Einstellung „Update to prereleases when available“; eine Beta-Installation erhält damit die nächste Beta statt einer Version, die sie zurückwerfen würde.

**Eine aus dem App Store installierte App bekommt nie einen Download von anderswo angeboten.** Schlägt die Abfrage des Stores fehl oder bleibt sie leer, weist die Zeile jetzt darauf hin, dass der Store diese App verwaltet – ohne Versionsnummer. Zuvor konnte die Prüfung auf den anderen Vertriebsweg der App ausweichen – ein anderer Build mit eigenen Versionsnummern – und anbieten, diesen über die Store-Kopie zu installieren.

**Einer App wird nie ein Update angeboten, das sie auf eine ältere Version zurücksetzen würde.** Manche Feeds führen eine stabile Version über einer Vorabversion, die in Wahrheit weiter ist – sie zu installieren hätte die App zurückgeworfen.

**Eine lange Fehlermeldung in einer Zeile schiebt den Rest der Liste nicht mehr nach unten.** Sie wird auf zwei Zeilen gekürzt, der vollständige Text erscheint beim Daraufzeigen.

## 0.3.86

**Vier weitere Apps werden abgedeckt: WhatCable, Qoder IDE, Qoder und Yaak.** Jede von ihnen erhält Update-Prüfungen und eine Installation mit einem Klick, und ihre Versionshinweise werden als Text ins Fenster geladen statt als eingebettete Seite.

**Die beiden Mac-Apps von Qoder werden nun auseinandergehalten.** Die IDE und die Desktop-App teilen sich Namen und Download-Seite, erscheinen aber in getrennten Versionsreihen; jede wird jetzt für sich verfolgt.

**Beta-Builds von WhatCable und Yaak werden auf einem eigenen Kanal verfolgt.** Eine Kopie mit einer Beta hatte bisher überhaupt keine Quelle und blieb auf „Failed“ stehen; ihr wird nun die nächste Beta angeboten, mit Versionshinweisen getrennt von denen der finalen Versionen. Bei WhatCable gehört dazu auch die finale Version, in die eine Beta schließlich übergeht – wer diese nimmt, wechselt damit auf den finalen Kanal.

## 0.3.85

**Das Prüfen Ihrer App-Store-Apps braucht nur noch einen Bruchteil des bisherigen Datenverkehrs.** Bei jeder Prüfung wurde bisher die Produktseite jeder App-Store-App erneut geladen; die Seiten werden jetzt eine Stunde lang behalten, und der Store wird in wenigen Anfragen nach allen Ihren Apps gefragt statt in einer pro App. Bei einem Prüfintervall von fünf Minuten sind das insgesamt etwa ein Drittel weniger Datenverkehr; beim voreingestellten Intervall von sechs Stunden verfallen die Seiten weiterhin zwischen zwei Prüfungen, dort ist die Ersparnis also geringer.

**Eine einzelne App erneut zu prüfen lädt nicht mehr alle App-Store-Apps neu.** Ein einzelnes „Check Again“ verwarf bisher jede zwischengespeicherte Produktseite, sodass die nächste geplante Prüfung für alle zahlte; es aktualisiert jetzt nur noch die App, nach der Sie gefragt haben.

**Das Prüfen von Apps, die über GitHub ausgeliefert werden, kostet nur noch einen Bruchteil des bisherigen Datenverkehrs.** Bei jeder Prüfung wurde bisher die vollständige Beschreibung jeder Version erneut geladen, auch wenn nichts veröffentlicht worden war; jetzt wird GitHub gefragt, ob sich die Version seit dem letzten Mal geändert hat, und nichts geladen, wenn nicht. Einmal täglich wird jede Version vollständig neu gelesen, sodass eine zurückgezogene Version innerhalb eines Tages auffällt.

**Apps auf einem Beta- oder Nightly-Kanal von GitHub fragen jetzt nach einer Version statt nach einer ganzen Seite davon.** Die neueste Version ist fast immer die Antwort; die vollständige Seite wird nur in den Runden geladen, in denen sie es nicht ist.

**Die Update-Prüfung von Vorssaint läuft nicht mehr über eine Weiterleitung.** Das Repository wurde umbenannt, und dem alten Namen zu folgen setzte die Anfrage stillschweigend auf das anonyme Anfragelimit von GitHub; die Prüfung geht jetzt direkt an den neuen Namen.

**Unter der Haube.** Das Release-Artefakt wird jetzt auf einem von GitHub gehosteten Mac gebaut, signiert und notarisiert, mit einer Build-Herkunft, die jede und jeder überprüfen kann, und das aufgezeichnete Anfrageprotokoll unterscheidet eine zwischengespeicherte Antwort von einer aus dem Netz.

## 0.3.84
**Exportierte Anfrageprotokolle enthalten nicht mehr Ihren Kontonamen.** Für Apps in Ihrem Benutzerordner stand bisher in jeder Zeile der vollständige Pfad; jetzt steht dort `~`, unabhängig davon, wie Sie das Protokoll herausnehmen.

**Der Tab „Requests“ sagt jetzt, was er erfasst und was nicht.** Erfasst werden die Abrufe, die DuoUpdater selbst durchführt. Eine Seite mit Versionshinweisen lädt ihre eigenen Bilder und Schriften, und Updates aus dem App Store und von Homebrew werden von eigenen Werkzeugen ausgeführt – nichts davon erscheint dort, und das Fenster sagt es nun, statt es Ihrer Annahme zu überlassen.

**„Copy URL“ maskiert die Adresse jetzt, bevor es sie herausgibt.** Pfade mit einem Leerzeichen – etwa die Downloads von Firefox, Thunderbird und Bartender – wurden roh kopiert; ein Browser gleicht das aus, die Kommandozeile nicht.

**Die Beta-Zeile von CapCut meldet keine fehlgeschlagene Prüfung mehr.** Zwischen zwei Betas – nachdem eine zur finalen Version geworden ist und bevor die nächste beginnt – veröffentlicht der Hersteller auf diesem Kanal nichts. Das erschien als rote Zeile mit einem „Retry“, das nicht funktionieren konnte. Die Zeile hat nun einfach keine Antwort aus dieser Quelle, bis die nächste Beta erscheint.

**Audacity zeigt jetzt das Zeichen dafür, womit es gebaut ist.** Es startet über einen kleinen Starter, der an das eigentliche Programm daneben übergibt, und das Zeichen wurde am Starter abgelesen – der nichts einbindet.

**App-Store-Versionshinweise erscheinen jetzt in Ihrer Sprache.** Sie wurden immer in der Standardsprache des Stores abgerufen, sodass ein Mac mit chinesischer oder japanischer Sprache sie weiterhin auf Englisch las.

**Die eigenen Updates von DuoUpdater erscheinen jetzt unter seinem eigenen Namen im Fenster „Network“.** Seine Versionsprüfung, seine Versionshinweise und sein Download standen alle mit leerer App-Spalte darin.

**Unter der Haube.** Ein Zugangsschlüssel im Pfad einer Webadresse wird jetzt entfernt, bevor die Anfrage aufgezeichnet wird – so wie einer in einer Abfragezeichenfolge schon immer.

## 0.3.83

**Das Fenster „Download Traffic“ heißt jetzt „Network“ und hat einen zweiten Tab.** „Downloads“ ist die Aufstellung, die Sie schon kannten — was jedes Update als Datei gekostet hat. „Requests“ ist neu: jeder Abruf, den DuoUpdater für Sie macht, wofür er war, zu welcher App er gehörte und was er gekostet hat, mit einem Filterfeld, um das Protokoll zu befragen statt es zu durchscrollen. Die Zahlen oben beantworten das, wonach Sie gerade gefiltert haben — nicht die Gesamtsumme.

**Die Update-Prüfung für Spotify braucht kaum noch Netzwerk.** Spotify veröffentlicht nirgends eine Versionsdatei, also liest die Prüfung die Version aus einem zwei Megabyte großen Installer-Stub — und holte sich jedes Mal die ganze Datei, rund um die Uhr. Sie fragt jetzt zuerst, ob sich die Datei geändert hat, und lädt sie nicht herunter, wenn nicht.

**PDF Expert findet jetzt seine Updates und zeigt, was sich geändert hat.** Es las eine Versionsliste, die der Hersteller seit 2022 nicht mehr ergänzt hat, und meldete sich deshalb als aktuell — ganz gleich, welche Version installiert war. Jetzt folgt es der Liste, die der eigene Updater der App verwendet, und zeigt auch die Hinweise zu früheren Versionen, nicht nur zur neuesten.

**UTM-Vorschauversionen erhalten jetzt eigene Updates, lassen sich mit einem Klick installieren und zeigen die passenden Versionshinweise.** Bisher wurde eine Vorschauversion am finalen Zweig gemessen und stillschweigend als aktuell gemeldet; sie folgt nun ihrem eigenen Zweig und wechselt zurück auf den finalen Zweig, sobald sie dessen finale Fassung übernimmt.

**Unter der Haube.** Die aufgezeichneten Anfragen liegen jetzt im selben Speicher wie die Download-Aufstellung, sodass die beiden Abrechnungen desselben Downloads nicht mehr auseinanderlaufen können. Ihre bestehende Aufstellung wird unverändert übernommen.

## 0.3.82

**Fünfzehn weitere Apps werden auf Updates überwacht, und alle bis auf eine lassen sich mit einem Klick installieren.** AgentsView, AnythingLLM, Chatbox, ChatGPT Classic, DSH Desktop, FluidVoice, GitHub Copilot, Kun, Meetily, Microsoft 365 Copilot, OpenLogi, OpenSuperWhisper, Paseo, T3 Code — auf beiden Kanälen, Alpha und Nightly — und Vorssaint. Die Ausnahme ist ChatGPT Classic: Geprüft wird es für Sie, aber es bringt ein Installationsprogramm mit, das die App selbst aktualisiert — dieses eine Update bleibt Ihre Sache.

**Acht Apps zeigen ihre Versionshinweise jetzt in DuoUpdater, statt Sie auf eine Webseite zu schicken.** Xcode, Antigravity, Antigravity IDE, AnyDesk, AnythingLLM, Chatbox, Headlamp und Helium. Bei dreien gab es überhaupt nichts, wohin man Sie hätte schicken können: Die Seite von AnyDesk antwortet mit einer Bot-Abfrage, die Macher von Helium veröffentlichen gar keine Hinweisseite, und Antigravity IDE hatte keinerlei Link.

**Xcode-Betas sagen jetzt, was sich in der jeweiligen Beta geändert hat.** Bisher bot die Zeile eine Build-Nummer und einen Link; jetzt listet sie Apples Hinweise zu Ihrer Beta und zu allen vorherigen Betas derselben Version.

## 0.3.81

**Word, Excel, PowerPoint, Outlook und OneNote merken jetzt, wenn ihr Update angekommen ist.** Diese fünf boten weiterhin an, das längst abgeschlossene Installationsprogramm erneut zu öffnen, und kamen nie beim Neustart an — Warten oder erneutes Prüfen half nicht.

**Eine Zeile, die auf den Neustart wartet, erklärt sich wieder – in beiden Fenstern.** War eine App zugleich neuer als das, was ihr Anbieter veröffentlicht, und wartete auf einen Neustart, beschrieb die Zeile über der Schaltfläche etwas ganz anderes; bei Zeilen, die auf einen Neustart nach „Alle aktualisieren" warteten, widersprachen sich Fenster und Menü zusätzlich.

**Statusetiketten verdrängen den App-Namen im Russischen nicht mehr.** Einige Etiketten hatten außerdem in allen übersetzten Sprachen Formulierungen aus unverwandten Texten übernommen.

**Für Apps, deren Anbieter eine Version nur datiert, ohne sie zu terminieren, erscheinen jetzt Versionshinweise.** Bisher hinterließen solche Veröffentlichungen nirgends eine Spur.

**Eine Seite mit Versionshinweisen, die wir nicht öffnen, nennt jetzt den Grund.** Zuvor blieb sie leer, was genau wie eine App aussah, die gar keine Hinweise veröffentlicht.

**Die Nightly-Versionshinweise der WeChat DevTools sind nicht mehr leer.**

**Unter der Haube.** Versions- und Veröffentlichungsdaten werden an einer Stelle behandelt, sodass eine Build-Nummer nie als Marketing-Version gelesen wird und die Veröffentlichungs-Chronik keine Uhrzeit erfindet, die der Anbieter nie genannt hat.

## 0.3.80

**Fehlgeschlagene Prüfungen sind jetzt auch im Fenster sichtbar.** Das Fenster zeichnete für eine Zeile, deren Prüfung fehlgeschlagen war, gar nichts — ebenso wenig für ignorierte, übersprungene oder vom App Store, von Toolbox oder TestFlight verwaltete Zeilen. Das sah genauso aus wie „auf dem neuesten Stand". Beide Fenster sagen jetzt dasselbe über dieselbe App, und die Schaltfläche zum erneuten Versuch gibt es in beiden.

**„Check Again" fragt in jeder Zeile nur nach dieser einen App.** Dabei wird auch neu eingelesen, welche Apps laufen — der schnellste Weg, einen grünen Punkt zu korrigieren, der falsch aussieht.

**Überspringen und Ignorieren lassen sich jetzt auch über das Kontextmenü des Fensters rückgängig machen.** Beide Zeilen forderten dazu auf, per Rechtsklick zu widerrufen — in einem Fenster, dessen Menü genau das nicht anbot.

**Die Hintergrundprüfung nimmt Ihnen keine Versionshinweise mehr weg, die Sie gerade lesen.** Die stündliche Prüfung verwarf alle bereits geladenen Hinweise, sodass ein offenes Fenster mit Versionshinweisen zurück zur Ladeanzeige sprang. Nur eine von Ihnen ausgelöste Aktualisierung beginnt jetzt von vorn.

**Unterbrochene Downloads werden geprüft, bevor sie als vollständig gelten.** Ein Server, der die ganze Datei erneut schickte, und einer, der vorzeitig abbrach, wurden bislang beide beim Wort genommen; der Ärger zeigte sich erst einen Schritt später als Installation aus einem defekten Archiv. Downloads hinter bestimmten Proxys, die zuvor dauerhaft fehlschlugen, funktionieren jetzt.

**Das Aktualisieren einer App liest nicht mehr alle Apps auf Ihrer Festplatte neu ein.** Jeder Klick führte zwei vollständige Durchläufe über alle Ihre Programme aus, nur um das eine angefragte anzusehen.

**Chromes Versionshinweise können nicht mehr hängen bleiben.** Eine gewöhnliche Umgestaltung von Googles Blog genügte, um sie minutenlang zu blockieren; jetzt laden sie zügig, wie die Seite auch aussieht.

**Ein bereits geladenes Installationsprogramm wird nicht mehr verworfen und erneut geholt.** Wenn ein Anbieter dieselbe Version auf zwei Arten schrieb (`v1.2.3` und `1.2.3`), wurde das wartende Paket nicht wiedererkannt, und „Neu starten" konnte auf einen Austausch warten, der längst erfolgt war.

**Eine fehlgeschlagene Installation mit Administratorrechten wird nicht mehr für einen Abbruch durch Sie gehalten.** Es wurde kein Fehler angezeigt, und die betreffende App bot stillschweigend keine Ein-Klick-Aktualisierung mehr an, bis Sie über ihr Zeilenmenü erneut Administratorrechte anforderten.

**Der grüne Punkt für „läuft" bemerkt jetzt jede App.** Bei manchen Apps meldet macOS das Starten oder Beenden überhaupt nicht, und ihr Punkt blieb falsch, bis etwas Unbeteiligtes ihn auffrischte.

**„Alle aktualisieren" flackert während einer Aktualisierung nicht mehr**, und eine App, die Sie mitten in der Aktualisierung aktualisieren, wird nicht mehr auf den bereits installierten Stand zurückgesetzt.

**Schlägt die Prüfung in dem Moment fehl, in dem Sie auf Aktualisieren drücken, wird das gesagt** — statt als „nichts zu tun" abgelegt zu werden.

**Unter der Haube.** Ungewöhnlich geschriebene Veröffentlichungsdaten werden korrekt gelesen, die Adressprüfung für Seiten mit Versionshinweisen erfasst jede gleichwertige Schreibweise, und die laufende Buchführung greift nicht mehr jedes Mal auf die Festplatte zu, wenn auf Ihrem Mac irgendeine App startet oder endet.

## 0.3.79

**Dockers Kennzeichnung beschreibt jetzt Dockers Oberfläche statt seines Hintergrunddienstes.** In der Zeile stand „nativ“; Docker Desktop ist eine Electron-App. Die Kennzeichnung wird aus dem Bundle der App gelesen, und Dockers Bundle ist eine Hülle: Das darin benannte Programm ist ein in Go geschriebener Hintergrunddienst, es bringt kein eigenes Framework mit, und die App, die tatsächlich Fenster zeichnet, sitzt eine Ebene darin. Alles wurde korrekt gelesen — nur aus der falschen Datei. DuoUpdater schaut nun in die verschachtelte App, aber ausschließlich dann, wenn die äußere selbst nichts beisteuert und genau eine solche App enthält, die belegt, womit sie gebaut ist. Ein Hilfsprozess, der neben einer echten Oberfläche ausgeliefert wird, kann seine Identität also weiterhin nicht an seinen Wirt verleihen. Docker liest sich als Electron 42.5.0, und von den hundertsechsundvierzig Apps in der Liste auf dem Rechner, auf dem dies geschrieben wurde, ist es die einzige Zeile, die sich ändert.

**Die Release Notes von CleanShot X sind wieder lesbar — auch die zu 5.0.** CleanShot hat seine größte Aktualisierung seit Jahren veröffentlicht und dabei die Changelog-Seite neu gebaut: Das Datum steht jetzt über der Versionsnummer, um sie herum sind zwei Ebenen hinzugekommen, und bei einer Funktionsversion stehen zwischen Version und Änderungsliste noch eine Einleitung und zwei Videolinks. Die Leseregel, die DuoUpdater verwendete, passte auf nichts davon mehr. Schlimmer als ein leeres Fenster war, dass es nicht leer aussah: Die bereits gespeicherten Notizen der vorherigen Version blieben unter der Überschrift der neuen stehen — das Fenster sagte „5.0“ über den Änderungen von 4.8.10, und nirgends stand etwas anderes.

**Vor einer Veröffentlichung gespeicherte Notizen werden jetzt erneut gelesen statt für immer geglaubt.** Das ist die andere Hälfte derselben Geschichte. DuoUpdater wusste sechs Minuten vor CleanShots Veröffentlichung des Inhalts, dass es 5.0 gibt, sicherte die Seite in ihrem damaligen Zustand und legte sie unter 5.0 ab — und von da an fand jede Prüfung etwas Gespeichertes und schaute nie wieder nach. Gespeicherte Notizen werden nun einmal pro Sitzung beim Anbieter bestätigt, bevor sie als endgültig gelten; eine Version, deren Notizen verspätet kommen, ergänzt sich damit beim nächsten Öffnen selbst, statt falsch stehen zu bleiben.

**Was Sie aus der Menüleiste öffnen, öffnet sich jetzt beim ersten Klick.** „Änderungen“ in einer Zeile zu wählen — oder die Einstellungen oder das Release-Log — bewirkte beim ersten Mal gar nichts und funktionierte erst ab dem zweiten Klick. Wer das Menü erneut öffnete, machte jeden Versuch wieder zum ersten; für alle, die das Menü öffnen, einmal klicken und ein Fenster erwarten, hat es also nie funktioniert. Der erste Klick im Menü wurde dafür verbraucht, DuoUpdater nach vorn zu holen, statt das zu tun, worauf er zielte. Das geöffnete Fenster wird jetzt außerdem vorn gehalten: Es wurde nach vorn gebracht und im selben Moment wieder nach hinten geschoben — die andere Art, auf die das wie ein wirkungsloser Klick aussah.

## 0.3.78

**Eine App, die ihren eigenen Namen leer lässt, bekommt jetzt trotzdem einen.** Eudic (欧路词典) stand mit Symbol und Version in der Liste, und dort, wo der Name hingehört, stand gar nichts. Das Bundle deklariert durchaus einen Anzeigenamen — und lässt ihn leer, weil die eigentlichen Namen in den Übersetzungen der App liegen — und DuoUpdater nahm diese leere Antwort als Antwort, statt die nächste Frage zu stellen. Jetzt greift es auf den anderen Namen der App zurück und danach auf den Namen der App-Datei, sodass keine Zeile mehr namenlos bleibt. Von hundertfünfzig Apps hier war eine betroffen; der Punkt ist, dass die Information längst da war und übersprungen wurde.

**Der Neustart sagt jetzt, wenn die App sich geweigert hat zu beenden, statt wie ein Klick ins Leere zu wirken.** Wenn eine App ein Fenster geöffnet hat, das auf Sie wartet — eine Sicherungsabfrage, ein Anmeldeblatt, irgendein Dialog —, lässt macOS sie nicht beenden, und daran kann diese Seite nichts ändern: Es ist das Fenster der App. DuoUpdater drehte sich früher dreißig Sekunden lang und stellte dann kommentarlos dieselbe Taste zurück, was genau wie eine kaputte Taste aussieht — also drückt man sie noch einmal. Die Zeile sagt jetzt, was passiert ist, und verweist Sie an die App, um sich um dieses Fenster zu kümmern. Nichts wurde verändert und die neue Version ist bereits installiert, es fehlt also wirklich nur der Neustart — und wenn Sie die App in den nächsten zehn Minuten einfach selbst beenden, merkt DuoUpdater das und bringt sie in der neuen Version zurück, ohne dass Sie irgendwo klicken.

**Die Versionshinweise von 欧路词典 sind wieder eine Veröffentlichung und nicht sechzehn Jahre davon.** Die App veröffentlicht ihre gesamte Geschichte — jede Version zurück bis 2.5.0 — innerhalb der Hinweise zu derjenigen Veröffentlichung, die gerade die neueste ist. Der Detailbereich zeigte das alles unter der Überschrift „26.9.0", und man musste an einem Jahrzehnt vorbeiscrollen, um zu finden, was sich soeben geändert hatte. Jede Version bekommt jetzt ihren eigenen Eintrag in der Liste, so wie bei jeder anderen App auch.

**Das Zeichen, das sagt, womit eine App gebaut ist, verlangt für Tauri jetzt einen Beweis statt einer Ähnlichkeit.** Tauri hinterlässt in einem Bundle nichts Auffindbares — kein Framework, keinen eigenen Ordner —, also wurde dieses eine Zeichen daraus erschlossen, wie die App verpackt ist, plus der Tatsache, dass sie Apples Webansicht einbindet. Longbridge passt auf all das und ist kein Tauri: Es zeichnet seine Fenster mit demselben Renderer wie Zed und bettet für eine Ecke seiner Oberfläche eine Webansicht ein. DuoUpdater liest jetzt Tauris eigenen Fingerabdruck aus der Binärdatei, bevor es die Behauptung aufstellt — eine App heißt also Tauri, wenn sie eine ist, und Longbridge liest sich als das, was es ist: eine native Mac-App.

**CapCut wird nicht mehr rot, weil ByteDances Server eine schlechte halbe Sekunde hatten.** Der Endpunkt, den DuoUpdater nach CapCuts Version fragt, antwortet mit einem Erfolgscode — und etwa jedes fünfzigste Mal mit einem Fehlerobjekt statt mit der Antwort: eine Zeitüberschreitung innerhalb von ByteDances eigener Infrastruktur, rund 390 Bytes, wo 436 Kilobyte erwartet wurden. Diese Seite konnte das nicht davon unterscheiden, dass CapCut die Form seiner Antwort geändert hat, also meldete die Zeile eine fehlgeschlagene Prüfung — was sich liest wie „das ist kaputt und jemand muss es reparieren" für etwas, das sich beim nächsten Versuch von selbst erledigt. Diese eine bestimmte Form wird jetzt als das erkannt, was sie ist: Die Anfrage wird sofort wiederholt, und als echtes Problem wird sie erst gemeldet, wenn sie fünf Tage lang anhält.

## 0.3.77

**Jede App in der Liste sagt jetzt, womit sie gebaut ist.** Eine Zeile für eine App-Store-App trug schon immer das Zeichen des Stores, und jede andere Zeile sah gleich aus — eine Sparkle-App, eine Electron-App und eine native waren nicht zu unterscheiden. Neben jedem Namen steht nun das Zeichen der Technologie selbst — ein Klick zeigt den Namen und einen Satz, ein Verweilen den Tooltip: Electron, Tauri, Flutter, Qt, Java, Chromium, Mac Catalyst, eine iPhone-App auf Apple Silicon oder eine native Mac-App — samt der Version der Laufzeit, wo sie sich als Tatsache lesen lässt (Electron 42.4.1, Qt 6.2, das eingebettete Chromium, das Tauri, mit dem gebaut wurde). Gelesen wird das aus dem App-Paket selbst — dem Framework, das ein Packager mitliefern musste, der Laufzeit, die ein Starter braucht, den Bibliotheken, die das Binary linkt — also eine Tatsache darüber, was installiert ist, und keine Vermutung aus dem Namen. Wo ein langer Name keinen Platz lässt, tritt das Zeichen zur Seite, statt den Namen in eine zweite Zeile zu drängen: die Zeile gehört dem Namen. Abschalten lässt sich das Ganze unter Einstellungen → Allgemein.

**LibreOffice behauptet nicht länger ein Downgrade auf die Version, die Sie schon haben.** Sein Download-Index führt dreiteilige Versionen (`26.8.0`), während die installierte Kopie vier meldet (`26.8.0.3`); das Auffüllen des fehlenden Teils mit einer Null ließ die installierte Kopie neuer aussehen — und so trug die Zeile einen gedämpften Hinweis "der Hersteller liegt hinter Ihnen" über eine App, die genau aktuell war. Eine Quelle, die weniger Teile veröffentlicht als die App meldet, wird nun als Beschreibung derselben Version gelesen und nicht als ältere; ein echter Rückschritt sagt das weiterhin.

**Eine Einstellung, die ein Update hinzugefügt hat, weist jetzt einmal auf sich hin.** Eine neue Option mitten im Einstellungsfenster ist eine Option, die niemand findet. Nach einem Update, das eine hinzugefügt hat, trägt das Zahnrad in der Menüleiste einen blauen Punkt, die zugehörige Seite einen in der Seitenleiste und das Bedienelement selbst einen, bis Sie es angesehen haben. Nur für die, die tatsächlich dorthin aktualisiert haben — eine frische Installation wird nicht mit Punkten auf Funktionen begrüßt, die für sie einfach zur App gehören.

## 0.3.76

**Die Electron-Apps aus der letzten Version lassen sich jetzt tatsächlich mit einem Klick aktualisieren.** 0.3.75 brachte DuoUpdater bei, die Datei zu lesen, die solche Apps in sich tragen — und gelesen hat es sie auch: die neue Version, den passenden Download für Ihren Mac, die Prüfsumme dazu. Nur hatte es nirgendwohin damit. Der Installationsknopf erschien nie, und `duo install` nannte einen Grund für die Ablehnung, der nicht der wirkliche war. Mit dieser Version ist der Installationsweg angeschlossen. In der Praxis merken Sie davon womöglich nichts: Dieses Lesen sitzt hinter jeder handgeschriebenen Regel und spricht nur für eine App, die sonst nichts abdeckt — und das ist heute, auf dem Rechner, auf dem dies gebaut wurde, keine einzige. Es zählt für die App, die Sie morgen installieren und für die noch niemand eine Regel geschrieben hat.

**Ein Download, der Sie von einem nativen Build wegholen würde, wird jetzt abgelehnt.** Apple Silicon kann Intel-Apps weiterhin übersetzt ausführen — ein reiner Intel-Download bestand also die Prüfung „läuft das auf diesem Mac?" und wurde sauber installiert. Zurück blieb eine übersetzt laufende Kopie einer App, die zuvor nativ lief; still, und bei jeder künftigen Aktualisierung aufs Neue. Jede Installation an Ort und Stelle vergleicht nun das Vorhandene mit dem Eingetroffenen und verweigert diesen Tausch. Der umgekehrte Weg, und der von einem Universal-Build zu einem Apple-Silicon-Build, ist normal und weiterhin erlaubt.

**Wenn sich die Architektur eines Downloads nicht belegen lässt, wird jetzt abgelehnt statt geraten.** Manche Anbieter veröffentlichen neben ihrem Standard-Download einen Apple-Silicon-Build, und der einzige Hinweis darauf, dass der Standard der Intel-Build ist, besteht darin, den anderen zu bemerken. Kommt diese zweite Prüfung nicht sauber zurück — der Server des Anbieters verweigert sie, die Verbindung bricht ab, oder beide sind sich über die Version uneins — behandelt DuoUpdater dieses Schweigen nicht länger als Antwort. Es nennt die Version und bietet keine Installation an. Das ist das ehrliche Ergebnis.

**Eine defekte Regel kann nicht mehr aus der Diagnose verschwinden.** Bei Apps, die sowohl von einer handgeschriebenen Regel als auch vom neuen Leseweg erfasst werden — absichtlich die Mehrzahl —, wurde ein Fehlschlag der Regel dadurch aufgehoben, dass der Leseweg unmittelbar danach Erfolg hatte. Eine App mit tatsächlich defekter Regel las sich weiterhin als gesund. Beide werden jetzt getrennt erfasst. Der Leseweg meldet außerdem erstmals eigene Fehlschläge: Auf dem Rechner, auf dem dies gebaut wurde, zeigten drei Apps auf Adressen, die seit geraumer Zeit „nicht gefunden" zurückgeben — und nichts hätte das zuvor ausgesprochen.

**Zeilen, die ein Update sehen, aber keines installieren können, bieten jetzt an beiden Stellen dasselbe an.** In der Menüleiste erschien ein Knopf mit der Aufschrift „Öffnen", der den Finder öffnete; im Fenster stand für dieselbe Zeile überhaupt nichts.

## 0.3.75

**Mit Electron gebaute Apps werden jetzt erkannt, ohne dass sie vorher jemand eintragen muss.** Sehr viele Mac-Apps führen im Inneren eine kleine Datei mit sich, die angibt, wo ihre Updates liegen. Bisher kannte DuoUpdater nur diejenigen, für die jemand von Hand eine Regel geschrieben hatte — alle übrigen standen ohne Versionsnummer in Ihrer Liste, und es gab keine Möglichkeit, Ihnen einen neuen Build zu melden. Jetzt liest es diese Datei direkt, genauso wie es die von Sparkle schon immer gelesen hat. Eine solche App ist damit an dem Tag abgedeckt, an dem Sie sie installieren, und nicht erst an dem Tag, an dem jemand dazu kommt.

**Apps mit einer bereits vorhandenen handgeschriebenen Regel bleiben unberührt.** Der neue Leseweg sitzt hinter ihnen und kann deshalb nur eine Lücke füllen — niemals etwas übernehmen, das bereits funktioniert hat.

**Zwei Details entscheiden darüber, ob der angebotene Download der richtige ist, und beide wurden an echten Apps geklärt statt angenommen.** Manche Anbieter kennzeichnen einen Intel-Build als ihren „primären" Download, obwohl sie daneben auch einen für Apple Silicon veröffentlichen; andere geben dem Apple-Silicon-Build einen Dateinamen, der sich von dem für Intel nicht unterscheidet, sodass der Name nichts verrät. DuoUpdater wählt nach Architektur aus, und wo es nicht sicher sein kann, dass ein Download auf Ihrem Mac läuft, nennt es Ihnen die Version und bietet die Installation nicht an — statt Ihnen etwas zu geben, das sauber installiert und sich dann nicht öffnen lässt.

## 0.3.74

**Wer die Beta-, Release-Candidate- oder Nightly-Version einer App nutzt, wurde von DuoUpdater stillschweigend auf dem falschen Zweig beobachtet.** Welchen Zweig Sie nutzen, ermittelt es, indem es Ihren Build in der Veröffentlichungsliste des Herstellers wiederfindet — doch eine Vorabversion behält meist dieselbe öffentliche Versionsnummer wie die Stable-Version, aus der sie stammt, und genau das ist bei Vorabversionen das Normale. Also traf die Zuordnung den Stable-Eintrag. Der Fehler blieb unsichtbar: Auf dem Bildschirm wirkte nichts falsch, Sie erfuhren lediglich nie vom nächsten Build Ihres eigenen Zweigs, und die angezeigten Versionshinweise gehörten zur Stable-Linie. Gefunden wurde das, indem die echten Vorabversionen von Supacode und TypeWhisper installiert und beobachtet wurden; beide folgen nun dem Zweig, auf dem sie tatsächlich sind.

**Die Beta von CapCut zeigte eine Version an, die Sie gar nicht haben.** In der Zeile stand „9.3.4545 → 9.4.0-beta6", während CapCut selbst, der Finder und jedes andere Update-Werkzeug Ihre Kopie 9.4.0-beta5 nannten. Manche Apps tragen ihre echte Version in ein anderes Feld ein als die meisten, und DuoUpdater las auf Ihrer Seite des Pfeils die ordentlicher aussehende Angabe, auf der Herstellerseite dagegen die echte. Das angebotene Update war immer das richtige — falsch war nur die Beschriftung. Beide Hälften der Zeile stammen jetzt aus derselben Quelle, bei CapCut wie bei den sieben weiteren so gebauten Apps.

**MacWhisper, GitHub Copilot for Xcode, TypeWhisper und OpenUsage zeigen jetzt ihre Versionshinweise.** Bei allen vieren blieb das Hinweisfeld leer: Ihre Update-Feeds enthalten überhaupt keine Hinweise, was erst auffiel, als jede einzelne mit dem abgeglichen wurde, was der Hersteller tatsächlich veröffentlicht. DuoUpdater liest nun MacWhispers eigene Seite mit Versionshinweisen, die Changelog-Datei von Copilot und die Changelog-Website von TypeWhisper und verweist bei OpenUsage auf dessen Veröffentlichungsliste.

**Helium aktualisiert sich jetzt über seinen eigenen Update-Dienst, was den Beta-Kanal und deutlich kleinere Downloads bringt.** Bisher wurde es über die öffentliche Veröffentlichungsliste verfolgt, in der nur Stable-Builds erscheinen — wer Heliums Beta nutzte, bekam also die Stable-Version gereicht. Der eigene Dienst veröffentlicht außerdem Patches, sodass ein gewöhnliches Update rund 40 MB statt eines erneuten Downloads von 124 MB benötigt. Geprüft wurde das mit einem Stable- und einem Beta-Build, und der Download wird gegen den Signaturschlüssel in Ihrer installierten Kopie verifiziert, bevor irgendetwas ersetzt wird.

## 0.3.73

**Die Beta-, Developer- und Nightly-Ausgaben von Firefox und Thunderbird werden jetzt richtig verfolgt — bis heute hatte keine dieser fünf Ausgabelinien jemals ein einziges Update gemeldet.** Gelesen wurden sie aus Mozillas öffentlicher Versionsdatei, die nur die angezeigte Version veröffentlicht, und beim Installieren einer Beta fällt das „b5“ weg: Eine Firefox-Beta nennt sich den ganzen Zyklus über 155.0, gefragt wurde also einzig „ist 155.0b5 neuer als 155.0?“, und die Antwort lautet nein. Nightly war schlimmer — Mozilla liefert täglich einen Build aus, und jeder davon heißt 157.0a1, sodass ein Zyklus von vier Wochen exakt nichts ergab. DuoUpdater fragt jetzt Mozillas eigenen Update-Dienst, dieselbe Adresse, die auch Firefox' eingebauter Updater benutzt und die die App selbst in sich stehen hat, und vergleicht die Build-Kennung, die beide Seiten tragen. Vor der Auslieferung gegen die echten Downloads aller fünf Linien geprüft: Die Kennung, die dieser Dienst meldet, ist Byte für Byte dieselbe wie die in deiner Kopie; eine Beta, die einen Build zurückliegt, zeigt ihr Update jetzt an, und ein Nightly vom selben Tag, nur früher gebaut, ebenfalls. Die stabile Ausgabe und ESR waren nie betroffen und bleiben unangetastet. Eine Einschränkung, klar gesagt: Dieser Dienst veröffentlicht kein Erscheinungsdatum, für diese fünf Linien hält das Release-Log also weiterhin fest, wann DuoUpdater einen Build zuerst gesehen hat — nicht, wann Mozilla ihn ausgeliefert hat.

## 0.3.72

**Wispr Flow, AionUi und Devin lassen sich jetzt mit einem Klick aktualisieren, statt nur zu melden, dass es eine neue Version gibt.** Alle drei konnten ihre Updates bereits sehen, keines konnte eines anwenden — mit der Begründung, der Hersteller liefere getrennte Builds für Intel und Apple Silicon aus und DuoUpdater habe keine Möglichkeit zu wählen. Nachgemessen statt angenommen hielt dieser Grund nicht stand: Die Endpunkte, aus denen diese drei gelesen werden, sind ohnehin die für Apple Silicon, und DuoUpdater läuft ausschließlich auf Apple Silicon — es gab nie eine Wahl zu treffen. Jeder Download wurde vor dem Einbau geholt und geprüft: die richtige App darin, signiert vom selben Entwickler wie deine Kopie, von Apple notarisiert, und der von AionUi zusätzlich gegen die Prüfsumme, die sein eigenes Manifest veröffentlicht. Anschließend wurde jedes tatsächlich über eine ältere Kopie installiert und kam auf der neuen Version zurück.

**Grok Bot wird jetzt verfolgt — neue Versionen und Updates mit einem Klick.** Die Desktop-App von xAI wird von Anysphere gebaut und signiert, der Firma hinter Cursor, und sie aktualisiert sich über Cursors eigenen Release-Dienst. Genau deshalb antwortete keiner der üblichen Wege für sie: kein Sparkle-Feed, kein Eintrag im App Store, kein öffentliches Release-Repository, und ein Homebrew-Cask, das das Aktualisieren an die App selbst zurückgibt. DuoUpdater liest stattdessen den Versions-Endpunkt des Herstellers. Zwei weitere Endpunkte standen zur Wahl und wurden aus genannten Gründen beide nicht genommen: der hinter dem Download-Knopf auf x.ai veröffentlicht überhaupt keine Versionsnummer, und der, den der eigene Updater der App benutzt, antwortet mit leerem Rumpf, sobald man bereits aktuell ist — ein Schweigen, das von einem kaputten Endpunkt nicht zu unterscheiden wäre. Der Download wurde vor dem Einbau geprüft: Das Disk-Image enthält die richtige App, signiert vom selben Entwickler wie deine Kopie und von Apple notarisiert. Eines bietet es bewusst nicht: Versionshinweise. xAI veröffentlicht für diese App keine, und das einzige Changelog, das es gibt, gehört zu einem anderen Produkt.

**Comet und Msty Studio aktualisieren sich jetzt ebenfalls mit einem Klick, und eine neue Prüfung achtet auf Apps, die könnten und es nicht tun.** Das waren die letzten beiden aus der Gruppe, die Updates sehen, aber nicht anwenden konnten — und beide brauchten nichts Neues, nur einen genaueren Blick darauf, was der Hersteller tatsächlich liefert. Comets Download-Link ist signiert und verfällt nach einer Stunde, deshalb zeigt der Update-Knopf auf Perplexitys eigenes Gateway: Der Link entsteht in dem Moment, in dem du klickst, und nicht Stunden früher bei der Prüfung. Msty veröffentlicht vier Downloads in einer Datei, den Intel-Build zuerst, und der Fingerabdruck zur Verifikation stammte aus genau diesem ersten Eintrag; er hängt jetzt an dem Apple-Silicon-Build, der wirklich geholt wird — wodurch eine zwischen Prüfung und Klick veröffentlichte Version in aller Regel auffällt, statt stillschweigend installiert zu werden. Beide wurden vor der Auslieferung heruntergeladen und durch die echten Installationsprüfungen geschickt. Die eigentliche Lehre betrifft keine der beiden Apps: Nichts in DuoUpdater konnte bemerken, dass eine App aus einem längst überholten Grund nur beobachtet wurde. Der nächtliche Rezept-Durchlauf sagt es jetzt, wenn eine bloß beobachtete App uns längst ein Installationspaket hinhält.

**QQ音乐 wird jetzt erfasst — neue Versionen, One-Click-Updates und seine Versionshinweise im Fenster.** Keiner der üblichen Wege antwortete. Es liefert weder einen Sparkle-Feed noch überhaupt das Sparkle-Framework aus; sein Homebrew-Cask gibt das Aktualisieren an die App selbst zurück; und der Update-Endpunkt, den die eigene Programmdatei benennt, antwortet auf alles, was wir ihn fragen können, mit 200 und leerem Rumpf — ein Schweigen, das von einem kaputten Endpunkt nicht zu unterscheiden ist, weshalb er unangetastet blieb. Was DuoUpdater stattdessen liest, ist die Datendatei, aus der die Download-Seite des Anbieters selbst gespeist wird: Diese Seite liefert nur eine leere Hülle aus und füllt sich erst aus dieser Datei — die zugleich der einzige Ort ist, an dem die Versionshinweise überhaupt existieren; es gibt weder einen Blog noch einen Appcast noch eine Seite pro Version. Die Hinweise werden also direkt aus ihr gelesen und im eigenen Fenster von DuoUpdater angezeigt, und das Update installiert das notarisierte Disk-Image, das in derselben Antwort genannt wird. Eine Falle, die man festhalten sollte: Diese Datei führt neben dem aktuellen noch einen zweiten, sechs Jahre alten Mac-Eintrag, weshalb beide Regeln am versionierten Dateinamen des Mac-Downloads ansetzen und nicht am Wort „Mac". Und eine Einschränkung, die man klar benennen sollte: Die Datei veröffentlicht nur die angezeigte Version, nie die dahinterliegende Build-Nummer, sodass ein Neuschnitt einer Version, der den Namen behält, hier unsichtbar bleibt. Sie kann kein Update erfinden, das es nicht gibt — sie kann diese eine Art von Änderung schlicht nicht sehen.

**TimeMachineEditor wird jetzt erfasst — neue Versionen und Updates mit einem Klick.** Keiner der üblichen Wege antwortete für die App: kein Sparkle-Feed (sie trägt weder eine Feed-Adresse noch das Framework, das eine nutzen würde), kein Eintrag im App Store, kein öffentliches Release-Repository, und ein Homebrew-Cask, das das Aktualisieren an die App selbst zurückgibt. Was es gibt, ist eine kleine Anbieter-Seite, deren einziger Download-Link die Version im eigenen Text nennt — dieselbe Seite, auf demselben Weg gelesen, auf die sich auch Homebrews eigene Versionsprüfung stützt; das ist also die Fläche, die der Hersteller vorsieht, und keine Vermutung. Das Update installiert als das eigene Installationspaket des Herstellers statt als einfacher App-Austausch, und das ist Absicht: Der Download platziert außerdem einen Hintergrund-Scheduler, ein Kommandozeilen-Werkzeug und einen Launch Daemon außerhalb der App selbst, sodass ein bloßer Austausch der App eine neue Kopie neben einem veralteten Scheduler zurückließe, ohne dass etwas den Widerspruch bemerken würde.

**Little Snitch wird jetzt erfasst, sowohl auf seinem Stable- als auch auf seinem Nightly-Release.** Object Development veröffentlicht eine Versionsdatei, auf die auch der eigene Updater zurückfällt — dieselbe, die Homebrew liest —, und DuoUpdater liest sie direkt, weil Little Snitch keinen Sparkle-Feed ausliefert und sein Cask das Aktualisieren an die App zurückgibt. Die beiden Releases teilen sich eine Identität, was sie normalerweise ununterscheidbar machen würde; dieses Mal verrät sich der Unterschied im Versionsstring, wo eine Nightly-Build das Wort ausgeschrieben trägt („6.5 nightly (7301)"), während die Stable-Build als schlichtes „6.4.1" erscheint. Es lohnt sich zu sagen, was das bewusst nicht tut: Es installiert das Update nicht für dich. Little Snitch betreibt neben der App eine System Extension und einen privilegierten Hintergrunddienst, und ob ein bloßer Austausch der App diese unversehrt ließe, wurde nicht an einer echten Maschine geprüft — deshalb sagt dir DuoUpdater nur, dass die neue Version da ist, und schickt dich zum eigenen Download des Herstellers, statt bei einem Netzwerkfilter zu raten.

**Carbon Copy Cloner wird jetzt erfasst, und es wird nicht versuchen, dich zwischen Hauptversionen zu bewegen.** Bombich hält drei Generationen gleichzeitig am Leben — CCC 5, 6 und 7 —, die alle dieselbe Identität an das System melden, was „gibt es etwas Neueres?" zu einer trickreicheren Frage macht, als es aussieht: 7.1.6 sortiert sich tatsächlich über 6.1.13, aber der Wechsel zwischen ihnen ist ein separater Kauf, und CCC 7 braucht ein neueres macOS, als ein CCC-5-Rechner überhaupt laufen haben mag. Allein nach Versionsnummer zu antworten hätte jedem Besitzer von CCC 5 und 6 für immer gesagt, ein kostenloses Update warte — und das wäre jedes Mal falsch gewesen. DuoUpdater liest deshalb jede Generation von ihrer eigenen Adresse und bietet ausschließlich das nächste Release innerhalb der Generation an, die tatsächlich vorhanden ist. Beta-Releases werden ebenfalls erfasst, für alle, die das in CCC eingeschaltet haben. Vorerst nur Erkennung — CCC installiert neben der App einen privilegierten Helper, was ein größerer Anspruch ist als die One-Click-Updates, die es hier bereits gibt —, und der Grund, warum bisher nichts diese Updates sehen konnte, ist es wert, festgehalten zu werden: Der eigene Update-Feed der App beantwortet jede Anfrage erfolgreich, mit einer vollständig leeren Seite.

**Ein Update, das dein Mac tatsächlich nicht ausführen kann, wird jetzt nicht mehr heruntergeladen und installiert.** Jedes App-Bundle nennt das älteste macOS, unter dem es startet, und manche Hersteller nennen auch das neueste — „diese Build ist nicht für ein so neues Betriebssystem gedacht", so drückt eine App aus, die mit macOS 27 noch nicht mitgezogen ist. DuoUpdater las bisher keines von beidem. Für Apps, die über einen Sparkle-Feed verfolgt werden, wurde die untere Grenze bereits beachtet, aber das ist eine Minderheit: Von den Apps auf einem typischen Mac ist die größere Hälfte diejenige, die aus dem eigenen Endpunkt eines Herstellers oder aus GitHub-Releases gelesen wird, und ein GitHub-Release veröffentlicht nirgendwo eine macOS-Anforderung. Also konnte eine App, die bereits zu einem neueren macOS gewechselt war als dem eigenen, angeboten, vollständig heruntergeladen, eingebaut werden — und sich dann nicht öffnen. DuoUpdater liest die Anforderung jetzt direkt aus der heruntergeladenen App aus, gleich neben der bereits bestehenden Prüfung, dass sie für deinen Prozessor gebaut ist, und verweigert den Austausch, statt eine funktionierende Kopie durch eine zu ersetzen, die nicht startet. Auch die vom Hersteller genannte Obergrenze wird beachtet, wo ein Feed eine veröffentlicht, sodass eine Build, die der Entwickler als nicht-für-dein-macOS markiert hat, gar nicht erst angeboten wird. Drei Einschränkungen, die man klar benennen sollte. Die Anforderung erst aus dem Download auszulesen bedeutet, dass der Download bereits stattgefunden hat — das verhindert eine kaputte Installation, nicht den Datenverkehr, denn bei den meisten Apps gibt es keine frühere Stelle, an der man fragen könnte. Eine App, deren neueste Version dein Mac nicht ausführen kann, zeigt dieses Update weiterhin an und lehnt die Installation weiterhin ab; die Ablehnung nennt jetzt das gewünschte macOS, aber DuoUpdater merkt sich die Antwort noch nicht und hört deshalb noch nicht auf, das Update anzubieten. Und die Prüfung deckt die Wege ab, auf denen DuoUpdater das App-Bundle selbst austauscht — ein Installationspaket übergibt die Datei an macOS, das die eigenen Anforderungen des Pakets durchsetzt, und Homebrew und der App Store wählen ihre eigenen Builds; Apps, die aus dem iPhone- und iPad-Bereich des App Store stammen, werden bewusst übersprungen, da die dort genannte Version eine iOS-Version ist und ein Vergleich mit macOS schlimmer wäre als gar keine Prüfung. Eine Sache, die es noch nicht laut ausspricht: Wo ein Entwickler eine Build als nicht-für-dein-macOS markiert hat, wird dieses Update einfach nicht angeboten, und die App erscheint als aktuell, statt zu erklären, warum. Bei einem älteren Mac löst sich das von selbst, sobald du macOS aktualisierst. Bei einem zu neuen Mac tut es das nicht, und dort ist eine bessere Antwort als Schweigen fällig.

## 0.3.71

**百度网盘 wird jetzt erfasst — neue Versionen, One-Click-Updates und seine Versionshinweise direkt im Fenster.** Keiner der üblichen Wege stand offen. Es veröffentlicht keinen Sparkle-Feed; das Homebrew-Cask kann für eine von Hand installierte Kopie nicht sprechen; und das Update-Manifest, das im eigenen Bundle-Namen genannt wird, ist tot — diese Datei, ihr arm64-Pendant und das Verzeichnis, das beide enthält, antworten mit 404. DuoUpdater liest deshalb den Endpunkt, aus dem die Download-Seite des Anbieters selbst gespeist wird, und installiert das notarisierte arm64-Disk-Image, das in genau dieser Antwort genannt wird. Seine Versionshinweise kommen auf demselben Weg: Die Seite „版本更新" hat einen Tab „Mac版", liefert aber selbst nichts aus und wird erst von einer API gefüllt — also liest DuoUpdater diese API direkt aus und zeigt die letzten vierzig Versionen im eigenen Fenster an. Eine Einschränkung, die man klar benennen sollte: Dieser Feed veröffentlicht nur die angezeigte Version, nie die dahinterliegende Build-Nummer, sodass ein Neuschnitt einer Version, der den Namen behält, hier unsichtbar bleibt. Es kann kein Update erfinden, das es nicht gibt — es kann diese eine Art von Änderung schlicht nicht sehen.

**Eine fehlgeschlagene Prüfung sagt jetzt, dass sie fehlgeschlagen ist, statt einen leeren Strich zu zeigen.** Eine App, deren Version nur der eigene Endpunkt des Anbieters beantworten kann, hatte bisher genau eine Art, „keine Antwort" auszudrücken: einen leeren Strich — genau das Bild, das auch eine App zeigt, die überhaupt nicht abgedeckt ist. Ein Anbieter-Endpunkt, der einen Timeout wirft oder umzieht, las sich also exakt wie „DuoUpdater verfolgt diese App nicht" — ein dauerhaft wirkendes Urteil, ohne angebotene Schaltfläche „Erneut versuchen" und ohne Eintrag im Zähler „Apps konnten nicht geprüft werden". Aus solchen Fällen wird jetzt eine fehlgeschlagene Prüfung, was sie tatsächlich sind: eine Schaltfläche „Erneut versuchen", ein Platz im Zähler und ein genannter Grund. Bewusst nicht alles: Ein Zustand, gegen den dein Mac schlicht nichts tun kann — kein Recipe für den Release-Track, auf dem du bist, ein Endpunkt, der eine Identität braucht, die diese Maschine nicht hat — bleibt der stille Strich, der es immer war, und eine Prüfung, die drei Runden in Folge fehlgeschlagen ist, tritt weiterhin aus dem Banner heraus, statt es für immer festzunageln. Eine App, die von JetBrains Toolbox installiert wird, behält außerdem ihre Schaltfläche „Toolbox öffnen", wenn das Auslesen der dahinterliegenden Version fehlschlägt — Toolbox zu öffnen war ohnehin in jedem Fall die richtige Antwort.

**Die Erinnerung „zum Anwenden neu starten" verschwindet jetzt, sobald du neu gestartet hast.** Wenn der eigene Updater einer App im Hintergrund eine neue Build heruntergeladen hatte, sagte DuoUpdater dir das und bot auf der Benachrichtigung eine Schaltfläche „Neu starten" an. Sie zu klicken funktionierte — die App kam mit der neuen Build zurück und meldete „Es läuft jetzt 1.0." —, aber die Erinnerung, die sie hätte ersetzen sollen, blieb darunter im Mitteilungszentrum liegen und forderte weiterhin dazu auf, etwas neu zu starten, das man gerade neu gestartet hatte. DuoUpdater prüfte, ob die Erinnerung *aufgehört* hatte, relevant zu sein, und ausgerechnet der Moment der Prüfung war genau der, der bereits erledigt war. Die Erinnerung wird jetzt immer entfernt, sobald kein Neustart mehr aussteht — das räumt auch eine Erinnerung weg, die eine App hinterlassen hat, die ihr eigenes Update angewendet hat, während DuoUpdater gar nicht lief.

## 0.3.70

**Apps, die viele Builds unter derselben Versionsnummer ausliefern, werden jetzt überall korrekt behandelt.** Eine Mac-App trägt zwei Versionsangaben: die, die sie dir zeigt („1.0") und eine Build-Nummer, die tatsächlich hochzählt. Die meisten Apps bewegen beide. Manche bewegen nur die Build-Nummer: Amp hat an einem einzigen Tag zehn Builds veröffentlicht, jede davon „1.0" genannt; Surge hat vier getrennte Versionen als 6.9.0 herausgebracht; die Preview-Builds von JetBrains machen es genauso. DuoUpdater entschied die Frage „hat sich das geändert?" an rund einem Dutzend Stellen durch den Vergleich der *angezeigten* Version — und für diese Apps kann dieser Vergleich immer nur „nein" antworten, oder, wo gefragt wurde „sind das dieselben?", immer nur „ja", ganz gleich, was tatsächlich passiert war. Was folgt, ist das, was dieser eine Fehler kaputtgemacht hat — gefunden, weil Amp ihn sichtbar gemacht hat.

**Neu starten wirkt nicht mehr, als würde es drei Minuten lang hängen, um dann einen Fehler zu melden, der gar nicht aufgetreten ist.** Ein Klick auf „Neu starten" bei einer App, deren eigener Updater bereits eine Build bereithielt, ließ die Anzeige drei Minuten lang drehen und meldete dann einen Fehlschlag — während das Update in Wirklichkeit innerhalb von ein, zwei Sekunden angewendet worden war und die App bereits mit der neuen Build wieder geöffnet hatte. DuoUpdater wartete auf eine Änderung der angezeigten Version, die niemals kommen würde. Gemessen an Amp: 189 Sekunden Ladeanzeige für einen Austausch, der unter einer Sekunde dauerte. Das Ganze ist jetzt in deutlich unter einer Sekunde erledigt, und ein echter Fehlschlag wird weiterhin als solcher gemeldet.

**Neu starten bietet dir nicht mehr eine Build an, die bereits veraltet ist.** Hatte der Updater einer App eine Build heruntergeladen und der Entwickler inzwischen eine weitere veröffentlicht, bot DuoUpdater trotzdem „Neu starten" an — man startete neu und war sofort eine Build im Rückstand, genau das, was diese Prüfung eigentlich verhindern sollte. Solche Zeilen bieten jetzt stattdessen **Aktualisieren** an, was die aktuelle Build lädt.

**Eine Version zu überspringen verstummt eine App nicht mehr für immer.** Das ist der Punkt, den man kennen sollte. „Diese Version überspringen" soll eine Veröffentlichung ablehnen und die nächste durchlassen. Gespeichert wurde aber nur die angezeigte Version — bei einer App, die über mehrere Builds hinweg denselben Versionsnamen behält, lehnte ein einziges Überspringen also *jede* künftige Veröffentlichung ab: dauerhaft, Neustarts überdauernd, ohne dass irgendetwas auf dem Bildschirm anzeigte, dass die App verstummt war. Ein Überspringen speichert jetzt die Build, die es abgelehnt hat. Eine Folge dieser Reparatur: Eine Version, die von einem älteren DuoUpdater übersprungen wurde, wird dir noch einmal angeboten, weil der alte Datensatz nicht sagen kann, welche Build gemeint war. Sie erneut zu überspringen speichert es dann korrekt.

**„Wiederherstellen“ taucht wieder auf bei Updates, die aussahen, als hätten sie nichts verändert.** Die Werkbank blendet ein Wiederherstellen aus, das ohnehin nichts bewirken würde — und bei diesen Apps sah jedes Wiederherstellen so aus, als würde es nichts bewirken. Die Zeile verschwand also nach einer echten Aktualisierung, während ein vollständiges Backup auf der Festplatte lag, ohne dass ein Weg dorthin bestand.

**Die Erinnerung zum Neu-Starten meldet sich wieder einmal pro Build und übersteht es, wenn der Entwickler eine weitere veröffentlicht.** 0.3.69 hatte eine Erinnerung, die sich alle fünf Minuten wiederholte, durch eine ersetzt, die sich einmal pro bereitliegender Build meldet — identifizierte die Build aber anhand ihres Namens, sodass sie bei diesen Apps die erste ankündigte und danach für jede weitere stumm blieb. Das ist behoben. Getrennt davon: Weigert sich eine App zu beenden, weil sie ungespeicherte Arbeit hat, merkt sich DuoUpdater, sie wieder zu öffnen, sobald man geantwortet hat. Diese Notiz wurde bisher verworfen, wenn der Entwickler während der Entscheidung eine weitere Build veröffentlichte — die App blieb nach dem Austausch geschlossen, ohne dass etwas sie wieder öffnete. Jetzt folgt die Notiz stattdessen der neuen Build.

**Ein fehlgeschlagenes App-Store-Update wird nicht mehr stillschweigend als Erfolg verbucht**, und ein bereits heruntergeladenes Installationspaket gilt nicht mehr als das aktuell angebotene, wenn es sich um eine ältere Build handelt. Beides ging auf denselben Vergleich zurück. Auch die Versionshistorie zählte zehn Builds einer App als eine einzige Veröffentlichung; ab jetzt zählt sie sie einzeln, auch wenn bereits aufgezeichnete Historie sich nicht mehr nachträglich korrigieren lässt.

**Die Zeile selbst zeigt jetzt, welche Build ein Neustart anwenden wird.** Sie las sich als „1.0 → 1.0" — eine Zeile, die überhaupt keinen Unterschied benennt. Jetzt liest sie sich als „1.0 (129) → 1.0 (130)", und zwar nur dann, wenn sich tatsächlich die Build geändert hat; unterscheiden sich die Versionsnamen bereits, bleibt die Zeile unverändert. Dieselbe Korrektur erreicht auch die Tooltips von „Neu starten", die Benachrichtigung, die Notiz, die erklärt, warum eine Installation aufgeschoben wurde, und die Ablehnungsmeldung von `duo install`.

## 0.3.69

**微信输入法 und 豆包输入法 lassen sich wieder mit einem Klick aktualisieren — und zwar so, wie sie sich selbst aktualisieren.** One-Click für Eingabemethoden wurde in 0.3.25 noch am Tag der Veröffentlichung wieder zurückgezogen, nachdem bei jemandem die Einstellungen der Eingabemethode verschwunden waren. Das Problem lag in der Art der Installation: Sie ersetzte die gesamte App, so wie es ein Erstinstallationsprogramm tut. Eine Eingabemethode wird bei macOS über den *Ort* ihrer App registriert, und beide Apps aktualisieren sich selbst, ohne diesen Ort anzufassen — sie behalten die App und tauschen nur aus, was in ihr steckt. DuoUpdater macht jetzt dasselbe, sodass die registrierte App ein Update als dieselbe App durchläuft und ein Fehlschlag an jeder Stelle die laufende Kopie genau dort belässt, wo sie war. Auch verlangt keine der beiden Apps mehr nach deinem Passwort.

**Bevor eine der beiden aktualisiert wird, wird zuerst alles gesichert, was sie gelernt haben.** Dein Wörterbuch, deine Einstellungen und dein Kontostatus leben nicht innerhalb der App, sodass die Backup-Kopie, die DuoUpdater ohnehin schon anlegte, für sie nichts aussagen konnte. Sie werden jetzt vor jedem Update einer Eingabemethode als Schnappschuss gesichert und beim Wiederherstellen mit zurückgespielt, und die Kopie kostet fast nichts — 578 MB bei einer der beiden brauchen ein Zehntel einer Sekunde und praktisch keinen Speicherplatz. Das ist ein Sicherheitsnetz, keine Reparatur: Was eine App beim nächsten Start mit deinen Daten macht, ist ihr eigener Code, der da läuft, und dem kann sich nichts in den Weg stellen. Jetzt gibt es aber etwas, zu dem man zurückkehren kann.

**Ein Update nimmt einer App nicht mehr stillschweigend die Fähigkeit, sich selbst zu aktualisieren.** Wenn DuoUpdater einen Administrator brauchte, um eine App zu ersetzen, stellte es zwar wieder her, wem die App gehörte, aber nicht die Berechtigungen, mit denen die App installiert worden war — und ein Download entpackt sich normalerweise mit engeren Berechtigungen, als ein Installationsprogramm setzt. Bei beiden Eingabemethoden macht das den Unterschied aus, ob ihr eigener Updater sein nächstes Update abschließen kann oder nicht einmal hinter sich selbst aufräumen kann. Die Berechtigungen, mit denen eine App installiert wurde, werden jetzt auf die Kopie übertragen, die sie ersetzt.

**DuoUpdater fragt nicht mehr alle fünf Minuten, ob du dieselbe App neu starten willst.** Eine App, die sich selbst aktualisiert, legt die neue Version auf der Festplatte ab und wartet darauf, dass du sie beendest. DuoUpdater bemerkte das bei jeder Prüfung und postete jedes Mal aufs Neue eine Erinnerung, sodass eine Build, die du noch nicht neu starten wolltest, so lange weiter nervte, wie du sie liegen ließest. Jede Build wird jetzt einmal angekündigt: Startest du sie neu, überspringst du sie oder ignorierst du die App, bleibt es still — und die nächste Build kündigt sich wieder ganz normal an.

**Eine App, die auf einen Neustart wartet, zählt als Update, und eine ignorierte zählt jetzt überhaupt nicht mehr.** Eine neue Version, die schon auf der Festplatte liegt, *ist* ein Update — sie ist nur früh heruntergeladen worden —, deshalb fließt sie jetzt ins Badge und in die Zeile „N Updates verfügbar" ein, statt in einen separaten Zähler daneben. Bei ignorierten Apps war es umgekehrt: Eine ignorierte App konnte das Badge aufleuchten lassen, während ihre Zeile nur ein blasses „Ignoriert"-Label ohne Schaltfläche zeigte — die Zahl zeigte also auf etwas, worauf man gar nicht reagieren konnte. Ignorieren und Überspringen bedeuten jetzt an allen drei Stellen dasselbe — in der Erinnerung, im Badge und in der Liste.

**搜狗输入法 wird jetzt erfasst — indem der eigene Updater befragt wird, statt die Website auszulesen.** Sogous Changelog-Seite veröffentlicht dreiteilige Versionsnummern, während die installierte Kopie vierteilige trägt, sodass ein Vergleich damit bedeutet hätte, die echte Version erst zu kürzen. Stattdessen antwortet die eigene Update-Prüfung in der Nummerierung des Bundles selbst. DuoUpdater fragt diesen Endpunkt so ab, wie es die App selbst tut, und liest die Veröffentlichung daraus aus, alle vier Segmente, sodass auch ein Neuschnitt, der nur das letzte Segment ändert, sichtbar wird. Das betrifft nur die Erkennung: Sogous Updater tut sehr viel mehr, als nur die App auszutauschen — er registriert einen QuickLook-Generator neu, verschiebt deine Daten an einen neuen Ort und beendet die Eingabemethode zwangsweise —, deshalb bleibt das Update selbst ihm überlassen. Eine Sache, die man wissen sollte, und die auf Sogous Konto geht, nicht auf unseres: Ihr Endpunkt antwortet abhängig von der macOS-Version, die anfragt, und ein Mac unter macOS 28, der für sich selbst anfragt, bekommt eine Build aus dem Jahr 2023 vorgesetzt. DuoUpdater fragt nicht für sich selbst an, sodass dir auf jedem Mac die aktuelle Version angezeigt wird.

**Versionshinweise, die eine spätere Korrektur richtig gelesen hätte, werden jetzt erneut gelesen, statt für immer falsch zu bleiben.** Versionshinweise werden pro Version zwischengespeichert, mit der Überlegung, dass sich die Hinweise einer veröffentlichten Version nie mehr ändern. Das stimmt für die Hinweise selbst, aber nicht für das, was DuoUpdater aus ihnen herausliest — eine App, deren Hinweise verstümmelt herauskamen, blieb für diese Version also so, egal wie viele Parsing-Korrekturen danach noch erschienen. Jeder zwischengespeicherte Eintrag verzeichnet jetzt, welche Generation der Extraktionslogik ihn geschrieben hat, und ein Eintrag einer älteren Generation wird erneut abgerufen. Das gilt für beide Caches — App-Changelogs und Homebrew-Formel-Hinweise.

**Eine Prüfung, die an einem fremden CDN scheitert, wird ein zweites Mal versucht, bevor sie als kaputt gilt.** 502, 503 und 504 bedeuten alle, dass ein Vermittler den dahinterliegenden Server nicht erreichen konnte: Die Anfrage kam nie an, weshalb dieselbe Anfrage einen Moment später in der Regel funktioniert. Genau daran scheiterte die Prüfung von Headlamp — GitHub antwortete mit 504, ganz ohne Rate-Limit-Header. Diese drei bekommen jetzt einen erneuten Versuch, nach acht Zehntelsekunden. Bewusst nur diese drei: Ein 500er ist ein Fehler des Servers selbst, und ihn zu wiederholen reproduziert ihn meist nur; und ein Rate-Limit erneut zu versuchen verbraucht genau das Kontingent, über das es sich beschwert.

**Die Versionshinweise einer Homebrew-Formel folgen jetzt der Version, zu der sie gehören.** Hinweise wurden pro Formel gespeichert und nie pro Version, sodass, sobald die Hinweise einer Formel einmal geladen waren, jeder spätere Blick darauf für den Rest der Sitzung die Hinweise dieser ersten Version bekam — auch nachdem eine neuere Version erschienen war. Sie zu lesen belegte außerdem die Warteschlange, auf die der Rest der Formelliste wartete, sodass ein langsames `brew info` die Liste drumherum ausbremsen konnte.

**Rechtsklick auf eine App in der Seitenleiste der Werkbank öffnet sie.** Dasselbe „Öffnen", das die Zeilen im Menüleisten-Menü schon immer angeboten haben, jetzt auch in dem Fenster, in dem man es eher braucht.

**Einen heruntergeladenen Installer zu verwerfen nimmt jetzt auch den Fehler mit, den er hinterlassen hat.** Ein bereitliegendes Paket wegzuwerfen setzte die Zeile schon bisher auf „Aktualisieren" zurück, aber ein roter Fehlschlag aus dem gerade abgebrochenen Versuch blieb darauf sitzen — und nichts hätte ihn je entfernt, denn ein Fehler wird erst zurückgezogen, sobald eine Zeile auf dem neuesten Stand ist, was eine Zeile, die noch ein Update anbietet, nie tut. Jetzt verschwindet er zusammen mit dem Download.

**`duo`, der optionale Begleiter für die Kommandozeile, beschreibt sich selbst nicht mehr falsch.** `--timeout` ist verschwunden — es wurde entgegengenommen, dokumentiert und von nichts gelesen. `--budget` ist erstmals dokumentiert, und die eine Meldung, die es erwähnt, nennt jetzt eine echte Zahl von Minuten statt immer „15". `--max-calls` behauptete einen Standardwert von 20, tatsächlich sind es 6. Ein Zahlen-Flag, dem etwas übergeben wird, das keine Zahl ist, ist jetzt ein Fehler: `duo verify --max-concurrency 1x` wurde bisher ignoriert und mit dem Standardwert vier weitergefahren — genau die falsche Richtung für jemanden, der einen Durchlauf bewusst verlangsamen will. Und `duo verify` zählt die Anfragen, die es wirklich gestellt hat: Ein Feed, der mit 502 antwortet und beim erneuten Versuch klappt, meldet nicht mehr fälschlich ein sauberes `ok`, obwohl er heimlich zweimal gefragt hat. `-h` funktioniert jetzt überall dort, wo auch `--help` funktioniert.

## 0.3.68

**Vier weitere Apps werden erfasst, zwei weitere Release-Channels kommen dazu, und jede von ihnen aktualisiert sich an Ort und Stelle.** CapCut, Canva und WorkBuddy auf beiden Seiten, von denen es ausgeliefert wird — die internationale und die chinesische Seite betreiben unabhängige Release-Züge, sodass eine Installation immer nur die Version und die Hinweise der eigenen Seite angeboten bekommt. Termius und VSCodium bekommen ihre Beta- und Insiders-Channels neben den bereits abgedeckten Stable-Builds. CapCuts zweiter Track ist der unangenehme Fall: Er wird über einen Schalter innerhalb von CapCut selbst gewählt und taucht in der Versionsnummer nirgends auf, sodass der Track, auf dem man sich befindet, von der Kopie auf der Festplatte abgelesen wird, statt aus der gerade laufenden Build geraten zu werden. Wissenswert, falls dieses Häkchen gesetzt ist — DuoUpdater bietet dir die neueste Beta an, sobald sie existiert, was dem Punkt voraus sein kann, den CapCuts eigenes gestaffeltes Rollout für dich erreicht hätte.

**Android Studios Preview-Channels bieten dir keine Build mehr an, die älter ist als die, die du schon hast.** Google listet seine Veröffentlichungen in der Reihenfolge ihres Erscheinens statt nach Version, und DuoUpdater durchsuchte diese Liste drei getrennte Male — einmal für die Version, einmal für das Datum, einmal für den Download —, sodass ein Release Candidate, der nach einem neueren Canary veröffentlicht wurde, die Version aus einem Eintrag und die Datei aus einem anderen liefern konnte. Am 26. August antwortete der Canary-Channel mit 2026.1.4 RC 2, während 2026.2.1 Canary 2 bereits erschienen war. Jede dieser Antworten muss jetzt aus demselben Eintrag stammen.

**Ein neu geschnittenes Release installiert nicht mehr die Kopie, die es eigentlich ersetzen sollte.** Baut ein Projekt ein Release neu, ohne die Version zu ändern, liegen beide Dateien unter demselben Tag — KeePassXC liefert `KeePassXC-2.7.11-1-arm64.dmg` neben dem ursprünglichen `KeePassXC-2.7.11-arm64.dmg` aus —, und DuoUpdater nahm einfach, was zuerst gelistet war. Diese Reihenfolge ist alphabetisch, was diesmal zufällig den Neuschnitt nach vorn stellte und ihn beim nächsten Mal nach hinten gestellt hätte. Nichts daran war sichtbar: Die Version in der Zeile stimmte, nur die Datei dahinter war falsch. Die richtige Datei wird jetzt danach gewählt, was der Dateiname selbst aussagt.

**Nightly- und Snapshot-Builds werden jetzt als das erkannt, was sie sind: Vorabversionen.** VLCs Nightly, der KeePassXC-Snapshot und Freelens Nightly installieren sich alle unter dem Namen und der Bundle-ID der Stable-App, sodass allein ihre Versionskennung sie verrät — und genau die wurde nicht ausgelesen. Sie zählten als Stable-Installationen, und so kommt es, dass einer Nightly-Version am Ende ein gewöhnliches Release zum Überschreiben angeboten wird. Die Nightly von DB Browser for SQLite wird aus demselben Grund aus dem eigenen Dateinamen der App gelesen, und dort war das Problem zwar vorhanden, aber verborgen: Ihre eingefrorene Version sortiert sich nur so lange über der aktuellen Stable-Version, bis Stable aufholt — ab dem Punkt hätte jede Nightly-Installation still eine Stable-Build vorgesetzt bekommen.

**Ein Neustart meldet jetzt, was wirklich passiert ist.** Ein paar Apps liefern eine zweite App innerhalb des eigenen Bundles mit — Surge hält dort sein Dashboard —, und wenn nur diese innere App neu gestartet werden musste, meldete DuoUpdater, die äußere App sei neu gestartet worden, obwohl nichts dergleichen geschehen war. Jetzt meldet es das tatsächliche Ergebnis der inneren App und löst keine Neustart-Benachrichtigung mehr für einen Neustart aus, der nie stattgefunden hat.

**Ein bereits heruntergeladenes Update sagt das jetzt auch, und man kann es rückgängig machen.** Eine Zeile mit einem fertig heruntergeladenen Installationspaket zeigte nur eine blaue Schaltfläche „Installieren" und sonst nichts — nichts sagte, dass der Download bereits abgeschlossen war oder dass die Schaltfläche den Installer nur wieder öffnet, statt das Update von vorn zu starten, und es gab keine Möglichkeit, es sich anders zu überlegen. Das steht jetzt auf der Zeile selbst statt versteckt in einem Tooltip, und **Geladenes Installationsprogramm verwerfen** im Rechtsklickmenü der Zeile wirft den Download weg und setzt die Zeile zurück auf „Aktualisieren". Einen Download zu verwerfen, während bereits eine Installation derselben App lief, konnte dabei auch ein Paket wegwerfen, das gerade erst fertig heruntergeladen worden war; das kann jetzt nicht mehr passieren.

**Schaltflächen für Versionshinweise, die auf stillgelegte Seiten zeigten, öffnen jetzt wieder lebende Seiten.** Microsoft hat die Enterprise-Versionshinweisseiten von Edge umbenannt, und Termius hat sein Changelog verschoben, sodass drei Edge-Channels und Termius auf eine Seite schickten, die es nicht mehr gab. Niemand hatte je geprüft, ob diese Seiten noch existierten, weshalb sie unbegrenzt verrotten konnten, während alles andere gesund aussah — sie werden jetzt regelmäßig geprüft. Der Dev-Channel von Edge bekommt gar keine Schaltfläche: Microsoft veröffentlicht dafür keine Hinweise mehr, und jede noch existierende Seite gehört zu einem anderen Release-Zug.

**Raycast wird auf Version 2 mitverfolgt — aber nur auf den Macs, die sie ausführen können.** Raycast 2 braucht macOS Tahoe und Apple Silicon. Ein Mac, der das nicht erfüllt, bleibt auf dem Version-1-Zug und wird nicht mehr über ein Release informiert, das er ohnehin nie hätte installieren können. Die beiden Züge behalten jetzt außerdem ihre eigenen Versionshinweise, sodass eine Version-1-Installation die Historie von Version 1 liest statt die von Version 2.

**Versionshinweise werden jetzt für weitere Apps im eigenen Fenster von DuoUpdater dargestellt.** BetterDisplay auf allen drei seiner Tracks, Shotbase, und WorkBuddy auf beiden Seiten. Die Hinweise von BetterDisplay enden nicht mehr im rohen Markup seiner Download-Schaltfläche und wiederholen auch nicht mehr bei jedem einzelnen Release die komplette Mitwirkendenliste.

**`duo`, der optionale Begleiter für die Kommandozeile, verweigert jetzt eine Kommandozeile, die es nicht versteht.** Ein falsch getipptes Flag wurde bisher ignoriert, und ein ignoriertes Flag liest sich wie eines, das man nie übergeben hat — sodass `duo verify --githubb` stillschweigend jedes Recipe prüfte, statt nur das gewünschte. Unbekannte Flags, ein Flag ohne seinen Wert und überzählige Argumente sind jetzt Fehler, die benennen, was der Befehl tatsächlich akzeptiert, und `--help` funktioniert jetzt nach jedem Befehl.

## 0.3.67

**Die Vorab- und internen Builds von BetterDisplay werden jetzt den Leuten angeboten, die sie sich gewünscht haben.** BetterDisplay führt alle drei seiner Release-Züge in einem einzigen Update-Feed und unterscheidet zwischen ihnen über zwei Schalter in den eigenen Einstellungen. DuoUpdater konnte deinen Zug bisher nur aus der gerade laufenden Build erraten, was nichts über einen Schalter aussagt, den man zwar eingeschaltet, aber noch nicht genutzt hat — sodass jemand mit beiden Schaltern an, aber noch auf einer Stable-Build, zu hören bekam, alles sei aktuell, während BetterDisplays eigener Updater bereits eine um vier Releases weiter fortgeschrittene Version anbot. Die tatsächliche Wahl wird jetzt direkt aus BetterDisplay ausgelesen, auch für den Fall, dass das Einschalten der internen Builds die gewöhnlichen Vorabversionen weiterhin mitliefert. Die nur für Apple Silicon gedachten Preview-Builds sind bewusst ausgenommen, sodass ein Intel-Mac nie eine angeboten bekommt, die er nicht ausführen kann.

**Den Release-Channel einer App zweimal hintereinander umzuschalten lässt die Zeile nicht mehr auf der alten Antwort hängen.** Ein Umlegen des Channel-Schalters veranlasst DuoUpdater, die App erneut zu prüfen, und ein zweites Umlegen, während die erste Prüfung noch lief, wurde bisher verworfen — die Zeile bot weiterhin eine Beta an, obwohl gerade Betas abgeschaltet worden waren, bis irgendetwas Unabhängiges eine weitere Prüfung auslöste. Das neuere Umlegen übernimmt jetzt gegenüber dem älteren, und eine Prüfung, die mittendrin unterbrochen wird, markiert die Apps, die sie nie erreicht hat, für den nächsten Durchlauf, statt sie als erledigt zu verzeichnen.

## 0.3.66

**Eine App, die in einer anderen App wohnt, wird jetzt zusammen mit ihr geschlossen und wieder geöffnet.** Ein paar Apps liefern eine zweite, vollständige App innerhalb des eigenen Bundles mit — Surge hält dort sein Dashboard. macOS behandelt das als eigenständige App, sodass beim Aktualisieren der umgebenden App nichts sie schloss: Sie lief einfach mit der Version weiter, die gerade ersetzt worden war, aus einer Kopie heraus, die nicht mehr dort lag, wo sie es vermutete, und konnte von dort aus nicht mehr mit der App sprechen, zu der sie gehört. Solche Apps werden jetzt zusammen mit der App geschlossen, in der sie wohnen, und danach wieder geöffnet, und genau das Fenster, in dem tatsächlich gearbeitet wurde, kommt wieder in den Vordergrund. Das gilt nur für Apps, die man selbst hätte öffnen können; die unsichtbaren Hilfsprozesse, die eine App für sich selbst betreibt, bleiben unangetastet.

**Wiederherstellungspunkte, die still und heimlich nicht mehr angelegt wurden, werden jetzt wieder angelegt.** Eine App kann eine ihrer eigenen Dateien so sperren, dass nichts sie löschen kann, und die für die Wiederherstellung aufbewahrte Kopie erbte diese Sperre — sie konnte also nie von der nächsten ersetzt werden, und ein einziger unterbrochener Versuch konnte eine Kopie hinterlassen, die nichts mehr entfernen konnte. Danach lief jedes Update dieser App ohne Weg zurück, und das mit einer einzigen Zeile, die keinen Grund nannte. Eine App auf dem eigenen Mac des Entwicklers steckte zwei Tage lang in genau diesem Zustand. Für die Wiederherstellung aufbewahrte Kopien tragen die Sperre jetzt nicht mehr, die installierte App bleibt exakt so, wie ihr Entwickler sie eingerichtet hat, und eine feststeckende Kopie kann die für sie vorgesehene Ersatzkopie nicht mehr blockieren.

**Ein Update, das funktioniert hat, meldet nicht mehr, dass es fehlgeschlagen sei.** macOS kann eine neue Version einrichten und dann beim Entfernen der verdrängten Version scheitern. Das wurde bisher für bare Münze genommen: DuoUpdater behauptete, eine App sei nicht aktualisiert worden, obwohl sie es war, oder es gebe keinen Wiederherstellungspunkt, während ein vollständiger im Backup-Speicher lag — und im ersten Fall wurde man aufgefordert, eine Berechtigung zu erteilen, die man bereits erteilt hatte und die ohnehin nicht hätte helfen können. Was tatsächlich auf der Festplatte passiert ist, wird jetzt geprüft, bevor irgendetwas gemeldet wird.

**Versionszeilen wiederholen nicht mehr Ziffern, die die Versionsnummer bereits enthält.** Wartet eine App auf einen Neustart, zeigt die Zeile die laufende Version und die, auf der man nach dem Neustart landet. Beide Seiten trugen bisher eine Build-Nummer — das macht den Unterschied lesbar, wenn ein Entwickler mehrere Builds unter demselben Versionsnamen ausliefert, und ist reines Rauschen, wenn sich die Versionen ohnehin schon unterscheiden. Chromes Zeile verbrauchte ihre gesamte Breite mit „151.0.7922.174 (7922.17…" und war zu Ende, bevor die tatsächlich geänderten Ziffern kamen. Die Build-Nummern erscheinen jetzt nur noch dann, wenn sie das sind, was sich geändert hat.

## 0.3.65

**Ein einziges Wort jetzt dafür, eine App zum Abschluss ihres Updates neu zu öffnen.** Eine App, deren eigener Updater die neue Version bereits eingerichtet hatte, bat um „Restart"; eine, die die neue Version erst beiseitelegt, bis man sie beendet, bat um „Relaunch". Dieser Unterschied war hinter den Kulissen real, machte für dich aber keinen: derselbe Klick in beiden Fällen, und dasselbe Ergebnis, wenn man nie klickt, weil das Update ohnehin beim nächsten Beenden der App landet. Es heißt jetzt überall „Neu starten" — in den Benachrichtigungen, den Tooltips und in der Einstellung, die es automatisch erledigt —, also genau das Wort, das auch Chrome, Claude und die meisten Apps, die sich selbst aktualisieren, dir vor die Nase setzen. Nur Englisch kannte für diesen Vorgang je zwei Wörter; Deutsch, Japanisch, Russisch und Chinesisch haben dafür schon immer nur eines verwendet, und Spanisch und Französisch wechseln jetzt auf die Formulierung, die sie andernorts ohnehin schon benutzt haben.

## 0.3.64

**Ein Update, das einen Administrator braucht, fragt jetzt danach, statt zu scheitern und eine Berechtigung zu beschuldigen.** Ob ein Austausch ein Passwort brauchte, wurde bisher danach entschieden, in welchem Ordner eine App liegt, statt nach der App selbst. Alles, was macOS als root installiert hatte — jede App-Store-App und jede App, die ein Installationspaket abgelegt hat —, bestand diesen Test, nahm den Weg ohne Passwort und konnte damit gar nicht fertig werden: Das Entfernen der alten Version braucht Schreibzugriff auf die Verzeichnisse in ihrem Inneren. macOS meldet diese Verweigerung mit demselben Code, den es auch für eine verweigerte Berechtigung zur App-Verwaltung verwendet, sodass der Fehlschlag als Aufforderung ankam, die App-Verwaltung zu gewähren — was nie hätte helfen können, weil das eigentliche Hindernis die Dateibesitzverhältnisse waren. Auf einem gewöhnlichen Mac betraf das jede App-Store-App und eine Handvoll weitere, und es traf Administratorkonten — Standardkonten wurden bereits korrekt weitergeleitet. Solche Updates nehmen jetzt den Weg, der funktioniert, und die App behält den Besitzer, den sie hatte, statt still dir zu gehören.

**App-Store-Apps bekommen jetzt genau wie alle anderen einen Wiederherstellungspunkt.** Backups übergingen sie bisher, mit der Überlegung, der Store könne eine frühere Build ohnehin jederzeit wieder besorgen. Das kann er nicht — der App Store bietet nur die jeweils aktuelle Version einer App an —, wodurch der Store als der eine Weg übrig blieb, auf dem ein Update angewendet wurde, ohne es rückgängig machen zu können. Diese Apps werden jetzt vor einem Update genauso gesichert wie jede andere, und weil die Kopie per Klonen entsteht, kostet sie fast keinen Speicherplatz, solange das Update das Original nicht tatsächlich ersetzt hat. Eine solche Kopie zurückzuspielen sagt das, was für den Store spezifisch ist: Das Update taucht sofort wieder in der Updates-Liste auf und wird von selbst erneut angewendet, wenn automatische App-Updates eingeschaltet sind. Und wenn ein Store-Update ohnehin nie angewendet worden wäre — eine iPhone-App, die auf einem Mac läuft, ein Titel, der in deiner Region nicht verkauft wird —, wird gar kein Wiederherstellungspunkt mehr angelegt, sodass eine Zeile nicht mehr anbieten kann, die Version wiederherzustellen, die sowieso schon läuft.

**Longbridge Desktop wird jetzt erfasst, auf beiden seiner Release-Züge.** Stable und Preview bekommen jeweils Versionserkennung, Ein-Klick-Installation der offiziellen Apple-Silicon-Build und Versionshinweise, die im eigenen Fenster von DuoUpdater dargestellt werden, Illustrationen inklusive.

**Die Versionshinweise von WhatsApp erscheinen jetzt schon, während seine App-Store-Prüfung noch läuft.** Die in diesem Fenster angezeigte App-Store-Seite war unter einem Namen abgelegt, den die Suche nie zuordnen konnte, sodass der Hinweis-Bereich leer blieb, statt sie anzuzeigen.

**Der Menü-Kopf zeigt jetzt mehr auf weniger Raum.** Die eigene Version von DuoUpdater steht jetzt neben seinem Namen, und ein Klick darauf öffnet die eigenen Versionshinweise. „Alle aktualisieren" ist auf eine eigene Zeile gezogen, wo ein übersetztes Label genug Platz hat, um ganz gelesen zu werden, und die Aktionen am unteren Rand sind jetzt Icons. Das Banner, das früher ankündigte, dass DuoUpdater sich selbst aktualisiert, ist verschwunden — stattdessen leuchtet das Glitzersymbol neben der Version auf und bleibt an, bis man gelesen hat, was sich geändert hat. Eine Statuszeile, die zu lang für das Menü ist, endet jetzt in Auslassungspunkten, statt es in die Breite zu ziehen.

**Einstellungen schneidet die angebotenen Update-Optionen nicht mehr ab.** Die beiden Menüs, die festlegen, wie ein Update angewendet wird, schnitten ihre eigenen Beschriftungen in mehreren Sprachen ab. Die Formulierungen sind jetzt kürzer, und wo die Wörter es noch immer brauchen, springen sie in eine zweite Zeile.

**Neuigkeiten sagt jetzt, wann jede Version erschienen ist.** Jede Version in der Leiste trägt jetzt ihr Veröffentlichungsdatum.

## 0.3.63

**Architekturspezifische Updates wählen jetzt die Build, die dieser Mac tatsächlich ausführen kann.** Ein paar Apps veröffentlichen dieselbe Version zweimal in einem Sparkle-Feed — einmal für Apple Silicon und einmal für Intel —, und DuoUpdater entschied diesen Gleichstand bisher danach, welche Download-Adresse zufällig zuerst sortiert wurde. Es liest jetzt die Hardware-Anforderung des Feeds aus und, wo ein Anbieter das leer lässt, die Architektur im Dateinamen. Die native Build gewinnt konsequent, und eine Build, die dieser Mac nicht starten kann, wird nicht angeboten. GitHub-Releases bekommen dieselbe Behandlung, ohne ein völlig gesundes Recipe als kaputt zu bezeichnen, nur weil sein neuestes Artefakt eine andere Architektur adressiert.

**ChatGPT-Update-Prüfungen folgen jetzt dem Rollout-Track, der am Konto hängt.** OpenAI hält Business- und Enterprise-Konten manchmal auf einer älteren Desktop-Build, während eine neue zuerst Consumer-Konten erreicht. DuoUpdater ließ den Plan des Kontos bei dieser Prüfung bisher aus, was für alle stillschweigend den vorsichtigen Track wählte: Es konnte behaupten, die installierte Kopie sei irgendwie voraus, während ChatGPT selbst bereits eine neuere Build herunterlud, oder eine Build anbieten, die der eigene Updater der App gleich wieder ersetzen würde. Es sendet jetzt das Plan-Label aus dem ChatGPT/Codex-Anmeldestatus mit derselben Update-Anfrage, die auch die App stellt. Ist dieses Label nicht verfügbar, bleibt es beim vorsichtigen Verhalten; Zugangsdaten selbst werden nie in die Anfrage oder in Diagnosen aufgenommen.

**Ein Neustart, auf den macOS nie antwortet, kann nicht mehr jedes spätere Update blockieren.** Launch Services nimmt gelegentlich eine Anfrage an, eine aktualisierte App wieder zu öffnen, und meldet sich dann nie zurück. Die Zeile blieb dann für immer bei „Wird neu gestartet …" hängen, ihre Schaltfläche „Neu starten" blieb deaktiviert, und DuoUpdaters eigenes Update wartete dahinter. Ein Start, der nach einer Minute nicht geantwortet hat, wird jetzt als fehlgeschlagen freigegeben, sodass sich die Zeile erholt und der Rest des Updaters weiterarbeitet.

**Das Release-Protokoll bleibt gefüllt, auch wenn seine Scrollleiste schnell gezogen wird.** Der alte Lazy Stack konnte von einem großen Sprung überholt werden und ließ das Fenster kurz leer, während rund um die neue Position Zeilen erst erzeugt wurden. Das Protokoll verwendet jetzt eine wiederverwertende Liste, die direkt zum Ziel springen kann. Auch das Aktualisieren-Steuerelement behält beim Wechsel zwischen Pfeil und Ladeanzeige denselben Platzbedarf, sodass die unterste Zeile nicht mehr zuckt, wenn eine Prüfung startet.

**Download-Traffic markiert jetzt Updates, die einen Binärpatch verwendet haben.** Neue Downloads verzeichnen den Weg, der tatsächlich abgeschlossen wurde — nicht bloß, ob ein Patch angeboten wurde — und tragen in ihrer Historie ein Delta-Badge. Auch die unverkennbar kleineren Patch-Downloads aus 0.3.62 werden erkannt, obwohl sie aufgezeichnet wurden, bevor das Traffic-Verzeichnis überhaupt ein Feld für den Weg hatte.

## 0.3.62

**Updates laden jetzt nur noch das, was sich geändert hat, wenn der Entwickler es so veröffentlicht.** Manche Apps liefern zu jedem Release einen kleinen Patch mit — genug, um die vorhandene Version in die neue zu verwandeln, ohne das Ganze erneut herunterzuladen. DuoUpdater ignorierte diese bisher und lud jedes Mal das vollständige Paket. Es nimmt jetzt den Patch, wenn einer genau zur laufenden Build passt. ChatGPTs letztes Update kam so auf 1,9 MB statt 605 MB; Dockers auf 87 MB statt 582 MB. Das Ergebnis ist in beiden Fällen dieselbe Anwendung — dieselbe Signatur, dieselben Bytes, geprüft gegen den vollständigen Download, bevor das ausgeliefert wurde. Passt kein Patch zur vorhandenen Version, oder scheitert einer beim Anwenden, läuft der vollständige Download wie zuvor ab, sodass daran keine Installation scheitern kann.

**DuoUpdater lädt ein Update nicht mehr herunter, das eine App bereits selbst herunterlädt.** Viele Apps aktualisieren sich auch selbst, und wenn beide gleichzeitig nach derselben 600-MB-Datei greifen, kostete das bisher die Datei doppelt. DuoUpdater bemerkt jetzt einen laufenden Download und lässt ihn in Ruhe, und sagt das auch auf der Zeile, statt stillschweigend nichts zu tun. Stellt sich dieser Download als abgebrochen heraus, hört das Warten nach zehn Minuten auf mitzuzählen, sodass nichts dauerhaft blockiert bleibt.

**Ein Update, das eine App bereits selbst vorbereitet hat, wird nicht mehr überschrieben.** Apps, die sich selbst aktualisieren, laden häufig im Hintergrund herunter und warten dann darauf, dass man sie beendet, bevor die neue Version eingesetzt wird. Darüber zu installieren sah aus, als hätte es funktioniert, machte sich dann aber in dem Moment rückgängig, in dem man die App beendete — und wo die eigene ausstehende Version der App älter war als das, was DuoUpdater gerade installiert hatte, landete man am Ende weiter zurück, als man begonnen hatte. Solche Updates dürfen jetzt zu Ende laufen, unabhängig davon, welche Version sie mitbringen.

**Auch DuoUpdaters eigene Updates sind kleiner geworden.** Seine Releases liefern jetzt dieselbe Art von Patch mit, sodass eine Aktualisierung von einer kürzlich installierten Version nur noch ein paar hundert Kilobyte statt elf Megabyte lädt.

## 0.3.61

**Erklärungen, die ein Update hinterlassen hat, räumen sich jetzt selbst auf.** Übergibt DuoUpdater eine App an deren eigenen Updater, sagt die Zeile das — „in den Vordergrund geholt, damit der eigene Updater das Update anwendet". Dieser Satz blieb bisher für immer stehen: Das Einzige, was ihn je entfernte, war der Start eines weiteren Updates derselben App, sodass er noch lange nach der Landung des Updates unter der Zeile stand und etwas beschrieb, das schon Stunden zuvor abgeschlossen war. Er verschwindet jetzt, sobald die App auf dem neuesten Stand ist. Eine Warnung, dass ein Update ohne Wiederherstellungspunkt angewendet wurde, bleibt bewusst unangetastet, denn die beschreibt das bereits geschehene Update und wird erst wichtig, sobald es vorbei ist.

**Ein abgelehntes Update erklärt sich jetzt in deiner Sprache.** Installiert bereits etwas anderes — DuoUpdater, das einen Stapel abarbeitet, oder `duo` im Terminal —, sagt die Zeile das. Diese Meldung war bisher rein englisch, in einem sonst übersetzten Fenster, und endete mit einer Prozessnummer: nützlich in einem Terminal, aber nichts, worauf man in einem Menü reagieren kann. Sie ist jetzt übersetzt und sagt, was zu tun ist, statt wer die Sperre hält. Das Kommandozeilen-Tool gibt die Prozessnummer weiterhin aus, dort, wo man damit etwas anfangen kann.

**Die Zeile zum Veröffentlichungsmuster, samt Uhrzeit, richtet sich jetzt nach deiner Ländereinstellung.** Release-Protokoll → Muster fasste bisher alles zusammen als „Erscheint am häufigsten Freitag, gegen 18 Uhr". Sprachen, die die Namen der Wochentage beugen, können das mit dem Tag mitten im Satz nicht korrekt ausdrücken und bekamen die falsche Wortform — Russisch las sich als „Чаще всего выходит пятница", wo es „по пятницам" heißen müsste. Es ist jetzt ein Label: „Spitze: Freitag, gegen 18 Uhr", was überall korrekt ist. Die Uhrzeit zieht mit — schreibt dein Mac Uhrzeiten im 24-Stunden-Format, tut das jetzt auch dieses Label, ebenso auf der Achse des Diagramms.

**Die Einstellungssuche versteht jetzt die Wörter auf deinem Bildschirm.** Das Suchfeld über der Seitenleiste der Einstellungen glich gegen eine Liste zusätzlicher Begriffe ab, die auf Englisch geschrieben und nie übersetzt worden war — „rollback" fand so den Weg zu Allgemein, während „Zurücksetzen" und „回滚" nichts fanden. Diese Begriffe existieren jetzt in jeder Sprache, die DuoUpdater spricht. Die englischen Begriffe funktionieren weiterhin zusätzlich in jeder Sprache, weil die Dokumentation, aus der sie stammen, auf Englisch ist.

## 0.3.60

**„Alle anzeigen" einzuschalten lässt das Menü nicht mehr stocken.** Die vollständige Liste aller vorhandenen Apps wurde jedes Mal, wenn sie erschien, komplett vermessen — auf einem Mac mit 127 Apps kam das auf etwa eine Sekunde Arbeit, fast ausschließlich für das Layout von Zeilen weit unterhalb der sichtbaren. Bezahlt wurde das bei jedem Umschalten, nicht nur beim ersten. Jetzt werden nur noch die tatsächlich sichtbaren Zeilen aufgebaut.

**Eine Erklärung, die von einem gescheiterten Update übrig geblieben ist, überlebt den Fehlschlag nicht mehr.** Konnte ein Update nicht angewendet werden — etwa weil schon etwas anderes installierte —, sagte die Zeile das in Rot. Nichts entfernte diese Zeile je: Landete das Update später doch noch und die Zeile kehrte zu einem Häkchen zurück, saß die alte Erklärung immer noch darunter und blieb dort über jede weitere erneute Prüfung hinweg, bis DuoUpdater neu gestartet wurde. Sie verschwindet jetzt, sobald die App auf dem neuesten Stand ist. Gründe, die zu noch ausstehenden Updates gehören, bleiben unangetastet, sodass ein Grund, den man noch nicht gelesen hat, nicht von einer im Hintergrund laufenden Prüfung gelöscht werden kann.

**Die Schaltfläche „Alle aktualisieren" ändert ihre Größe nicht mehr mit der Länge der Liste.** Standen nur wenige Updates aus, wurde sie eine Nummer kleiner gezeichnet, als sie sein sollte, mit achtzig Punkten leerem Raum daneben, und sprang auf ihre richtige Größe zurück, sobald die Liste wuchs. Jetzt behält sie die Größe, die sie haben soll.

## 0.3.59

**Download-Traffic sagt jetzt, zu welcher Build ein Update gewechselt hat, nicht nur zu welcher Version.** Etliche Apps liefern mehrere Builds unter demselben Versionsnamen aus — Surge hat vier getrennte Releases als „6.9.0" herausgebracht —, sodass solche Zeilen „6.9.0 → 6.9.0" lasen und nichts aussagten. Sie lesen jetzt „6.9.0 (12028) → 6.9.0 (12030)", und zwar nur dann, wenn der Versionsname allein nicht genügt; hat sich die Version bereits geändert, wäre die Build-Nummer nur Rauschen und bleibt weg.

**Die verzeichnete Build ist die, die tatsächlich gelandet ist — ausgelesen aus der App selbst, sobald das Update sitzt.** Nicht die Zahl, die der Update-Feed des Entwicklers verkündet hat — Feeds melden durchaus falsch, und so funktioniert es auch für die Stellen, die überhaupt keine Build-Nummer veröffentlichen: GitHub, Homebrew und der App Store. Bei einem Update, das noch auf das Installationsfenster von macOS wartet, wird nie geraten; für es wird nichts verzeichnet, bevor es real ist.

**Ein Download, der nichts verändert hat, wird jetzt auch so markiert.** Gelegentlich lädt und installiert ein Update genau die Build, die schon auf dem Mac lag — eine falsche Versionsnummer auf Entwicklerseite, ein Mirror, der ausliefert, was man ohnehin schon hat. Das ist reale Bandbreite für kein Ergebnis, und genau das will man im Traffic-Fenster sehen. Solche Zeilen tragen jetzt ein Label „keine Änderung". Vor diesem Release aufgezeichnete Downloads haben keine Build-Nummern zum Vergleichen, deshalb bleiben sie unangetastet, statt geraten zu werden: unbekannt ist nicht dasselbe wie unverändert.

## 0.3.58

**DuoUpdater zeigt jetzt, was das Aktuellhalten deiner Apps an Downloads wirklich gekostet hat.** Es hat schon die ganze Zeit über, byte-genau, jedes für dich geladene Update mitgezählt — nur hatte die Zählung nirgends einen Platz zum Erscheinen, sodass die Zahl in einer Datei lag, die niemand lesen konnte. Es gibt jetzt ein Download-Traffic-Fenster, das über die Diagramm-Schaltfläche am unteren Rand des Menüs geöffnet wird, mit dem Wert dieses Monats direkt neben dieser Schaltfläche, sodass die häufigste Frage beantwortet ist, ohne irgendetwas zu öffnen. Darin: die Gesamtsumme, die letzten drei Monate nebeneinander mit der Veränderung dazwischen, eine Aufschlüsselung, woher die Bytes kamen, und jede App gereiht nach dem, was sie gekostet hat — ein Klick zeigt jedes einzelne ihrer Updates, von welcher zu welcher Version es ging und wie groß der Download war.

**Die Summe ist ehrlich darüber, was sie nicht sehen kann.** Homebrew, der App Store und Apps, die über ihren eigenen eingebauten Updater aktualisieren, laden ihre Bytes selbst, und DuoUpdater bekommt sie nie zu fassen — die Zahl ist also eine Untergrenze, keine vollständige Abrechnung. Das war schon immer so; geändert hat sich, dass das Fenster das jetzt dauerhaft sagt, statt nur auf dem leeren Bildschirm, den man sieht, bevor überhaupt etwas verzeichnet wurde.

**Apps, die inzwischen umbenannt oder gelöscht wurden, behalten ihre Historie.** Traffic wird gegen den Ort verzeichnet, an dem eine App auf der Festplatte liegt — das ist auch, was zwei Channels derselben App, etwa Android Studio Canary und Beta, auseinanderhält, statt sie zusammenzuzählen. Der Preis dafür: Eine App umzubenennen lässt ihre Vergangenheit unter einem Namen zurück, den es nicht mehr gibt. Als OpenAI Codex in ChatGPT umbenannte, teilte das 30 GB an Downloads auf zwei Einträge auf, die wie Duplikate aussahen. Solche Einträge werden jetzt am unteren Ende der Liste gruppiert und abgeblendet dargestellt, unter einer Überschrift, die sagt, was sie sind. Nichts wird weggeworfen, und die Gesamtsumme schließt sie weiterhin ein.

## 0.3.57

**Einer App, die aus dem App Store installiert wurde, wird nicht mehr der eigene Download des Entwicklers angeboten.** Viele Apps werden gleichzeitig an zwei Orten veröffentlicht — im Store und als Direkt-Download von der eigenen Seite des Entwicklers — unter derselben Identität, aber als tatsächlich unterschiedliche Builds. Der Direkt-Download ist meist voraus, weil er nicht auf die Store-Prüfung warten muss. DuoUpdater prüft bei einer aus dem Store installierten App zuerst den Store, aber wenn diese Prüfung aus irgendeinem Grund fehlschlug — eine abgebrochene Verbindung, ein Storefront, der nicht antwortete —, ging es stillschweigend zur Seite des Entwicklers über und bot an, was dort lag. Bei WhatsApp zeigte sich das als „26.32.75 → 26.33.19": eine echte Version, aber von der falschen Stelle. Sie zu übernehmen hätte die Store-Kopie durch eine ersetzt, die der App Store nie wieder hätte aktualisieren können. Aus dem Store installierte Apps werden jetzt ausschließlich gegen den Store geprüft, und sonst nirgends.

**Den Update-Channel einer App innerhalb dieser App umzuschalten wird jetzt sofort erkannt.** Manche Apps lassen dich zwischen ihren normalen Releases und ihren Beta-Releases wählen — Surge, Tailscale, Fork, OrbStack, IINA, Alfred und andere —, und DuoUpdater folgt der jeweils getroffenen Wahl, sodass es nie eine Build anbietet, die man nicht angefordert hat. Bisher bemerkte es die Änderung nur, wenn man die App beendete oder eines der Fenster von DuoUpdater öffnete. Keins von beidem deckt ab, was Leute tatsächlich tun: die Einstellung umschalten, die App weiterlaufen lassen und nur einen Blick in die Menüleiste werfen. Schlimmer noch: Apps speichern diese Einstellung auf der Festplatte, wann es ihnen passt, statt im Moment des Klicks — bei Surge dauerte das hier fünf Minuten —, sodass selbst ein Beenden zu früh gelesen werden konnte. DuoUpdater beobachtet jetzt die Einstellung selbst auf Änderungen und prüft diese eine App innerhalb von ein, zwei Sekunden erneut. Surge zurück auf normale Releases umzuschalten entfernt die Beta-Version jetzt sofort aus der Zeile, statt sie bis zu einer Stunde lang dort stehen zu lassen.

**Eine Prüfung, die nichts erreichen konnte, sieht nicht mehr aus wie ein sauberer Befund.** Scheiterten alle Quellen — kein Netzwerk, oder ein Proxy, der Verbindungen still verweigert, während das WLAN-Symbol weiterhin sagt, alles sei in Ordnung —, wurden die gescheiterten Apps versteckt, und das Panel meldete „127 Apps · aktuell". Das ist genau derselbe Bildschirm, den man sieht, wenn wirklich alles aktuell ist, wodurch eine gescheiterte Prüfung von einer erfolgreichen nicht zu unterscheiden war. Das Panel sagt jetzt, wie viele Apps es nicht erreichen konnte, zeigt, was schiefgelaufen ist, und bietet an, genau diese erneut zu versuchen — nur diese, ohne alles anzurühren, was tatsächlich geprüft werden konnte.
